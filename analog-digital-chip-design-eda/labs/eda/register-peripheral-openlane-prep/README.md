# Register-peripheral OpenLane preparation

This package stages the model-generated, review-approved address-zero CSR
decode repair into the hierarchical `peripheral` wrapper. It is local
open-source OpenLane evidence with extracted STA and physical checks; it is not
commercial signoff or tapeout evidence.
