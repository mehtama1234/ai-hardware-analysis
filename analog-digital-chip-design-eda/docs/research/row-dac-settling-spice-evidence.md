# Row-DAC Settling SPICE Evidence

This page is generated from the row-DAC settling SPICE measurement.

It checks the first executable converter handoff test: whether a simple 10-bit row-driver load settles within half of one DAC step before the analog array is sampled.
