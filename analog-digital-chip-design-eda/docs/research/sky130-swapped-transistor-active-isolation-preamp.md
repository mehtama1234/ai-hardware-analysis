# Sky130 Swapped Transistor Active Isolation Preamp

This page is generated from the targeted swapped-output transistor isolation test.

The prior transistor isolation pair produced enough corrected voltage but inverted the sign. This test keeps the prior best isolation setting and swaps the isolation outputs into the preamp. It checks whether the failure is a simple output-order problem.

It is schematic transistor evidence only. It is not layout, not DRC/LVS, not noise evidence, and not accepted converter evidence.
