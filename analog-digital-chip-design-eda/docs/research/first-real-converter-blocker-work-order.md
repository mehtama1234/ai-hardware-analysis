# First Real Converter Blocker Work Order

This page renders the generated work order for the remaining first-candidate blockers.

It takes the rejected rehearsal payload and turns the remaining failures into concrete circuit measurements and payload outputs.
