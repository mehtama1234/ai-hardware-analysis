# Sky130 Corrected-Convention Isolation Range Stress

The isolated latch-input branch passed at the target edge. The next question is whether it remains stable when the input difference moves above that edge.

This page appends the range-stress evidence. It keeps the same tiny capacitive isolation, the same sampled nodes, and the corrected `outp - outn` latch output convention, then tests both signs at one, two, and five times the target differential.
