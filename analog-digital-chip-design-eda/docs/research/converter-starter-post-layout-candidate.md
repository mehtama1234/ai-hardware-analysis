# Converter Starter Post-Layout Candidate

This page records a separate starter candidate packet built from the extracted starter layout cells.

It is not the canonical accepted post-layout evidence path. It proves only that starter layout outputs can be shaped into the strict candidate payload format without writing accepted evidence.

