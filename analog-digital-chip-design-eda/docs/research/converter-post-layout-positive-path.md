# Converter Post-Layout Positive Path

This page shows that strict post-layout intake can accept a complete command-path fixture when all referenced files exist.

It is not converter evidence. The fixture is temporary and synthetic, so it only proves that the validator and rerun command path can work when a real payload arrives.
