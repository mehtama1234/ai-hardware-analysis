# Shared Converter Loading SPICE Evidence

This page is generated from the shared-converter loading evidence artifact.

It checks the third executable converter handoff test: whether the readout node still settles within half of one ADC step when the converter is shared through a simple muxed load model.
