# Analog Converter Layout Starter Package

This page is generated from `evidence/aimc-simulator-adapters/analog-converter-layout-starter-package.md`.

It creates the source-side layout workbench that comes after the layout work order and before any strict post-layout payload can be accepted.
