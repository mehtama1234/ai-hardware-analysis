# Converter Post-Layout Candidate Workspace Audit

This page reports whether the candidate converter workspace is still a scaffold.

The audit is intentionally separate from preflight. It is for humans editing the staging folder. It counts placeholder fields, checks whether the named netlist, model files, and rerun artifact exist, and reminds the reader that a `template_only` payload cannot be submitted.
