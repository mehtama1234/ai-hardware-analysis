# Converter Post-Layout Candidate Workspace

This page shows the staging folder for the first real converter post-layout or measured-silicon payload.

The workspace is not evidence. It is a place to assemble the real files before preflight and submission. The default payload is marked `template_only` and must be rejected until every placeholder is replaced with real extracted or measured values.
