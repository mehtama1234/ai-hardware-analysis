# First Real Converter Candidate Packet

This page renders the generated first-candidate packet.

The packet collects the local measurements that can honestly travel toward a converter payload, then names the measurements that are still missing.

It should be read after `First Real Converter Candidate Execution Plan`.
