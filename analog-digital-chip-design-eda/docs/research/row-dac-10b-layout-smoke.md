# Row DAC 10b Layout Smoke

This page records the first named physical converter starter cell.

It is deliberately not accepted post-layout evidence. It only proves that `row_dac_10b.mag` exists in the workbench and can be extracted by the local Sky130 Magic path.

It is not a production DAC proof.
