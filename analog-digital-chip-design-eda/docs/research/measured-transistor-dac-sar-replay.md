# Measured Transistor DAC SAR Replay

This page puts the measured 16-code transistor DAC transfer into the SAR decision algorithm. The comparator is an exact voltage comparison, so this is a transparent transfer-function replay rather than a same-deck transistor comparator proof.

The generated evidence report is appended below.
