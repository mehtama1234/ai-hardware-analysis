# Sky130 Known-Good-Shape Preamp Transient Latch Debug

This page is generated from the first successful debug-ladder transient step.

The preamp uses the OP-passing known-good shape, starts from the measured OP initial point, and keeps the regenerative latch disconnected. It checks whether both target-edge signs settle with enough margin before latch reconnection.

It is preamp transient evidence only. It is not latch resolution, not kickback, not SAR, not layout, and not accepted post-layout converter evidence.
