# Converter Circuit-Simulation Estimate

This page is generated from the converter evidence artifacts.

It checks the 10-bit row-drive and 12-bit readout target with a small behavioral circuit model before the project spends effort on layout. The model keeps the claim narrow: it can show whether the target is internally coherent, but it cannot replace extracted parasitics, measured converter energy, or silicon noise.
