# Converter Post-Layout Payload Validator

This page shows the executable gate for future extracted converter evidence.

The validator is deliberately strict. It accepts a payload only when the converter target, extracted circuit, simulation conditions, energy, latency, noise, area, sharing rule, and break-even rerun all describe the same physical replacement claim.
