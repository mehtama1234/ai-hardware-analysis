# Sky130 Physical DAC SAR Sequence

This page sequences retained-bit SAR decisions using the physical Sky130 DAC and physical transistor comparator. Each trial code is measured in a physical transient before the next bit is chosen.

The generated evidence report is appended below.
