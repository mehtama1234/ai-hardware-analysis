# Converter Post-Layout Break-Even Rerun Path

This page shows the executable step after a future converter post-layout payload passes validation.

The rerun is separate from validation. Validation checks whether the payload is real enough to use. The rerun checks whether the real converter is worth using.
