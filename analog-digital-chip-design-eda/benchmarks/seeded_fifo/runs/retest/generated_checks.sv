// Generated by verification_platform; 3 approved plans
module generated_checks (input logic clk, input logic rst, input logic enable, input logic [3:0] counter_q);
  // requirement: REQ-FIFO-RESET
  // rationale: requirement identifiers mapped to a clocked reset-zero assertion
  assert property (@(posedge clk) rst |-> count == '0);
  // requirement: REQ-FIFO-BOUNDS
  // rationale: FIFO bound requirement mapped to a depth-two count invariant
  assert property (@(posedge clk) count <= 2);
  // requirement: REQ-FIFO-WRITE
  // rationale: FIFO write requirement mapped to a full-gated count increment
  assert property (@(posedge clk) wr_en && count < 2 |=> count == $past(count) + 1'b1);
endmodule
