// Generated by verification_platform; 3 approved plans
module generated_checks (input logic clk, input logic rst, input logic enable, input logic [3:0] counter_q);
  // requirement: REQ-COUNTER-RESET
  // rationale: requirement identifiers mapped to a clocked reset-zero assertion
  assert property (@(posedge clk) rst |-> counter_q == '0);
  // requirement: REQ-COUNTER-ENABLE
  // rationale: enable requirement mapped to a one-cycle increment assertion
  assert property (@(posedge clk) disable iff (rst) enable |=> counter_q == $past(counter_q) + 1'b1);
  // requirement: REQ-COUNTER-HOLD
  // rationale: requirement identifiers mapped to a one-cycle stability assertion
  assert property (@(posedge clk) disable iff (rst) !enable |=> $stable(counter_q));
endmodule
