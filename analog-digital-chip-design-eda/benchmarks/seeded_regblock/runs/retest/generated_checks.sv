// Generated by verification_platform; 3 approved plans
module generated_checks (input logic clk, input logic rst, input logic enable, input logic [3:0] counter_q);
  // requirement: REQ-REG-RESET
  // rationale: requirement identifiers mapped to a clocked reset-zero assertion
  assert property (@(posedge clk) rst |-> reg0 == '0);
  // requirement: REQ-REG-ADDRESS
  // rationale: register address requirement mapped to a nonzero-write stability assertion
  assert property (@(posedge clk) wr_en && addr != 0 |=> $stable(reg0));
  // requirement: REQ-REG-WRITE
  // rationale: register write requirement mapped to an addressed data update assertion
  assert property (@(posedge clk) wr_en && addr == 0 |=> reg0 == $past(wdata));
endmodule
