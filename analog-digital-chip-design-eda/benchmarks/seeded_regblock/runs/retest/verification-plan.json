[
  {
    "assertion": "assert property (@(posedge clk) rst |-> reg0 == '0);",
    "id": "CHECK-REQ-REG-RESET",
    "kind": "sva",
    "rationale": "requirement identifiers mapped to a clocked reset-zero assertion",
    "requirement_id": "REQ-REG-RESET"
  },
  {
    "assertion": "assert property (@(posedge clk) wr_en && addr != 0 |=> $stable(reg0));",
    "id": "CHECK-REQ-REG-ADDRESS",
    "kind": "sva",
    "rationale": "register address requirement mapped to a nonzero-write stability assertion",
    "requirement_id": "REQ-REG-ADDRESS"
  },
  {
    "assertion": "assert property (@(posedge clk) wr_en && addr == 0 |=> reg0 == $past(wdata));",
    "id": "CHECK-REQ-REG-WRITE",
    "kind": "sva",
    "rationale": "register write requirement mapped to an addressed data update assertion",
    "requirement_id": "REQ-REG-WRITE"
  }
]
