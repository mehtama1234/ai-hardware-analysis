[
  {
    "assertion": "assert property (@(posedge clk) rst |-> control == '0);",
    "id": "CHECK-REQ-CSR-RESET",
    "kind": "sva",
    "rationale": "requirement identifiers mapped to a clocked reset-zero assertion",
    "requirement_id": "REQ-CSR-RESET"
  },
  {
    "assertion": "assert property (@(posedge clk) wr_en && addr != 0 |=> $stable(control));",
    "id": "CHECK-REQ-CSR-ADDRESS",
    "kind": "sva",
    "rationale": "register address requirement mapped to a nonzero-write stability assertion",
    "requirement_id": "REQ-CSR-ADDRESS"
  },
  {
    "assertion": "assert property (@(posedge clk) wr_en && addr == 0 |=> control == $past(wdata));",
    "id": "CHECK-REQ-CSR-WRITE",
    "kind": "sva",
    "rationale": "register write requirement mapped to an addressed data update assertion",
    "requirement_id": "REQ-CSR-WRITE"
  },
  {
    "assertion": "assert property (@(posedge clk) rst |-> ready == '0);",
    "id": "CHECK-REQ-CSR-READY",
    "kind": "sva",
    "rationale": "requirement identifiers mapped to a clocked reset-zero assertion",
    "requirement_id": "REQ-CSR-READY"
  }
]
