{
  "problem_in_depth": "Start with a single, stubborn physical fact: it is cheap to do arithmetic, and expensive to fetch the numbers you want to do arithmetic on. Inside a computer chip there is a part that does the math — adding, multiplying, comparing — and a separate, much larger part that holds all the numbers: the memory. The math part is astonishingly fast. It can finish a multiplication in a sliver of a billionth of a second. But before it can multiply two numbers, those two numbers have to physically arrive from wherever they are stored. And they are usually stored far away, in a different chip on the same circuit board, connected by wires. The trip down those wires takes hundreds of times longer than the multiplication itself. So the math part spends most of its life not doing math. It sits idle, waiting for data to show up, like a chef with a perfect knife standing at an empty cutting board because the vegetables are still being trucked in from a warehouse across town.\n\nThis is the root problem this entire chapter is about, and it has a name people use: the memory wall. The wall is the widening gap between how fast a chip can compute and how fast it can be fed. Over the past few decades, the ability to do arithmetic grew explosively, roughly doubling and doubling again. The ability to move data grew too, but far more slowly. Every year the chef got faster and the delivery truck barely did. The result is that for most real programs, the chip's speed is decided almost entirely by the delivery truck — by memory — and almost not at all by the knife. If you want a faster computer today, making the arithmetic faster usually does nothing, because the arithmetic was never the thing you were waiting on.\n\nWhy is moving data so expensive when moving math is not? It comes down to distance and physics. Storing a number means parking a tiny amount of electric charge somewhere. To store billions of numbers cheaply, you build a vast, dense grid of tiny charge-holding cells — this is the main memory, and to make it cheap and dense you place it in its own chip, physically separated from the fast logic. Now every number you want has to travel out of that grid, across a pin, down a wire on the circuit board, and into the compute chip. Wires have to be charged and discharged to carry a signal; longer wires hold more electrical 'weight' and take more time and more energy to flip. Physicists measure this in nanoseconds and picojoules, but the intuition is simple: distance costs time, and distance costs energy. Moving a number even a few centimeters across a board can cost far more energy than the multiplication you were going to perform with it. The computation is nearly free; the commute is the whole expense.\n\nTo hide this commute, engineers built a hierarchy — a set of progressively larger, slower, farther-away storage tiers. Right next to the math there are tiny, blazing-fast holding pens called caches (think of them as the small counter space right next to the chef, holding just the ingredients for the current dish). A little farther out is the big, cheap, slow main memory (the warehouse). Farther still are storage drives that hold everything permanently but are glacially slow to reach (the distant depot). The entire art of a fast computer is guessing what data the chef will need next and quietly pre-loading it onto the nearby counter before it is asked for, so that when the moment comes, the ingredient is already at hand. When the guess is right, the machine flies. When the guess is wrong — when the needed number is not on the counter and has to be fetched from the warehouse — everything stops and waits. A single wrong guess can cost hundreds of wasted cycles. Much of this chapter is about making those guesses better, or about arranging data so fewer guesses are needed at all.\n\nNow layer on modern artificial intelligence, and the problem goes from serious to dominant. A large language model — the kind of system that writes and converses — is fundamentally a colossal pile of numbers (its 'weights,' the learned settings that encode what it knows) that must be read, in full, every single time it produces even one word of output. The pile is so large it cannot fit on the fast nearby counter; it lives out in the warehouse and must be streamed across the expensive wires constantly. On top of the weights, the model keeps a growing set of 'running notes' about the conversation so far — a memo of everything already said, which it must re-read to decide what to say next — and this memo also lives in memory and grows with every word. The striking measured reality is that when one of these models generates text, the fast math hardware sits mostly idle — in one study only about a fifth busy — while the machine spends its time hauling weights and notes back and forth. The bottleneck is not thinking; it is fetching. This is why so much of modern computer-hardware research, and this whole theme, has converged on one obsession: stop moving so much data, move it a shorter distance, or move the math to where the data already lives.\n\nThat last idea is the deepest reframing in the field, and it is worth stating plainly because it sounds backwards. For seventy years the assumption was: data is stored in memory, and when you need to compute, you bring the data to the processor. But if the commute is the whole expense, why not do the opposite — leave the data still and send the arithmetic out to meet it? Put small, simple math units directly inside or right beside the memory chips, so a number can be added or multiplied without ever making the long trip to the main processor. This is the second half of the chapter's title — 'moving the math to the data' — and it turns the traditional layout inside out. It is harder than it sounds, because memory chips were never designed to compute and computing chips were never designed to store, and bolting the two together strains both. But the potential payoff is enormous: if the data never has to travel, the wall stops mattering."
  ,
  "approaches": [
    {
      "title": "Keep the right data close: caches and smarter eviction",
      "prose": "The oldest and most universal answer to the memory wall is the cache: a small, fast pocket of storage sitting right next to the math, holding a copy of the data most likely to be needed soon. Because it is small, it can only hold a tiny fraction of everything, so the machine is constantly making a brutal decision — when a new number arrives and the pocket is full, which old number do you throw out to make room? Get this right and the needed data is almost always already at hand; get it wrong and you keep evicting exactly the thing you were about to use, forcing expensive trips to the warehouse. This 'which to throw out' decision is called a replacement policy, and a surprising amount of hardware research is devoted to it.\n\nThe subtlety is that not all data is equal, and the standard policies pretend it is. A major insight running through this work is that instructions — the program's own recipe steps — are far more costly to lose than ordinary data, because when the chip cannot find its next instruction it simply freezes, unable to make any progress at all. Several efforts (isca-2025-060, micro-2025-054, micro-2025-116, hpca-2025-089) rebuild cache eviction to specifically protect instructions, or to tag each piece of data with how 'hot' (frequently reused) it is so the cache keeps the hot things and discards the cold. Others (isca-2025-081, micro-2025-093, hpca-2025-120) chase near-perfect eviction decisions, some even proving what the theoretically best possible policy would be and then approximating it cheaply. This matters enormously for the sprawling software that runs data centers, whose recipe is so large it constantly overflows the small nearby storage.\n\nThe tradeoff is that cleverer policies need more bookkeeping — extra little tables that themselves take up space and energy to maintain. The whole game is squeezing better guesses out of the least possible bookkeeping. A recurring finding is that a simple, well-tuned rule often matches an elaborate learned one, so the elegant designs are the ones that get most of the benefit for almost none of the overhead."
    },
    {
      "title": "Fetch data before it is asked for: prefetching",
      "prose": "If the commute is the problem, one fix is to start the commute early. Prefetching means predicting which numbers the program will want next and sending for them in advance, so they arrive on the fast nearby counter before the moment they are needed — hiding the long trip behind work the machine is already doing. When it works, the wait vanishes; the data is simply there. The engine that does this watches the stream of memory requests, spots a pattern (say, the program is marching through a list one item after another), and races ahead to grab what comes next.\n\nThe hard cases are where the pattern is not a simple march. Many important programs chase pointers — they read one number to find out where the next number lives, which they must read to find the one after that, a chain where each step depends on the last (hpca-2025-021, isca-2025-036, isca-2025-069). You cannot run ahead when you do not know where you are going until you arrive. A cluster of work attacks exactly this: some use the compiler (the tool that translates human-written programs into machine steps) to analyze the code ahead of time and leave hints about the coming access pattern (isca-2025-036, isca-2025-069, isca-2025-102), some profile the program on a practice run to learn its habits (isca-2025-069, isca-2025-102), and some feed the predictor richer clues about the timing and ordering of past accesses (hpca-2025-021, hpca-2025-045-adjacent efforts).\n\nPrefetching's danger is the mirror image of its promise: a wrong guess is not free. Fetching data that is never used wastes the very memory bandwidth you were trying to conserve, and can shove genuinely useful data off the counter. So the mature designs spend as much effort on restraint as on aggression — filtering out low-confidence guesses (hpca-2025-087, hpca-2025-057), gating prefetches by how busy the memory system already is, and, in tiny battery-scavenging devices that may lose power at any instant, suppressing prefetches that would simply be lost before they are ever used (isca-2025-030, hpca-2025-028)."
    },
    {
      "title": "Build a ladder of memory: tiering across fast and slow pools",
      "prose": "No single kind of memory is both big and fast and cheap — you get to pick two. Fast memory is expensive and small; cheap memory is large and slow. So instead of choosing, engineers stack them into tiers: a modest amount of fast memory close in, backed by a large amount of slower, cheaper memory farther out, backed by even slower storage beyond that. The trick is to keep the frequently-touched ('hot') data in the fast tier and let the rarely-touched ('cold') data settle into the slow tier — and to keep re-sorting as the program's attention shifts. Guess hotness well and the machine feels as fast as the expensive tier while costing as little as the cheap one.\n\nA new plumbing standard called CXL (think of it as a fast, flexible expansion port that lets you bolt large pools of extra memory onto a machine, or even share memory across many machines) has made this tiering suddenly urgent and practical, and a large share of this theme is about managing it (hpca-2025-125, isca-2025-121, asplos-2025-125, asplos-2025-130, asplos-2025-087). The extra memory reached over this port is a little slower than the memory soldered right next to the processor — measured at a few hundred billionths of a second of added delay — so the whole question becomes: which pages of data are worth keeping in the fast local pool, and which can be exiled to the roomier CXL pool without anyone noticing? The answers range from tracking both long-term and momentary popularity of each page (hpca-2025-125), to letting the memory port's own controller precisely count accesses so the system stops mistaking merely-warm data for genuinely-hot data (asplos-2025-130), to letting a learning agent tune the migration strategy on the fly (isca-2025-121).\n\nThe tension here is that the sorting itself costs something. Moving a page from the slow tier to the fast tier takes time and bandwidth, and the bookkeeping to track what is hot takes memory of its own. Migrate too eagerly and you spend all your effort shuffling; migrate too lazily and hot data languishes in the slow tier. The best systems are the ones that track hotness cheaply and move data only when the move will clearly pay for itself."
    },
    {
      "title": "Move the math to the data: processing in and near memory",
      "prose": "This is the most radical rethink and the beating heart of the chapter's title. If hauling data to the processor is the whole expense, invert the arrangement: leave the data where it sits and place small arithmetic units inside the memory chips themselves, or right beside them. Then a number can be multiplied or added without ever making the long trip out to the main processor. There is a spectrum here. At one end, tiny compute units are placed near the memory banks (called processing-in-memory or near-memory processing); at the other, the memory cells themselves are coaxed into doing the arithmetic as a side effect of how they are read (called compute-in-memory). Both share one goal: keep the data still.\n\nThis reframing is a particularly good fit for modern AI, because generating text is mostly a torrent of simple multiply-and-add operations over an enormous pile of weights that must be read in full — exactly the case where the data movement dwarfs the computation. Several ambitious systems build entire AI-serving machines out of memory-with-math and skip the flexible general-purpose accelerator chip altogether (asplos-2025-051, micro-2025-057, micro-2025-113, hpca-2025-050, hpca-2025-077). One reports serving a large language model faster and at a fraction of the energy of a rack of conventional accelerators, precisely because its arithmetic lives inside the memory and the weights barely move (asplos-2025-051). Others target the specific memory-hungry steps — the running-notes lookup, the state updates of newer model designs — and offload just those to memory-side compute while a conventional chip handles the rest (hpca-2025-050, hpca-2025-077, micro-2025-117, micro-2025-119).\n\nThe cost of this approach is real and shapes every design. Memory chips are built for density and cheapness, not computation; there is very little room to add logic without either shrinking the memory or blowing the power and heat budget. So the work is a constant negotiation: keep the in-memory math units small and simple (asplos-2025-051, micro-2025-057 shares one unit between two memory banks to halve its footprint), decide which operations are worth doing in memory versus shipping out (asplos-2025-054, micro-2025-067), and rearrange data so the in-memory units are not starved or imbalanced (asplos-2025-102, hpca-2025-104). And because these chips must serve two masters — ordinary storage and now also computation — a whole sub-thread is about compilers and mapping tools that figure out how to lay a program across this strange hybrid substrate (isca-2025-074, isca-2025-096, isca-2025-098-adjacent, hpca-2025-102)."
    },
    {
      "title": "Do the filtering where the data lives: computing inside storage",
      "prose": "The same 'move the math to the data' logic applies one tier further out, at the permanent storage drives — and there the payoff can be even larger, because those drives are the farthest away and the slowest to reach. The classic waste is this: to find the few records you actually want, a program drags every record across the slow link from the drive into the main processor, checks each one, and throws almost all of them away. The commute was spent hauling data that was immediately discarded. In-storage computing puts a small amount of logic inside the drive so the useless records are filtered out before they ever leave (asplos-2025-007, isca-2025-000, isca-2025-095, hpca-2025-091, micro-2025-118-adjacent). Only the survivors make the trip.\n\nThis is especially potent for two workloads that dominate this theme. One is databases and analytics, where a query often asks for a tiny, highly selective slice of an enormous table — pushing the 'does this row match?' test down into the drive can cut the data that crosses the link by orders of magnitude (isca-2025-000, isca-2025-095, isca-2025-051, asplos-2025-092). The other is the retrieval step that feeds modern AI systems: before answering, these systems search a giant library of documents for the few most relevant passages, and that search is a bandwidth-hungry hunt through huge tables of numbers. A cluster of work performs that nearest-match search inside the storage or memory itself, so the machine only pulls out the handful of passages it actually needs (isca-2025-020, isca-2025-076, isca-2025-091, isca-2025-072, isca-2025-066, asplos-2025-112).\n\nThe difficulty is that storage drives speak in coarse, rigid chunks and have their own quirky internal rules about how data can be written and erased, so teaching them to compute means carefully co-designing the drive's logic with the software that uses it (hpca-2025-091, isca-2025-051, micro-2025-059). And the filtering logic must be flexible enough to handle the many shapes real queries take, which is why several designs build a small, purpose-made instruction set so the host can ship arbitrary filtering rules down to the drive rather than hard-wiring a fixed few (isca-2025-051, asplos-2025-007)."
    },
    {
      "title": "Send fewer bits and skip the useless ones: compression and eliminating dead traffic",
      "prose": "If you cannot make the wire faster, send less down it. Two complementary ideas fall under this banner. The first is compression: squeeze the numbers into fewer bits before they travel, then expand them at the far end, so the same wire effectively carries more useful information. The catch is that expanding compressed data takes work, and if that work is slow it eats the very time you saved — so the clever designs build extremely fast, parallel unpackers that can restore data at the pace the processor consumes it (isca-2025-021). For AI specifically, the model's weights and running notes can be shrunk dramatically, multiplying the effective size of the fast memory and cutting the traffic (isca-2025-021, micro-2025-103, which cheekily repurposes the video-compression hardware already sitting on graphics chips to compress AI data instead).\n\nThe second idea is subtler and almost philosophical: a large fraction of the data a computer moves is not useful at all. It writes back numbers that a program has already finished with and will never read again; it fetches memory that has not yet been filled with anything meaningful. This is 'dead' traffic — pure commute with no cargo. One line of work has the software (the compiler and the memory manager) tell the hardware which regions are dead so it can simply skip moving them, cutting a quarter of memory traffic in warehouse-scale systems for free (asplos-2025-015). Related efforts eliminate redundant copies entirely — deduplicating identical pages of memory (hpca-2025-115), or letting many machines share one copy of a process's state over the fast expansion port instead of each copying it (asplos-2025-014).\n\nThe underlying tradeoff is accuracy and complexity. Compression that discards a little precision (using 'smaller numbers' — fewer bits per value) saves the most but risks degrading the AI model's answers, so a delicate art has emerged around shrinking the unimportant values hard while protecting the critical few (isca-2025-021, isca-2025-109). And identifying dead or duplicate data requires cooperation across the software and hardware boundary, which only pays off in tightly integrated systems where one team controls the whole stack."
    },
    {
      "title": "Tame the irregular and the crooked accesses",
      "prose": "Everything above works best when memory is touched in tidy, predictable streams. But some of the most important workloads are the opposite: they touch memory in a scattered, unpredictable, jumping-around fashion. Graph analysis (following connections between people or web pages), recommendation systems, and sparse math (where most values are zero and only the scattered non-zeros matter) all leap around memory almost randomly. This is poison for the memory system, because memory is designed to deliver a fat chunk of neighboring data at once — and when you only want one number out of that chunk, you have hauled a whole crate to use a single item. The commute is paid in full but the cargo is almost entirely wasted.\n\nA broad set of work attacks this crooked-access problem from several angles. Some reorganize the data itself so that things touched together sit together, restoring locality where none existed (isca-2025-016, hpca-2025-030, hpca-2025-106). Some build specialized fetch engines that gather the scattered specific numbers efficiently and reorder the requests so the memory can serve them with less waste — one such engine reorders thousands of scattered requests at once, giving it hundreds of times more visibility than the ordinary memory controller and reclaiming most of the wasted bandwidth (isca-2025-055, isca-2025-036, hpca-2025-079). And many push the scatter-gather work down into or near the memory, so the wasteful hauling never crosses the long wire (hpca-2025-079, isca-2025-056, micro-2025-092, asplos-2025-138).\n\nAI's newer irregular workloads join this family too: graph neural networks (which learn over networks of connections) and the growing use of 'mixture of experts' models (which, for each word, wake up only a small, unpredictable subset of the model's parts) both create exactly this scattered-access headache (hpca-2025-070, hpca-2025-103, isca-2025-056, micro-2025-012, micro-2025-057). The through-line is that regularity is a resource: where the hardware cannot count on it, the software, the data layout, and sometimes new memory-side logic must manufacture it — because a memory system fed crooked accesses will waste most of its effort no matter how fast its wires are."
    },
    {
      "title": "Make the address lookup itself faster: page tables and translation",
      "prose": "There is a hidden tax on nearly every memory access that outsiders never see. Programs do not use the real, physical location of data; they use convenient made-up addresses, and the hardware must translate each made-up address into a real one before it can fetch anything. That translation is itself a lookup in a table that lives in memory — so a single data fetch can secretly require several memory fetches just to figure out where to go. To hide this, chips keep a tiny cache of recent translations close by; but when a program's data is spread across a vast range of addresses, that little cache overflows and the translations start missing, and suddenly every access drags a chain of extra memory trips behind it. This is a real and growing bottleneck, especially for the flexible, massively parallel chips used for AI (hpca-2025-053, isca-2025-035) and for the enormous, sprawling programs in data centers.\n\nThe responses are inventive. Some batch many translation lookups together so they can share work instead of each paying the full price (hpca-2025-053). One striking design notices that the address-translation table is shaped like a tree, and that graphics chips already contain specialized hardware for walking tree structures — built for rendering images — so it repurposes that idle image-rendering hardware to do address translations, gaining translation throughput almost for free (isca-2025-035). Others reduce the pressure at its source by keeping data in larger contiguous blocks so fewer translations are needed at all (hpca-2025-115), or attack the related problem on the instruction side where the recipe's own address lookups stall the machine (asplos-2025-025).\n\nThe reason this belongs in a memory chapter is that translation is memory movement in disguise: every translation miss is one or more extra trips to the warehouse, hidden underneath the trip you actually wanted to make. Speeding up the arithmetic does nothing for it; the only cures are the same family of cures as the rest of the chapter — cache the lookups better, predict them, batch them, or move them closer to where they resolve."
    }
  ],
  "where_it_stands": "The field has genuinely conquered the easy, regular cases. For programs that march predictably through memory, caches and prefetchers hide the commute so well that the memory wall barely shows — decades of refinement have wrung most of the waste out of these paths, and the remaining gains here are small, single-digit-percent tunings rather than breakthroughs. What is emphatically not solved is the hard core of the problem: the irregular, scattered, pointer-chasing accesses of graphs and sparse math, the address-translation tax on huge sprawling programs, and above all the sheer volume of data that modern AI must haul — colossal piles of weights and ever-growing running notes that no cache can hold and that dominate both time and energy. For these, the machine still spends most of its life waiting, and no clean general answer exists.\n\nThe most promising and least settled frontier is moving the math to the data — computing inside or beside memory and storage. In narrow, hand-tuned demonstrations it delivers spectacular wins, sometimes several-fold speedups at a fraction of the energy, precisely because it stops the data from moving at all. But it remains far from a drop-in solution: memory chips have brutally tight room for logic, the hardware and software must be co-designed together rather than mixed and matched, the programming tools are immature, and thorny practical issues loom underneath — reliability problems where densely packed memory cells disturb their neighbors (a hazard these compute-in-memory schemes can worsen), and the awkwardness of chips forced to serve as both storage and calculator at once. The honest summary is that the community has a rich menu of partial answers, each excellent in its niche, and no consensus on which will generalize. The one thing everyone now agrees on is the diagnosis: the bottleneck is the movement of data, not the doing of arithmetic — and for the first time in the history of computing, the entire field is reorganizing itself around that fact."
}
