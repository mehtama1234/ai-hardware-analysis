# Enabling Unstructured Sparse Fine-Tuning and Inference for Foundation Models on Wafer-Scale Engine

**Venue:** SC · **Subtheme:** Sparsity-Preserving Fine-Tuning on Specialized Hardware

## What It Does

Phoenix merges sparse pre-trained weights with low-rank adapters during fine-tuning to preserve sparsity structure across downstream task adaptation. Foundation models are often pre-trained with sparsity (e.g., magnitude pruning removes 50-60% of weights to reduce inference compute). However, task-specific fine-tuning on new datasets traditionally uses dense full-parameter updates (via SGD or Adam on all parameters), which fills in the pruned weights and negates sparsity savings.

Phoenix instead applies LoRA-style low-rank adapters on top of the sparse pre-trained weights: the adapter weights are initialized as low-rank matrices and trained, while the sparse pre-trained weights remain frozen. During inference, the final weight matrix is the sum of sparse pre-trained weights and the low-rank adapter, maintaining the original 50-60% sparsity pattern from pre-training. This preserves the efficiency of sparse pre-training while adapting the model to downstream tasks.

The method is deployed on Cerebras CS-2 (wafer-scale engine), which has native support for unstructured sparsity via sparse weight and activation buffers that efficiently skip zero computations.

## The Key Result

On Cerebras CS-2 with 50-60% sparsity, Phoenix maintains model accuracy comparable to dense fine-tuning while achieving approximately 2x FLOP reduction. Inference throughput is 1.7x higher than NVIDIA A100 GPU baseline.

## Why This Approach

Sparse pre-trained models hold significant efficiency promise: 50-60% weight sparsity reduces computation and memory directly. However, downstream task adaptation (fine-tuning on domain-specific data) traditionally requires dense fine-tuning to achieve good accuracy, negating sparsity savings. Prior approaches either (1) fine-tune densely, losing sparsity, or (2) retain sparsity structure but sacrifice adaptation quality (accuracy drops).

Phoenix's insight is to use low-rank adapters, which are widely proven effective (LoRA, QLoRA) for efficiency. By training only the low-rank adapter (small model) while keeping sparse pre-trained weights frozen, the method combines two efficiency mechanisms: (1) sparse pre-training reduces computation in the large pre-trained component, and (2) LoRA reduces parameters in the training component. The result is that total compute during fine-tuning is already reduced (relative to dense fine-tuning), and inference maintains the original sparsity, maximizing end-to-end efficiency.

Cerebras CS-2 hardware is particularly suited for this because its unstructured sparsity support natively handles arbitrary sparsity patterns without requiring N:M structure, so the 50-60% sparsity from pre-training maps directly to hardware efficiency.

## What It Leaves Open

- Comparison limited to NVIDIA A100; Cerebras cost per compute unit, amortization over inference volume, and financial tradeoff versus dense GPU clusters not discussed.
- Scalability to larger models (100B+) unclear; fine-tuning experiments limited to smaller model scales in initial evaluation.
- Applicability to other sparse pre-training methods (pruning-at-init, lottery ticket hypothesis, dynamic sparsity) not evaluated; method may be specific to magnitude pruning.
- Low-rank adapter rank selection (how many dimensions in the adapter?) not optimized; whether adapters should be per-layer or global not explored.
- Accuracy recovery analysis missing: does the adapter fully recover downstream task performance, or is there a permanent accuracy gap versus dense fine-tuning?
