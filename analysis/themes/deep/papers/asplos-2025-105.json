{
  "id": "asplos-2025-105",
  "confidence": "high",
  "what_it_does": "Mixture-of-Experts (MoE) models are a style of large AI model where each input token is routed to just a handful of specialized sub-networks called experts, rather than running through every part of the model. Training these models across many GPUs is slow because tokens must be physically shipped between machines (a collective messaging operation called AlltoAll), and this communication time is not well-hidden behind computation. FSMoE is a training system that finds and exploits every possible overlap between different kinds of communication and computation to minimize wasted time, and it does so in a way that works for many different MoE routing strategies, not just one.",
  "method_in_detail": "The idea. When training MoE models on a cluster, four types of operations compete for time: (1) AlltoAll Dispatch/Combine — sending tokens between machines so each expert receives the right inputs; (2) AllGather/ReduceScatter — within-node messages that reassemble or split expert outputs across GPUs inside a single server; (3) expert computation; and (4) Gradient-AllReduce — synchronizing weight updates across data-parallel replicas. Existing systems overlap only some pairs; FSMoE tries to overlap all of them simultaneously.\n\nHow it works. First, FSMoE breaks every MoE layer into six plug-in sub-modules (Gate, Order, I-Order, Dispatch, Combine, Expert) with hook points for custom logic. This modular design lets researchers swap in any routing algorithm without changing the scheduler. Second, a profiler fits simple linear performance models to each sub-module (startup cost + bytes-per-second), then classifies the workload into one of four cases depending on which operation dominates. For each case, a math optimization (called SLSQP, a standard constrained solver) finds the optimal number of chunks r to split the input into, so that communication and computation tiles interleave without gaps. Crucially, the forward pass and backward pass each get their own optimal chunk count, because the backward pass involves extra gradient work. Third, the gradient synchronization step is handled separately: a two-step algorithm greedily assigns gradient chunks to the idle gaps already computed by step two, then uses a global evolutionary optimizer to fine-tune the assignment across all layers.\n\nThe key design choice. The system operates on three parallel GPU streams simultaneously — one for intra-node messages, one for inter-node messages, and one for computation — and deliberately stages the chunked input so all three streams stay busy.\n\nThe result. FSMoE is 1.18-1.22x faster than Tutel/PipeMoE across 1,458 tested configurations and 1.19-3.01x faster on real GPT-2 and Mixtral MoE models on clusters of 32-48 GPUs."
}
