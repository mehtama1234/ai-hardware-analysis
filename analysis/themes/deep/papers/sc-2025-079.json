{
  "id": "sc-2025-079",
  "confidence": "low",
  "what_it_does": "FT-Transformer adds fault tolerance to transformer inference running on large-scale HPC systems to handle soft errors (bit flips and transient faults unavoidable at exascale). The system protects the attention module as a single unit with end-to-end error detection and correction. For non-attention linear layers, it uses algorithm-based fault tolerance via stride patterns to avoid expensive inter-thread synchronization.",
  "method_in_detail": ""
}
