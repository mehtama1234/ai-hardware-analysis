{
  "id": "asplos-2025-117",
  "confidence": "high",
  "what_it_does": "When you rent a virtual machine on a public cloud service like AWS, other customers' workloads are running on the same physical hardware at the same time, creating unpredictable interference — some runs of your application take 300 seconds, the next run 500 seconds, through no change of your own. Performance tuning tools (which try out many combinations of configuration settings to find the fastest one) assume a quiet, consistent environment; in a noisy cloud, they end up recommending settings that happened to look fast on a lucky low-interference run rather than settings that are genuinely fast. DarwinGame solves this by running multiple competing configurations simultaneously on the same machine, so they all experience the same interference level, and judging them by their relative performance rather than their absolute time.",
  "method_in_detail": "The idea. You can't control the noise in a cloud environment, but you can neutralize it by exposing every competing configuration to identical noise at the same moment. If configuration A and configuration B run concurrently on the same VM, whatever interference the cloud inflicts hits both equally — so whichever finishes more work in that window is genuinely faster. DarwinGame calls each such concurrent run a 'game' and organizes many games into a structured tournament.\n\nHow it works. The search space of configuration knobs (millions of combinations for Redis, GROMACS, or FFmpeg) is first divided into 10,000 regions. Within each region, a Swiss-style tournament phase runs: in each round, the top-scoring configurations from earlier rounds compete against new untested ones. 'Score' is a work-done fraction — how much of the application's total work (requests processed, frames encoded, simulation steps completed) did this configuration finish relative to the fastest configuration in the same game window? Games in early rounds can terminate early if one configuration pulls ahead by more than 10%, saving time. Regional winners advance to a global double-elimination phase, where losers get a second-chance bracket because a good configuration might have been unlucky with noisy co-location partners. In this phase, configurations are scored on both execution speed and consistency — the average of their reciprocal rank across all games they've played — so that settings which are fast but erratic are filtered out. Top survivors from the global phase play a barrage-style playoff (progressive head-to-head knockouts), and the final two configurations play a deciding game.\n\nThe key design choice. Running configurations side-by-side on the same VM is the core mechanism. Prior tuners tested configurations sequentially, so configuration A might see heavy interference at minute 1 and configuration B might see light interference at minute 2 — the comparison is unfair. Co-location makes the comparison fair by construction.\n\nThe result. Tested on AWS with Redis, GROMACS, FFmpeg, and LAMMPS, DarwinGame's chosen configuration runs within 4.2% of the optimal (found by exhaustive testing in a quiet, dedicated environment), while the next-best tuner (BLISS, a Bayesian optimization tool) produces configurations that run more than 40% slower than optimal. DarwinGame's winning configuration also shows a coefficient of variation (run-to-run variation) of only 0.46%, versus more than 6% for all competing solutions."
}
