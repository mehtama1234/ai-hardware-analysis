{
  "id": "isca-2025-116",
  "confidence": "low",
  "what_it_does": "SpecEE accelerates large language model inference by using a small draft model to speed up the early-exit predictor that decides when to stop computing. The early-exit predictor normally searches through 30,000 possible next tokens per layer, adding 20% latency overhead. The system combines this speedup with speculative decoding (a technique that generates multiple candidate next tokens in parallel) without exploding in complexity.",
  "method_in_detail": ""
}
