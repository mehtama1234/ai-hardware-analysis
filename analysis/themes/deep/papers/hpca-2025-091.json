{
  "id": "hpca-2025-091",
  "confidence": "low",
  "what_it_does": "CCZNS enables data compression inside solid-state drives (SSDs) without losing the performance benefits of ZNS (Zoned Namespace) interface used in databases. Traditional compression hides details from the host, but ZNS requires the host to know where data is stored. This solution has the SSD compress data while exposing the compression boundaries to the host, allowing both hardware compression and proper storage management.",
  "method_in_detail": ""
}
