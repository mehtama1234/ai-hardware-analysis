{
  "id": "sc-2025-379",
  "confidence": "low",
  "what_it_does": "KAMI is a set of communication-avoiding algorithms for general matrix multiplication operations on a single GPU. The algorithms work at the thread block level and utilize tensor cores (specialized hardware for matrix operations), thread registers (fast per-thread memory), and shared memory (fast shared across threads). The approach includes variants for sparse matrix operations, enabling better performance on both dense and sparse matrix multiplication workloads.",
  "method_in_detail": ""
}
