{
  "id": "sc-2025-329",
  "confidence": "low",
  "what_it_does": "Reduces cost and latency for serving large language models by dynamically moving model weights between a fast local GPU device and slower but larger host memory. When the GPU is busy with other requests, the system checkpoints the model, retrieves only the parts it needs from host memory on-demand, and restores them to GPU memory when each new request arrives.",
  "method_in_detail": ""
}
