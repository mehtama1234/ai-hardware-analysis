{
  "id": "sc-2025-386",
  "confidence": "low",
  "what_it_does": "This paper addresses the slowness of large language model (LLM) inference, which currently generates one token at a time in sequence. It proposes Smurfs, a method using multiple smaller models (State Space Models) to predict multiple future tokens in parallel while the main LLM verifies them. The system uses a voting mechanism across the smaller models to improve prediction accuracy and decouples the prediction and verification steps for better efficiency.",
  "method_in_detail": ""
}
