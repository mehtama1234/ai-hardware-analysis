{
  "id": "sc-2025-018",
  "confidence": "low",
  "what_it_does": "gLLM improves how large language models are served across multiple processors (pipeline parallelism) by fixing an imbalance problem: different stages in the pipeline finish at different times, leaving some processors idle (pipeline bubbles). The system uses Token Throttling, a scheduling technique that independently controls how many prefill tokens (initial processing) and decode tokens (response generation) flow through each stage, keeping computation balanced and throughput high.",
  "method_in_detail": ""
}
