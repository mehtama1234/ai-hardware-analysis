{
  "id": "sc-2025-066",
  "confidence": "low",
  "what_it_does": "UpANNS addresses the bottleneck of searching through billions of similar vectors (used for semantic search in AI systems). Existing approaches struggle: CPUs run out of bandwidth to access memory fast enough, and GPUs have idle compute periods because they finish work before memory data arrives. This work uses a special hardware architecture called Processing-In-Memory where compute happens close to where data lives, dramatically reducing energy waste and increasing throughput.",
  "method_in_detail": ""
}
