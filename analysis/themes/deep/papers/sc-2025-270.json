{
  "id": "sc-2025-270",
  "confidence": "low",
  "what_it_does": "This work extends network source routing with awareness of different application traffic classes. Instead of treating all network flows equally, the system classifies traffic at the network edge and makes routing decisions based on each application's characteristics. It uses in-band network telemetry (collecting performance metrics from within the network itself) and programmable switches to classify and route traffic optimally, balancing network load across multiple paths while respecting each traffic class's needs.",
  "method_in_detail": ""
}