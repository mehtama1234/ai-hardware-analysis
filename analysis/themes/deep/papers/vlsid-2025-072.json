{
  "id": "vlsid-2025-072",
  "confidence": "low",
  "what_it_does": "This paper applies neural networks to optimize system-on-chip (SoC) designs. A three-stage framework trains neural models to predict performance from design parameters (like frequency and voltage), then uses those predictions to guide a search algorithm toward optimal configurations. The approach uses compositional neural networks that combine predictions from individual component-level models into a full-chip prediction.",
  "method_in_detail": ""
}
