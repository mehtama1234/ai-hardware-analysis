{
  "id": "dac-2025-048",
  "confidence": "low",
  "what_it_does": "Addresses GPU-accelerated routing for large VLSI circuit designs by using a sweep-sharing technique that routes multiple nets (interconnection paths) in a single pass. Instead of launching many separate GPU operations, it reduces memory pressure and overhead. The approach also selectively reroutes only the problematic segments of failing routes while keeping successful parts intact, speeding up overall computation.",
  "method_in_detail": ""
}
