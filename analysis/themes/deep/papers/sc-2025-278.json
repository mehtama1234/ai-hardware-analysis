{
  "id": "sc-2025-278",
  "confidence": "low",
  "what_it_does": "Plexus trains Graph Neural Networks (GNNs) on massive graphs with billions of edges by distributing computation across thousands of GPUs while avoiding memory overflow. The key innovation is a 3D parallel strategy that combines spatial parallelism (splitting the graph across GPUs) and temporal parallelism (splitting the computation across time steps). A performance model automatically selects the best configuration for any given graph and hardware setup.",
  "method_in_detail": ""
}
