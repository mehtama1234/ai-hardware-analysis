{
  "id": "isca-2025-042",
  "confidence": "high",
  "what_it_does": "Protein structure prediction models like AlphaFold2 — the AI systems that won the 2024 Nobel Prize in Chemistry for figuring out the 3D shape of proteins from their amino acid sequences — run out of GPU memory on proteins longer than about 2000 amino acids because their internal data structures grow quadratically and then cubically with sequence length. LightNobel is a custom chip design that compresses those internal numbers using adaptive precision — different parts of the data get 4-bit, 8-bit, or 16-bit representations depending on their statistical behavior — reducing peak memory by up to 120x and speeding up execution up to 8.4x versus the best current NVIDIA GPUs while keeping prediction accuracy virtually unchanged.",
  "method_in_detail": "The idea. Protein folding models maintain a special data structure called the Pair Representation — a table with one entry for every pair of amino acids in the protein, encoding how those two positions relate spatially. If a protein has N amino acids, this table has N×N entries, each of which is a vector of 128 numbers. The attention mechanism (which lets every pair look at every other pair) then operates over N×N×N triples. At N=2000 this already exceeds GPU memory; at N=45000 (a real protein called titin) it would require terabytes. LightNobel addresses this by representing numbers at lower precision to shrink everything.\n\nThe key insight about data structure. In most AI models, different channels (the 128 positions in each vector) vary wildly — one might have values around 100, another around 0.01 — so quantization must be done channel by channel. In protein models, the opposite is true: all 128 channels in a given token (a specific amino acid pair) have similar magnitudes, but different tokens vary enormously. This 'distogram pattern' — the protein's pairwise distance map is imprinted on the data — means grouping by token is the right granularity.\n\nThree activation groups. The paper analyzes every tensor in the computation and sorts them into three buckets. Group A (before layer normalization, connected to residual pathways): values are large (average magnitude 82) and have outliers, so they get 8-bit precision for inliers plus 16-bit storage for the few extreme outliers. Group B (after normalization): values are small (magnitude ~4) but outliers persist, so 4-bit for inliers, 16-bit for outliers. Group C (other activations): small values, almost no outliers, so plain 4-bit with no special outlier handling. Outliers are identified at runtime using a parallel sorting algorithm (bitonic sort) that runs directly on the chip's vector units.\n\nThe hardware. A 'Reconfigurable Matrix Processing Unit' chops every token's values into 4-bit slices and routes them through a dynamically reconfigurable tree of adders — effectively doing multiplication by shift-and-add rather than with fixed-width multipliers. It can simultaneously handle groups of 4 PE lanes (for 4-bit inliers) and 5 PE lanes (for 16-bit outliers mixed in) without reconfiguration stalls. A 'Versatile Vector Processing Unit' with 128 parallel lanes handles the sorting, scaling, and normalization operations that surround the matrix math. Attention scores are computed using a streaming approach similar to FlashAttention — never writing the full N×N×N score cube to memory — which alone provides large memory savings.\n\nThe result. Versus an NVIDIA A100 GPU: up to 8.44x speedup and 37x better power efficiency; versus H100: 8.41x and 43x. Peak memory reduced by up to 120x, enabling proteins that previously could not fit on any GPU to be processed."
}
