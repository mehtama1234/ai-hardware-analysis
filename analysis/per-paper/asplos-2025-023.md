# SmoothE: Differentiable E-Graph Extraction

**Venue:** ASPLOS  
**Confidence:** low (abstract-only)  
**Primary Theme:** differentiable e-graph extraction for scalable compiler optimization

## Problem
E-graph extraction suffers from an unfavorable scalability-quality trade-off: exact methods are slow, fast heuristics are suboptimal, and current methods only support simple linear cost functions—limiting use for realistic optimization objectives.

## Motivation
E-graphs are increasingly used in compilers and program synthesis; extraction quality directly determines the quality of the optimized output, and existing extractors bottleneck the usefulness of equality saturation for complex real-world cost models.

## Method
SmoothE reformulates e-graph extraction from discrete yes/no choices to a continuous problem with smooth weights, then applies gradient descent to find good answers faster than exact methods. This approach works with any cost function that has gradients—including learned neural models—whereas existing methods only support linear costs. The method ensures the smooth solution maps to a valid extraction from the original e-graph.

## Key Novelty
Reformulating e-graph extraction as a smooth, continuous optimization that gradient descent can solve with arbitrary learned cost functions instead of just linear ones.

## Contributions
- SmoothE: a differentiable relaxation of the e-graph extraction problem
- Support for non-linear and learned (neural) cost models in e-graph extraction
- Improved scalability-quality trade-off vs. existing exact and heuristic extractors
- Applicability to compiler optimization, program synthesis, and theorem proving

## Hardware Target
CPU, GPU

## Technique Category
compiler, approximation

## Workloads
HPC

## Metrics
- other: improved scalability-quality trade-off over existing extractors; specific numbers not in abstract

## Baselines
existing e-graph extraction heuristics and exact solvers

## Limitations
The quality of the continuous relaxation's correspondence to valid discrete extractions under all cost models is not fully characterized in the abstract.

## Tags
e-graphs, equality-saturation, compiler-optimization, differentiable, program-synthesis, extraction
