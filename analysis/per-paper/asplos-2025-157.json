{
  "id": "asplos-2025-157",
  "title": "Mosaic: Exploiting Instruction-Level Parallelism on Deep Learning Accelerators with iTex Tessellation",
  "venue": "ASPLOS",
  "problem": "Deep learning compilers use top-down tiling to map tensor computations homogeneously to a single type of hardware arithmetic unit (scalar, vector, or tensor), failing to exploit simultaneous use of all available compute units (scalar, vector, and tensor) in parallel. This leaves substantial instruction-level parallelism (ILP) on CPUs and GPUs unexploited, especially when computations can be decomposed into mixed instruction types.",
  "motivation": "Commodity hardware platforms (CPUs, GPUs) provide scalar, vector, and tensor arithmetic units that can execute concurrently, but the homogeneous tiling strategies of existing compilers only utilize one type at a time, wasting available ILP and peak FLOP throughput.",
  "method": "Mosaic introduces iTex (instruction-level texture) tessellation: a compiler technique that decomposes a tensor computation task into heterogeneous sub-tiles assigned to different hardware arithmetic units (scalar, vector, tensor) simultaneously. The tessellation partitions the iteration space of a tensor operation such that different sub-regions are mapped to the most appropriate unit type, and the schedule coordinates their concurrent execution. A compiler pass analyzes operation shapes and hardware unit capabilities to determine the optimal tessellation, then generates mixed-instruction kernel code that overlaps scalar, vector, and tensor unit execution within a single kernel.",
  "key_novelty": "iTex tessellation: a compiler pass that partitions tensor operation iteration spaces into heterogeneous sub-tiles mapped concurrently to scalar, vector, and tensor units, achieving intra-kernel ILP across all available arithmetic unit types.",
  "contributions": [
    "iTex tessellation compiler technique for heterogeneous sub-tile mapping to scalar/vector/tensor units simultaneously",
    "Mosaic compiler system implementing iTex for deep learning workloads on CPUs and GPUs",
    "Performance improvement over homogeneous tiling by exploiting intra-kernel ILP across all arithmetic unit types",
    "Analysis of tessellation strategies for different tensor operation shapes and hardware platforms"
  ],
  "hardware_target": ["GPU", "CPU"],
  "technique_category": ["compiler", "parallelism", "kernel-fusion", "dataflow"],
  "workloads": ["CNN", "transformer", "LLM-inference", "attention"],
  "metrics": {
    "speedup": "performance improvement over homogeneous tiling (specific numbers not in abstract)",
    "energy_or_tops_w": null,
    "area": null,
    "ppa": null,
    "accuracy": null,
    "other": "utilization of scalar, vector, and tensor units simultaneously vs. homogeneous mapping"
  },
  "baselines": ["homogeneous tiling-based DL compilers (TVM, Triton, etc.)"],
  "limitations": "Specific speedup numbers and the range of hardware platforms evaluated are not stated in the abstract.",
  "tags": ["dl-compiler", "instruction-level-parallelism", "tensor-unit", "heterogeneous-mapping", "tiling"],
  "primary_theme": "heterogeneous arithmetic-unit instruction-level parallelism via compiler tessellation",
  "confidence": "low"
}
