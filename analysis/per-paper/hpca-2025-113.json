{
  "id": "hpca-2025-113",
  "title": "AccelES: Accelerating Top-K SpMV for Embedding Similarity via Low-bit Pruning",
  "venue": "HPCA",
  "problem": "Top-K sparse matrix-vector multiplication (SpMV) for recommendation system embedding similarity suffers from high latency due to inefficient memory access patterns when scanning full-precision sparse matrices to identify the most relevant embeddings.",
  "motivation": "Real-time recommendation systems require sub-millisecond embedding similarity queries at scale, but existing CPU/GPU/FPGA solutions are bottlenecked by memory bandwidth when performing SpMV over large sparse feature matrices.",
  "method": "AccelES splits computation into two stages to avoid expensive full-precision math on the entire sparse matrix. Stage 1 uses a low-bit representation (an approximation using fewer bits per number, making it faster to scan) to rapidly identify the top-K most-relevant entries and filter out the rest with minimal data movement. Stage 2 performs exact full-precision calculation only on those selected top-K entries. To handle the irregular memory access patterns of sparse matrix operations, the system uses two new storage formats called Ultra-CSR and Random-CSR, which optimize data flow through memory.",
  "key_novelty": "The innovation is a two-stage approach: a fast low-bit pass identifies top-K candidates, then full-precision math is applied only to those candidates, so expensive computation is used only on the ones that actually matter.",
  "contributions": [
    "Two-stage Top-K SpMV algorithm combining low-bit candidate selection with full-precision verification",
    "Ultra-CSR and Random-CSR matrix formats optimized for memory bandwidth in sparse embedding lookup",
    "Accelerator achieving 3.4x over FPGA, 2.5x over GPU, and 153.3x over CPU for Top-K SpMV",
    "Demonstrated applicability to real-time recommendation system workloads"
  ],
  "hardware_target": [
    "FPGA",
    "ASIC"
  ],
  "technique_category": [
    "sparsity",
    "quantization",
    "memory-system",
    "approximation"
  ],
  "workloads": [
    "recommendation",
    "DLRM"
  ],
  "metrics": {
    "speedup": "3.4x over FPGA, 2.5x over GPU, 153.3x over CPU"
  },
  "baselines": [
    "FPGA baseline (unspecified)",
    "GPU baseline (unspecified)",
    "CPU baseline (unspecified)"
  ],
  "limitations": "The accuracy impact of the low-bit pruning approximation on final recommendation quality (beyond top-K overlap) is not discussed in the abstract.",
  "tags": [
    "spmv",
    "embedding-similarity",
    "recommendation-systems",
    "low-bit",
    "sparsity",
    "fpga"
  ],
  "primary_theme": "Low-bit two-stage pruning for accelerated Top-K sparse embedding similarity",
  "confidence": "low"
}