# asplos-2025-065 — Accelerating Number Theoretic Transform with Multi-GPU Systems for Efficient Zero Knowledge Proof

**Venue:** ASPLOS  
**Confidence:** low (abstract-only)

## Problem
Number Theoretic Transform (NTT), a key bottleneck in zero-knowledge proof generation, has not been efficiently parallelized across multiple GPUs because its butterfly permutation access pattern generates high inter-GPU communication overhead that offsets the compute gains from additional GPUs.

## Motivation
ZK proof generation is becoming critical for verifiable outsourcing and blockchain applications, and NTT—unlike Multi-Scalar Multiplication (MSM) which has been multi-GPU accelerated—remains the unaddressed bottleneck preventing proof generation from scaling to practical speeds.

## Method
The paper restructures the NTT algorithm so data stays within each GPU's memory as long as possible, delaying transfers between GPUs until they're necessary. The butterfly computation stages (the ordered steps that transform numbers) are planned based on which GPUs are physically connected, with expensive data-reorganization steps grouped together and run simultaneously with computation to reduce their cost. The approach also selects the most efficient physical connection path between GPUs—either NVLink for direct high-speed connections or PCIe—to maximize the rate at which data moves.

## Key Novelty
Map where NTT computation stages execute based on GPU physical connections, so butterfly permutations require less data transfer between GPUs.

## Contributions
- Analysis of inter-GPU communication as the key barrier to multi-GPU NTT scalability.
- Topology-aware NTT algorithm that localizes data access per GPU and batches cross-GPU permutations.
- Multi-GPU NTT system implementation enabling scalable ZK proof acceleration.
- Demonstrated speedup on ZK proof generation benchmarks exceeding single-GPU NTT performance.

## Hardware Target
GPU

## Technique Categories
parallelism, interconnect, scheduling, cryptography

## Workloads
cryptography

## Metrics
- Multi-GPU NTT speedup over single-GPU baseline (details in full paper).

## Baselines
- Single-GPU NTT implementations
- Existing multi-GPU ZK proof frameworks

## Limitations
Scalability beyond a small number of GPUs and dependency on NVLink bandwidth are not quantified in the abstract.

## Tags
zero-knowledge-proof, ntt, multi-gpu, cryptography, zkp

## Primary Theme
multi-GPU NTT acceleration for zero-knowledge proofs
