# KDRSolvers: Scalable, Flexible, Task-Oriented Krylov Solvers

**Paper ID:** sc-2025-425
**Venue:** SC
**Authors:** David Kai Zhang, Rohan Yadav, Alex Aiken, Fredrik Kjolstad, Sean Treichler

## Problem

Sparse linear solver frameworks (PETSc, Trilinos) are inflexible in data partitioning; changes to partitioning strategies require extensive code refactoring and don't automatically propagate.

## Motivation

Krylov methods require flexible partitioning for optimal load balancing on heterogeneous systems; manual tuning for each new partition strategy wastes developer time and introduces bugs.

## Method

The key insight is decoupling how sparse matrices are stored from how algorithms use them. A new abstract layer automatically handles data partitioning across multiple computers by tracking three things in parallel: the problem domain, the output space, and the locations of non-zero values. Universal operators co-partition matrices and vectors together, and a multi-operator approach lets algorithms work with data scattered non-contiguously across memory. The implementation targets Legion, a task-based runtime that breaks work into independent chunks.

## Key Novelty

The first abstraction decoupling sparse matrix storage format from Krylov algorithm implementation, so partitioning changes automatically propagate without code rewrites.

## Contributions

- Dependent partitioning abstraction for sparse matrix representation
- Universal co-partitioning operators independent of storage format
- Multi-operator systems for non-contiguous data processing
- Competitive performance vs PETSc/Trilinos (9.6% time reduction on 1,024 GPUs)

## Hardware Targets

- heterogeneous supercomputers
- NVIDIA GPUs

## Technique Categories

- sparse solvers
- Krylov methods
- task-based runtime
- data partitioning

## Workloads

- sparse linear systems
- scientific computing

## Metrics

- **gpu_scale:** up to 1,024 GPUs
- **speedup:** 9.6% reduction in iteration time
- **flexibility:** automatic partitioning propagation

## Baselines

- PETSc
- Trilinos

## Limitations

Speedup modest (9.6%); Legion runtime overhead not isolated; scalability beyond 1,024 GPUs not tested; limited evaluation on diverse problem types.

## Tags

sparse-solvers, Krylov-methods, data-partitioning, Legion, heterogeneous-computing

## Primary Theme

Flexible sparse linear solvers via abstraction-based partitioning

## Confidence

high
