{
  "id": "dac-2025-403",
  "title": "SSFT: Algorithm and Hardware Co-design for Structured Sparse Fine-Tuning of Large Language Models",
  "venue": "DAC",
  "problem": "Sparse fine-tuning (SFT) reduces time and memory requirements compared to dense fine-tuning, but GPU implementation underperforms due to irregular sparse data access patterns that modern GPUs cannot efficiently handle, negating computational benefits.",
  "motivation": "LLM fine-tuning is expensive; sparse fine-tuning promises efficiency but GPU execution fails to exploit sparsity benefits.",
  "method": "Sparse fine-tuning skips some calculations to reduce time and memory, but on regular GPUs the random pattern of which calculations to skip causes inefficient, scattered memory access. SSFT solves this by using regular, predictable patterns for which calculations to skip (structured sparsity), making memory access organized and efficient. A custom hardware accelerator (SSFT-Hw) is co-designed with the algorithm (SSFT-Alg) to efficiently handle these patterns without the memory overhead that generic sparse operations cause on standard GPUs.",
  "key_novelty": "The core idea is designing sparsity patterns to be regular and predictable specifically for hardware accelerators, rather than using generic sparse operations that scatter memory access.",
  "contributions": [
    "SSFT-Alg with predictable sparsity patterns for regularity",
    "SSFT-Hw accelerator with sparsity-aware design",
    "Comparable accuracy to dense fine-tuning on BERT, LLaMA 2 7B, and 13B",
    "51.0x throughput vs GPU, 1.32x vs SOTA sparsity-aware accelerators; 19.0x and 1.48x energy improvements"
  ],
  "hardware_target": [
    "ASIC"
  ],
  "technique_category": [
    "sparsity",
    "quantization"
  ],
  "workloads": [
    "LLM-training"
  ],
  "metrics": {
    "speedup": "51.0x vs GPU, 1.32x vs SOTA sparsity accelerators",
    "energy_or_tops_w": "19.0x vs GPU, 1.48x vs SOTA"
  },
  "baselines": [
    "GPU fine-tuning",
    "SOTA sparsity-aware transformer accelerators"
  ],
  "limitations": "Not discussed.",
  "tags": [
    "sparse-finetuning",
    "llm-training",
    "sparsity",
    "algorithm-hardware-codesign"
  ],
  "primary_theme": "Structured sparse fine-tuning with hardware-algorithm co-design for LLMs",
  "confidence": "low"
}