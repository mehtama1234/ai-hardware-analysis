# JSA-CIM: A Joint-Sparse AdderNet Compute-In-Memory Accelerator for Energy-Efficient Edge AI Applications

**Venue:** ICCAD  
**ID:** iccad-2025-085

## Problem
Existing CIM designs for AdderNet fail to exploit inherent model sparsity in both activations and weights, missing substantial opportunities for energy efficiency improvement in edge AI deployment.

## Motivation
AdderNet offers significant computational advantages over CNNs through efficient Sum-Of-Absolute-Differences operations, but realizing energy-efficient CIM deployment requires exploiting joint sparsity patterns in both activation and weight dimensions.

## Method
JSA-CIM is a hardware accelerator built with 8-transistor SRAM memory cells that compute neural networks using addition instead of multiplication (AdderNet). It saves energy by exploiting zeros in both activation values and weights—processing them one bit at a time, skipping entire activation values when they're zero at the system level, and skipping individual weight bits that are zero at the circuit level. A robust method reliably selects minimum values during this bit-by-bit processing, which is critical for AdderNet computation.

## Key Novelty
It exploits zeros in both weight parameters and intermediate values (a property called joint-sparsity) using a specialized 8-transistor memory design, delivering energy-efficient computation for addition-based neural networks (AdderNet).

## Contributions
- Sparsity and kernel-size-aware CIM macro for flexible architecture configuration and throughput optimization
- Novel 8T-SRAM bitline architecture exploiting word-level activation and bit-level weight sparsity
- Robust sparsity-gated minimum selection circuit for reliable bit-serial minimum operations
- 65nm implementation achieving 170.7 GOPS throughput, 1.8 TOPS/mm² area efficiency, 75.98 TOPS/W energy efficiency

## Hardware Targets
- ASIC
- PIM

## Technique Categories
- near-data-processing
- sparsity
- circuit-design

## Workloads
- CNN

## Metrics
| Metric | Value |
|--------|-------|
| Speedup | — |
| Energy/TOPS/W | 75.98 TOPS/W |
| Area | 1.8 TOPS/mm² area efficiency |
| PPA | — |
| Accuracy | preserved through sparsity exploitation |
| Other | 170.7 GOPS peak throughput |

## Baselines
- conventional CIM designs
- non-sparsity-aware accelerators

## Limitations
Evaluation limited to AdderNet architecture; applicability to other sparse neural network models and scaling to larger arrays is not discussed.

## Tags
`cim` `addernet` `sparsity` `8t-sram` `edge-ai` `energy-efficiency`

## Primary Theme
Joint-sparsity AdderNet acceleration via specialized CIM

## Confidence
Low
