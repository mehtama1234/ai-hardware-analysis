# Uni-Render: A Unified Accelerator for Real-Time Rendering Across Diverse Neural Renderers

**Venue:** HPCA  
**Confidence:** low (abstract-only)

## Problem
Existing neural rendering accelerators are designed for a single specific rendering pipeline, making them unable to efficiently support the diverse and evolving landscape of neural rendering algorithms (NeRF, 3DGS, hybrid methods) required for different application scenarios.

## Motivation
Immersive 3D applications demand real-time rendering (>30 FPS) across multiple distinct neural rendering pipelines, but dedicated pipeline-specific accelerators are economically and practically infeasible for edge deployment.

## Method
Uni-Render is a specialized computer chip (ASIC) that can reconfigure itself to accelerate different types of neural rendering. It identifies computational operations shared across rendering methods—ray marching (stepping rays through 3D space to find surfaces), Gaussian rasterization (projecting 3D points as pixels), MLP inference (evaluating neural networks), and volume compositing (blending samples into a final image)—and implements a flexible processing engine that dynamically adjusts the dataflow and operations based on the rendering algorithm being used. The chip's processing units and memory layout can be reorganized at runtime to support both typical and hybrid rendering pipelines.

## Key Novelty
A single reconfigurable hardware accelerator that covers all representative neural rendering pipelines by exploiting their shared computational operations and dynamically adapting data flow for each algorithm.

## Contributions
1. Systematic identification of common computational operators across diverse neural rendering pipelines
2. Reconfigurable hardware architecture with dynamically adjustable dataflow supporting multiple neural rendering algorithms
3. First unified accelerator achieving real-time neural rendering (>30 FPS) across varied pipelines at ~5W edge power
4. Up to 119x speedup over state-of-the-art neural rendering hardware

## Hardware Target
ASIC

## Technique Categories
dataflow, scheduling, kernel-fusion

## Workloads
vision, diffusion

## Metrics
- Speedup: up to 119x over state-of-the-art neural rendering hardware
- Power: ~5W (edge device constraint)
- Frame rate: >30 FPS real-time rendering achieved

## Baselines
- State-of-the-art neural rendering hardware accelerators

## Limitations
As neural rendering algorithms continue to evolve rapidly, the reconfigurability may require hardware updates to support future methods not anticipated at design time.

## Tags
`neural-rendering` `unified-accelerator` `reconfigurable-hardware` `edge-inference` `3d-gaussian-splatting` `nerf`
