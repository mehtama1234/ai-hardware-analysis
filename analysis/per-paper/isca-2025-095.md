# Hermes: Algorithm-System Co-design for Efficient Retrieval-Augmented Generation At-Scale

**Venue:** ISCA 2025
**Confidence:** low (abstract-only)

## Problem
As RAG datastores expand into the trillions of tokens, retrieval phase dominates end-to-end latency and energy. Existing RAG systems have no mechanism for efficiently routing queries to relevant subsets of a massive distributed datastore.

## Motivation
RAG avoids expensive LLM retraining by anchoring generation to external knowledge, but trillion-token datastores make retrieval the dominant bottleneck at datacenter scale.

## Method
Hermes distributes a large RAG database across multiple computers. It uses a two-tier system: a fast preliminary index rapidly identifies which data partitions might be relevant to a query, then only those partitions go through full dense retrieval—either exact k-nearest-neighbor search (finding the most similar documents) or approximate k-NN (finding good-enough matches faster). By skipping full retrieval on irrelevant partitions, this approach cuts computation time while preserving retrieval quality.

## Key Novelty
Hermes combines datastore partitioning with intelligent hierarchical routing that directs queries to relevant data partitions (skipping irrelevant ones), cutting computation while preserving recall—the fraction of relevant documents that get found.

## Contributions
1. Algorithm-systems co-design framework characterizing large-scale RAG retrieval bottlenecks
2. Hierarchical search strategy with dynamic query routing to relevant datastore subsets
3. Distributed datastore partitioning across nodes for parallel retrieval
4. 9.33x latency and 2.10x energy improvement on open-source RAG benchmarks without quality loss

## Hardware Target
GPU

## Technique Category
memory-system, parallelism, near-data-processing, scheduling

## Workloads
LLM-inference, database

## Key Metrics
- Speedup: 9.33x end-to-end latency vs. SOTA RAG
- Energy: 2.10x improvement
- No retrieval quality degradation

## Baselines
State-of-the-art RAG retrieval mechanism

## Limitations
Evaluation focused on open-source datastores; scalability to heterogeneous hardware not discussed.

## Tags
rag, retrieval, llm-inference, distributed-systems, hierarchical-search, datastore
