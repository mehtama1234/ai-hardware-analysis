# isca-2025-068 — H2-LLM: Hardware-Dataflow Co-Exploration for Heterogeneous Hybrid-Bonding-based Low-Batch LLM Inference

**Venue:** ISCA  
**Confidence:** low (abstract only)

## Problem
Edge-side low-batch LLM inference is bottlenecked by DRAM bandwidth; existing in-die NMP designs have limited compute capacity, creating an imbalance for both compute-bound prefill and bandwidth-bound decoding stages. Hybrid-bonding technology enables higher compute density near memory but requires simultaneous architecture and dataflow co-optimization.

## Motivation
Low-batch LLM inference at the edge is memory-bandwidth-bound during decoding; hybrid bonding (face-to-face die stacking) offers far more compute logic near memory than in-die NMP, but maximizing efficiency requires joint hardware architecture and dataflow exploration.

## Method
H2-LLM places high-capacity compute dies in face-to-face contact with DRAM dies via hybrid bonding, exploiting high through-silicon-via bandwidth. A data-centric dataflow abstraction maps LLM attention, feed-forward, and KV-cache operations onto heterogeneous on-die and near-die compute resources across prefill and decoding. An automated DSE framework jointly searches the architecture and dataflow design space for Pareto-optimal configurations.

## Key Novelty
H2-LLM is the first system to jointly explore hardware architecture and dataflow design space for hybrid-bonding-based heterogeneous LLM accelerators using a data-centric dataflow abstraction.

## Contributions
1. A hybrid-bonding-based heterogeneous accelerator architecture with explicit design space characterization.
2. A data-centric dataflow abstraction mapping prefill and decoding LLM operations onto heterogeneous resources.
3. An automated DSE framework for joint architecture-dataflow optimization.
4. 2.72x geomean speedup and 1.48x geomean energy efficiency improvement over in-die NMP accelerators.

## Hardware Target
chiplet, ASIC, PIM (processing-in-memory)

## Technique Category
near-data-processing, dataflow, packaging, memory-system

## Workloads
LLM-inference

## Metrics
- **Speedup:** 2.72x geomean over in-die NMP-based heterogeneous accelerators
- **Energy:** 1.48x geomean better energy efficiency

## Baselines
in-die NMP-based heterogeneous accelerators

## Limitations
Not discussed; thermal and manufacturing yield implications of hybrid bonding at scale not evaluated.

## Tags
hybrid-bonding, llm-inference, edge-ai, near-memory, heterogeneous-architecture, design-space-exploration
