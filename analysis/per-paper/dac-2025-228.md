# Ensembler: Protect Collaborative Inference Privacy from Model Inversion Attack via Selective Ensemble

**Venue:** DAC
**ID:** dac-2025-228

## Problem
Collaborative inference allows cloud providers to reconstruct sensitive client inputs via model inversion attacks; existing defenses either sacrifice privacy or add prohibitive overhead.

## Motivation
Privacy in collaborative DNN inference is essential for healthcare, finance; current split-learning exposes gradient information enabling inversion.

## Method
The server uses multiple different model branches (variations of the neural network) and combines their predictions together. When an attacker tries to reverse-engineer the original input from the mathematical calculation signals (called gradients), they see a scrambled mix from all the branches instead of a clear pattern. This obfuscation makes the attack much harder while keeping accuracy nearly the same, with under 5% overhead.

## Key Novelty
Multiple model branches working together hide the input's fingerprint in the gradients that attackers need to reconstruct it.

## Contributions

- Ensembler framework for privacy-preserving collaborative inference
- Selective model ensemble to obfuscate input reconstruction
- 43.5% structural similarity improvement over baselines
- 4.8% inference time overhead for privacy guarantee

## Hardware Targets
None specified

## Techniques
security

## Workloads
CNN

## Metrics
{
  "other": "43.5% SSIM improvement, 4.8% overhead"
}

## Baselines
baseline model inversion defenses

## Limitations
Not discussed; evaluation limited to image classification, generalization to other modalities unclear.

## Tags
privacy, model-inversion, collaborative-inference, security, ensemble, adversarial

## Primary Theme
ensemble-based privacy for collaborative inference

## Confidence
low
