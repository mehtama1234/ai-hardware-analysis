# Interleaved Bitstream Execution for Multi-Pattern Regex Matching on GPUs

**Venue:** MICRO 2025  
**Confidence:** Low (abstract-only)

## Problem
Bitparallel regex engines compile regular expressions into bitstream programs with fine-grained parallelism suited to GPUs, but the naive sequential (per-instruction loop-over-data) execution model suffers from poor data reuse — each data block is loaded multiple times across instructions — and high memory consumption from intermediate bitstream state, limiting GPU throughput.

## Motivation
Pattern matching is a critical and performance-sensitive operation in unstructured data analytics (log processing, network intrusion detection, text search), and GPU-accelerated regex engines are increasingly important, but fundamental memory access inefficiencies prevent them from reaching peak GPU throughput.

## Method
The paper introduces an **interleaved bitstream execution model** in which all bitstream instructions are fused into a single loop executed block-wise, so each data block is loaded once and processed by all instructions. Three enabling techniques:
1. **Dependency-Aware Thread-Data Mapping** — resolves cross-block data dependencies via selective recomputation of dependency chains (not inter-thread communication).
2. **Shift Rebalancing** — restructures dependency chains to reduce synchronization barrier depth between GPU warp threads.
3. **Zero Block Skipping** — exploits bitstream sparsity to skip computation on all-zero intermediate blocks.

## Key Novelty
Fusing all regex bitstream instructions into a single block-wise loop to maximize GPU data reuse, with cross-block dependency resolution via selective recomputation instead of inter-thread communication.

## Contributions
- Interleaved bitstream execution model that fuses all regex instructions into a single data-block-wise GPU loop.
- Dependency-Aware Thread-Data Mapping for correct cross-block dependency resolution.
- Shift Rebalancing to reduce synchronization barrier count.
- Zero Block Skipping for exploiting bitstream sparsity; 19.5x geometric mean speedup over state-of-the-art GPU regex engine.

## Hardware Target
GPU

## Technique Categories
kernel-fusion, parallelism, dataflow

## Workloads
Database / unstructured data analytics (regex pattern matching)

## Metrics
- **Speedup:** 19.5x geometric mean over state-of-the-art GPU regex engine

## Baselines
State-of-the-art GPU bitparallel regex engine

## Limitations
Not discussed (abstract-only analysis)

## Tags
regex, pattern-matching, gpu-kernel, bitstream, data-analytics, kernel-fusion
