# DiffServe: Efficiently Serving Text-to-Image Diffusion Models with Query-Aware Model Scaling

**Problem:** High-quality diffusion models are 4.6-18x slower than lightweight variants, and query demand fluctuates; static resource provisioning wastes capacity or fails SLOs. Standard quality metrics (CLIP, PickScore) cannot identify "easy" queries for lightweight routing.

**Method:** DiffServe routes requests through a lightweight model first, then uses a classifier (EfficientNet-V2) to decide if the result is good enough or should be redone by the heavyweight model; the classifier was trained on real photographs (MSCOCO) and generated images, with the counterintuitive finding that training with only real photos as 'good' works better than also using heavyweight outputs. A controller periodically solves an optimization equation (Mixed Integer Linear Program) that adjusts device counts, batch sizes, and the classifier's confidence threshold to meet latency and throughput targets as demand fluctuates (tracked via exponential smoothing with 5% safety margin), solving asynchronously in ~10ms outside the serving path.

**Key novelty:** Real-image-trained EfficientNet discriminator repurposed for diffusion quality routing; MILP co-optimizing confidence threshold, batch sizes, and device allocation under dynamic demand.

**Metrics:**
- Up to 24% FID improvement vs static configurations
- 19-70% lower SLO violation rate vs static provisioning
- Up to 52x lower SLO violations vs Clipper-Heavy
- Up to 20% FID improvement vs Proteus (random routing)

**Tags:** diffusion-models, model-serving, cascaded-inference, MILP, resource-allocation, text-to-image, SLO

**Confidence:** high
