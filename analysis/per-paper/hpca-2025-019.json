{
  "id": "hpca-2025-019",
  "title": "AsyncDIMM: Achieving Asynchronous Execution in DIMM-Based Near-Memory Processing",
  "venue": "HPCA",
  "problem": "DIMM-based near-memory processing (NMP) architectures cannot efficiently support asynchronous execution between host CPUs and near-memory accelerators (NMAs) because independent memory controllers on both sides cause memory bus contention and potential DRAM errors when issuing concurrent accesses. Existing designs resort to synchronous execution, wasting either host or NMA compute.",
  "motivation": "The memory wall makes near-memory processing attractive for bandwidth-bound workloads, but synchronous host-NMA execution severely underutilizes hardware by idling one side while the other accesses DRAM.",
  "method": "AsyncDIMM lets the host CPU and a near-memory accelerator (a specialized processor attached to memory) share the memory bus through three modes: one device alone, both at once, or switching between them seamlessly. It manages concurrent access using an offload-schedule-return mechanism (host sends work, accelerator executes, accelerator returns results) with explicit synchronization (host waits for a signal) and implicit synchronization (timing coordinates handoffs). Three optimizations minimize delays: expedited work offload, reduced mode-switching overhead via locality-aware methods, and improved result throughput through adaptive batching and timing-division multiplexing notifications. The implementation modifies only existing memory controllers and was validated on an FPGA.",
  "key_novelty": "AsyncDIMM achieves concurrent host and near-memory accelerator access on existing unmodified memory buses through an offload-schedule-return mechanism with dual synchronization modes (explicit and implicit).",
  "contributions": [
    "Identification of memory bus contention and DRAM state races as the fundamental barriers to asynchronous DIMM-NMP execution.",
    "AsyncDIMM: single/concurrent/hybrid access modes with offload-schedule-return synchronization for correct concurrent host+NMA memory access.",
    "Optimized timing constraints, locality-aware switch-recovery, and adaptive batch with TDM notifications to maximize bandwidth and minimize latency.",
    "1.19x-1.92x speedup over four NMP baselines; up to 2.25x bandwidth utilization; 47% access latency reduction; FPGA-validated."
  ],
  "hardware_target": [
    "PIM (processing-in-memory)",
    "CPU"
  ],
  "technique_category": [
    "near-data-processing",
    "memory-system",
    "scheduling",
    "coherence"
  ],
  "workloads": [
    "HPC"
  ],
  "metrics": {
    "speedup": "1.19x-1.92x over four NMP baselines",
    "energy_or_tops_w": null,
    "area": null,
    "ppa": null,
    "accuracy": null,
    "other": "Up to 2.25x bandwidth utilization uplift; 47% access latency reduction"
  },
  "baselines": [
    "Four DIMM-NMP baseline configurations with synchronous execution"
  ],
  "limitations": "Hardware modifications to memory controllers are required on both host and NMA sides; evaluation is on FPGA emulation rather than real DIMM-NMP silicon.",
  "tags": [
    "near-memory-processing",
    "dimm",
    "asynchronous-execution",
    "memory-controller",
    "bandwidth",
    "fpga-validation"
  ],
  "primary_theme": "Asynchronous host-NMA co-execution in DIMM-based near-memory processing",
  "confidence": "low"
}