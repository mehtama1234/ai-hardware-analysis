# Piccolo: Large-Scale Graph Processing with Fine-Grained in-Memory Scatter-Gather

**Venue:** HPCA  
**Confidence:** low (abstract-only)

## Problem
Graph processing requires irregular fine-grained random memory accesses incompatible with DDR memory access granularity, leading to massive bandwidth waste and cache capacity waste; existing approaches using graph tiling (maximizing on-chip reuse) or PIM (placing arithmetic near memory) each have limitations when implemented with DDR standards, and combining them is difficult.

## Motivation
Graph processing is extremely memory-bound due to random access patterns at sub-cache-line granularity, and neither tiling nor PIM alone adequately addresses both the bandwidth waste from DDR granularity mismatch and the flexibility needed for diverse graph operations.

## Method
Piccolo puts a scatter-gather operation directly into memory hardware. Scatter-gather means grabbing data from many random locations or writing data there — the access pattern that wastes bandwidth in graph algorithms. Instead of putting full arithmetic units into DRAM (which is expensive and complex), Piccolo only embeds the scatter-gather operation. The accelerator's cache system and miss-handling architecture are redesigned to work with this in-memory scatter-gather, enabling graph tiling (on-chip data reuse) to work alongside the memory-based operations and reducing off-chip data traffic without the expensive overhead of full processing-in-memory.

## Key Novelty
The core insight is using scatter-gather operations in memory hardware instead of full arithmetic processing, cutting the cost of fine-grained random access while keeping cache and tiling optimizations compatible.

## Contributions
1. Piccolo: graph processing accelerator with fine-grained in-memory scatter-gather (non-arithmetic function-in-memory)
2. Redesigned cache and miss-handling architecture enabling simultaneous tiling and in-memory scatter-gather benefits
3. Avoidance of expensive arithmetic-in-memory while still reducing off-chip traffic from DDR granularity mismatch
4. Up to 3.28x speedup (1.62x geomean) and up to 59.7% energy reduction across graph processing benchmarks

## Hardware Target
ASIC, PIM (processing-in-memory)

## Technique Category
near-data-processing, memory-system, dataflow

## Workloads
graph-analytics

## Metrics
- **Speedup:** Up to 3.28x (1.62x geomean) over prior graph processing accelerators
- **Energy:** Up to 59.7% reduction

## Baselines
- Graph tiling-based accelerators
- PIM-based graph accelerators

## Limitations
The evaluation is limited to DDR-based systems; how Piccolo's in-memory scatter-gather integrates with HBM or GDDR interfaces used in higher-performance graph systems is not discussed.

## Tags
graph-processing, in-memory-computing, scatter-gather, ddr, accelerator

## Primary Theme
non-arithmetic in-memory scatter-gather for graph processing
