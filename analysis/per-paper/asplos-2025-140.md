# asplos-2025-140: CTXNL: A Software-Hardware Co-designed Solution for Efficient CXL-Based Transaction Processing

**Venue:** ASPLOS  
**Confidence:** low (abstract-only)

## Problem
Directly using CXL for multi-node transaction processing incurs high performance overhead because standard CXL cache coherence primitives enforce fine-grained coherence uniformly, while transactional data is inherently loosely coherent and does not require full coherence at every access.

## Motivation
CXL enables low-latency cache-coherent shared memory across nodes, making it attractive for distributed OLTP, but naive adoption wastes bandwidth and latency on unnecessary coherence traffic.

## Method
CTXNL combines software and hardware to speed up transactions on CXL servers. It lets developers specify for each piece of data whether it needs automatic synchronization across servers—a requirement called cache coherence. Most transaction data doesn't need constant synchronization during normal execution, so skipping this overhead saves time. The system only forces synchronization when transactions commit or conflicts are detected, ensuring correctness.

## Key Novelty
CTXNL's innovation is giving developers selective, per-data-item control over CXL cache coherence, so transactions only pay the synchronization cost when data actually needs coordination.

## Metrics
- Up to 2.08x throughput over vanilla CXL memory sharing on OLTP workloads
- Outperforms network-based (RDMA) distributed transaction systems

## Hardware Target
CPU, SoC

## Baselines
Vanilla CXL memory sharing, network-based (RDMA) distributed transaction systems

## Tags
`cxl` `cache-coherence` `transaction-processing` `oltp` `co-design` `disaggregated-memory`
