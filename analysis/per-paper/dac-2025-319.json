{
  "id": "dac-2025-319",
  "title": "HIVE: A High-Priority Victim Cache for Accelerating GPU Memory Accesses",
  "venue": "DAC",
  "problem": "GPU memory hierarchies suffer from high latency when victim caches are accessed as secondary L1D miss handlers, particularly for memory-intensive workloads.",
  "motivation": "CPU-inspired victim cache designs are suboptimal for GPUs; accessing both L1D and its victim cache incurs excessive latency, degrading performance for certain application classes.",
  "method": "Normally, GPUs check their L1D cache (the fastest, primary cache closest to the processor) first, then fall back to the victim cache (a smaller backup cache holding data that didn't fit in L1D). HIVE reverses this for latency-critical memory accesses—it checks the victim cache first instead. This works because those requests can be immediately served from data already in the victim cache, bypassing the delay of first checking L1D and discovering the data isn't there. By prioritizing victim cache lookups for time-sensitive accesses, HIVE reduces the delay on the critical path.",
  "key_novelty": "Reversing the cache hierarchy—checking the victim cache before the L1D cache for latency-critical accesses to reduce critical-path latency (the delay in the time-sensitive execution path).",
  "contributions": [
    "High-priority victim cache design with early memory request interception",
    "77.1% average performance improvement over baseline GPU",
    "21.7% improvement over prior state-of-the-art GPU cache architecture"
  ],
  "hardware_target": [
    "GPU"
  ],
  "technique_category": [
    "cache",
    "memory-system"
  ],
  "workloads": [
    "LLM-inference"
  ],
  "metrics": {
    "speedup_vs_baseline": "77.1%",
    "speedup_vs_sota": "21.7%"
  },
  "baselines": [
    "Baseline GPU architecture",
    "State-of-the-art GPU cache design"
  ],
  "limitations": "Workload scope and specific GPU architecture not detailed; applicability to different GPU families unclear.",
  "tags": [
    "gpu-cache",
    "memory-hierarchy",
    "victim-cache",
    "latency-reduction"
  ],
  "primary_theme": "Victim cache prioritization for GPU memory latency",
  "confidence": "low"
}