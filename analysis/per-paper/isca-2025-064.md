# isca-2025-064 — Single-Address-Space FaaS with Jord

**Venue:** ISCA  
**Confidence:** low (abstract only)

## Problem
FaaS systems incur millisecond-level overheads from isolating functions in separate address spaces (containers or microVMs), preventing microsecond-scale SLOs required by microservice workloads; the address-space isolation forces costly context switches and cross-function data copies.

## Motivation
FaaS was designed for microsecond-level event-driven execution, but all modern implementations impose container/VM overhead that pushes latency to milliseconds, limiting usefulness for latency-sensitive microservices.

## Method
Jord co-locates functions in a single memory space but isolates them using hardware memory protection keys—CPU features that mark which memory regions each function can access, with enforcement at nanosecond speed and no OS involvement required. Functions share memory directly without copying, and Jord's scheduler switches between functions within the process instead of triggering OS context switches. The result is microsecond-scale latency instead of the millisecond overhead of container isolation.

## Key Novelty
Jord achieves microsecond-scale function latency by co-locating functions in a single memory space with hardware-enforced isolation at nanosecond granularity, instead of isolating them in separate containers or virtual machines.

## Contributions
1. A single-address-space FaaS architecture with user-level in-process memory isolation at nanosecond granularity.
2. Zero-copy cross-function communication through shared address space with hardware-enforced isolation.
3. Meets microsecond-level SLOs while performing within 16% of an idealized insecure baseline.
4. Over 2x higher throughput compared to enhanced state-of-the-art FaaS systems.

## Hardware Target
CPU

## Technique Category
virtualization, memory-system, security

## Workloads
database

## Metrics
- **Speedup:** Over 2x throughput vs. state-of-the-art FaaS
- **Other:** Within 16% of idealized insecure baseline; microsecond SLO achieved

## Baselines
container-based FaaS, microVM-based FaaS, idealized insecure baseline

## Limitations
Not discussed; security properties of user-level memory isolation under adversarial function code not thoroughly analyzed.

## Tags
faas, serverless, memory-isolation, single-address-space, microsecond-latency, cloud
