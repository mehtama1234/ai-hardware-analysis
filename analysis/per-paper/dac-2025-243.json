{
  "id": "dac-2025-243",
  "title": "Tropical: Enhancing SLO Attainment in Disaggregated LLM Serving via SLO-Aware Multiplexing",
  "venue": "DAC",
  "problem": "Disaggregated LLM serving isolates prefill and decode on separate workers to optimize one phase but creates high queuing delays; non-disaggregated serving reduces queuing but causes compute interference between phases, violating latency SLOs (TTFT, TPOT).",
  "motivation": "LLM serving systems must meet strict latency SLOs for both time-to-first-token (prefill) and time-per-output-token (decode); current architectures optimize one at the cost of the other.",
  "method": "Tropical introduces SLO-aware multiplexing that dynamically balances prefill and decode execution on hybrid workers to leverage benefits of both disaggregated (low interference) and non-disaggregated (low queuing) architectures. Uses adaptive scheduling to minimize queuing delays while controlling interference, enabling simultaneous TTFT and TPOT SLO compliance.",
  "key_novelty": "First hybrid disaggregated/non-disaggregated LLM serving system using dynamic SLO-aware multiplexing to simultaneously optimize TTFT and TPOT latencies.",
  "contributions": [
    "2.09x throughput improvement at 90% SLO attainment vs. state-of-the-art",
    "9x P90 TTFT improvement over disaggregated systems with 15% P90 TPOT trade-off",
    "2.8x P90 TPOT improvement over non-disaggregated systems at same TTFT",
    "SLO-aware multiplexing strategy balancing queuing and interference"
  ],
  "hardware_target": ["GPU"],
  "technique_category": ["scheduling"],
  "workloads": ["LLM-inference"],
  "metrics": {
    "speedup": "2.09x throughput at 90% SLO",
    "other": "P90 TTFT 9x improvement; P90 TPOT 2.8x improvement"
  },
  "baselines": ["Disaggregated LLM serving systems", "Non-disaggregated LLM serving systems"],
  "limitations": "Evaluation on real-world datasets; hardware-specific optimizations and multi-GPU cluster scaling not detailed.",
  "tags": ["llm-inference", "disaggregated-serving", "scheduling", "slo-optimization", "transformer-serving"],
  "primary_theme": "hybrid disaggregated LLM serving with SLO-aware multiplexing",
  "confidence": "low"
}
