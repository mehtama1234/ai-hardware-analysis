# Caravan: A Hardware/Software Co-Design for Efficient SIMD Neighbor Search on Point Clouds

**Venue:** ISCA 2025  
**Confidence:** low (abstract-only)

## Problem
3D point cloud neighbor search via k-d trees underutilizes SIMD (VPU) lanes because consecutive queries follow similar tree paths but diverge at deeper levels, causing lane sparsity that standard SIMD cannot handle.

## Motivation
Neighbor search is the core bottleneck in 3D computer vision point cloud processing; accelerating it on commodity CPUs with minimal hardware changes offers broader applicability than custom ASICs.

## Method
Caravan groups together multiple neighbor search queries that initially follow the same path through a k-d tree (a tree structure that organizes 3D points to speed up searches), and processes these groups using SIMD instructions (AVX512, which can perform the same operation on many data elements in parallel). As the search goes deeper into the tree, different queries need to take different branches, leaving some processors idle. To keep all processors busy, Caravan adds two new processor instructions: one that reassigns which data each processor is working on, and another that reorganizes the data layout so processors can work efficiently. The entire hardware addition takes up only 0.032 square millimeters of silicon.

## Key Novelty
Two new processor instructions that reassign and reorganize active processors when searches diverge at deep tree levels, eliminating the efficiency loss from unused processing capacity in batched neighbor search without a custom accelerator.

## Contributions
1. Caravan-SW: query batching based on k-d tree traversal similarity for parallel SIMD neighbor search
2. Caravan-HW: two new CPU ISA instructions for lane re-indexing and operand shuffling at leaf levels
3. 5.19x neighbor search speedup (1.97x end-to-end) with HW; 4.05x (1.85x) SW-only
4. Only 0.032 mm2 area overhead for hardware extension

## Hardware Target
CPU (AVX512)

## Key Metrics
- Speedup: 5.19x neighbor search, 1.97x end-to-end (with Caravan-HW); 4.05x / 1.85x (SW only)
- Area: 0.032 mm2 additional

## Baselines
Commodity CPU baseline; custom accelerators (end-to-end comparison)

## Limitations
End-to-end speedup (1.97x) is substantially lower than kernel speedup (5.19x), indicating pipeline bottlenecks beyond neighbor search.

## Tags
`point-cloud` `kd-tree` `simd` `neighbor-search` `isa-extension`
