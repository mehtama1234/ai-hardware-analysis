# Scalable and Asynchronous Differential Checkpointing for Intermittently Powered Devices

**Venue:** ICCAD  
**ID:** iccad-2025-089

## Problem
Intermittently powered devices relying on checkpoint-recovery face scalability challenges: hardware-based differential checkpointing costs increase proportionally with memory size, and synchronous checkpointing blocks the MCU during backups, causing unacceptable latency.

## Motivation
Batteryless energy harvesting systems require efficient checkpointing to survive frequent power failures, but current approaches don't scale to devices with increasing memory capacity or support non-blocking checkpoint operations.

## Method
FLASC is a hardware-software co-design enabling scalable and asynchronous differential checkpointing. The approach:
1. Uses memory modification tracking that scales with increasing memory size
2. Supports safe asynchronous checkpoint execution allowing MCU to continue work during backups
3. Combines efficient synchronization mechanisms with reduced hardware overhead through smart software-hardware partitioning

## Key Novelty
Hardware-software co-designed differential checkpointing achieving scalability and asynchronous operation without proportional hardware cost growth.

## Contributions
- Scalable memory modification tracking mechanism that avoids hardware cost proportional to memory size
- Support for safe asynchronous checkpoint execution enabling MCU progress during backups
- 9.4% faster execution compared to state-of-the-art while using only 49.1% hardware resources
- 6.9× better scalability with increasing data memory size addressing prior architectural limitations

## Hardware Targets
- SoC

## Technique Categories
- memory-system
- reliability
- power

## Workloads
None specified

## Metrics
| Metric | Value |
|--------|-------|
| Speedup | 9.4% execution speedup |
| Energy/TOPS/W | — |
| Area | 49.1% hardware resources vs state-of-the-art |
| PPA | — |
| Accuracy | — |
| Other | 6.9× better scalability with memory size; 1.85× efficiency per hardware resource |

## Baselines
- state-of-the-art hardware-based differential checkpointing

## Limitations
Evaluation limited to FPGA prototyping; silicon implementation and applicability to very large memory systems (>1GB) is not demonstrated.

## Tags
`checkpointing` `energy-harvesting` `intermittent-computing` `memory-management` `low-power`

## Primary Theme
Scalable asynchronous checkpointing for intermittent devices

## Confidence
Low
