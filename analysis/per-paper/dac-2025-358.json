{
  "id": "dac-2025-358",
  "title": "GoPTX: Fine-grained GPU Kernel Fusion by PTX-level Instruction Flow Weaving",
  "venue": "DAC",
  "problem": "Concurrent kernel execution on GPUs misses opportunities to mitigate warp stalls and improve instruction-level parallelism (ILP) through inter-kernel resource sharing due to sequential default execution of kernels.",
  "motivation": "GPU kernel fusion can boost efficiency, but existing approaches overlook ILP improvements from deliberate instruction-level coordination across fused kernels.",
  "method": "GoPTX merges multiple GPU kernels (programs that run on the GPU) into a single program by interleaving their instructions at the PTX level (the GPU's lowest-level instruction format). It uses a latency-aware algorithm—one that tracks how long each instruction takes—to decide which kernel's instruction to run next, and adaptive code slicing to create more scheduling choices. This lets the GPU switch to another kernel's instruction when one stalls (waits for data), keeping the processor busy instead of idle.",
  "key_novelty": "Interleaving instructions from multiple kernels at the PTX level (GPU's lowest-level code) so the GPU can execute any kernel's next instruction when needed instead of running kernels sequentially, reducing stalls and improving instruction-level parallelism.",
  "contributions": [
    "PTX-level kernel fusion approach with merged CFG",
    "Instruction interleaving algorithm minimizing data hazard stalls",
    "Latency-aware weaving algorithm for efficient instruction scheduling",
    "Adaptive code slicing method enlarging scheduling space",
    "Average 11.2% speedup over concurrent execution, max 23%"
  ],
  "hardware_target": [
    "GPU"
  ],
  "technique_category": [
    "kernel-fusion",
    "parallelism",
    "scheduling"
  ],
  "workloads": [
    "CNN",
    "LLM-inference"
  ],
  "metrics": {
    "speedup": "11.2% average, 23% maximum vs concurrent execution",
    "resource_utilization": "enhanced eligible warps per cycle and resource use"
  },
  "baselines": [
    "baseline concurrent kernel execution"
  ],
  "limitations": "Not discussed; focus is on PTX-level optimization.",
  "tags": [
    "gpu",
    "kernel-fusion",
    "ptx",
    "instruction-weaving",
    "ilp",
    "performance-optimization"
  ],
  "primary_theme": "PTX-level kernel fusion via instruction flow weaving for ILP improvement",
  "confidence": "low"
}