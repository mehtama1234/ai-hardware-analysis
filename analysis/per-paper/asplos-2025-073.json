{
  "id": "asplos-2025-073",
  "title": "Aqua: Network-Accelerated Memory Offloading for LLMs in Scale-Up GPU Domains",
  "venue": "ASPLOS",
  "problem": "LLM inference under bursty request patterns exhausts GPU memory, causing serving engines to use admission control that renders them unresponsive; preemptive time-sliced scheduling requires KV-cache paging that incurs high overhead and reduces throughput.",
  "motivation": "Cloud-hosted LLM serving must remain responsive under bursty demand while sustaining high throughput, but current GPU memory management cannot reconcile both goals simultaneously.",
  "method": "Aqua pages LLM request state using the high-bandwidth cables that connect GPUs (NVLink/NVSwitch fabric), storing data on remote GPU memory instead of the system's slow main memory or disk. When the system needs to pause a request to handle a higher-priority one, it moves that request's key-value cache—the data structure tracking what the model computed so far—across these fast GPU cables to another GPU's memory. When the paused request resumes, retrieving this data from a nearby GPU is much faster than reading from slow storage, so the system can efficiently time-slice many requests on a single GPU without performance loss.",
  "key_novelty": "Using the fast cables that connect GPUs (NVLink/NVSwitch) to page LLM key-value caches instead of slow storage, enabling quick pause-resume of requests without throughput loss.",
  "contributions": [
    "Identification that scale-up network bandwidth can be repurposed for fast GPU memory offloading",
    "Aqua GPU memory management framework enabling preemptive time-sliced LLM inference",
    "20x improvement in responsiveness and 4x throughput improvement over state-of-the-art for bursty LLM workloads"
  ],
  "hardware_target": [
    "GPU"
  ],
  "technique_category": [
    "memory-system",
    "scheduling",
    "interconnect"
  ],
  "workloads": [
    "LLM-inference"
  ],
  "metrics": {
    "speedup": "4x throughput improvement over single long prompt baseline; 20x responsiveness improvement vs state-of-the-art",
    "energy_or_tops_w": null,
    "area": null,
    "ppa": null,
    "accuracy": null,
    "other": "evaluated on 8x NVIDIA H100 80GB GPU server"
  },
  "baselines": [
    "state-of-the-art LLM serving engines with admission control"
  ],
  "limitations": "Requires scale-up network (NVLink/NVSwitch) which is only available in high-end multi-GPU server configurations; not applicable to scale-out or single-GPU deployments.",
  "tags": [
    "llm-inference",
    "gpu-memory",
    "kv-cache",
    "paging",
    "nvlink",
    "preemption"
  ],
  "primary_theme": "network-accelerated KV-cache paging for responsive LLM serving",
  "confidence": "low"
}