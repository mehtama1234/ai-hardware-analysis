# micro-2025-084: Symbiotic Task Scheduling and Data Prefetching

**Venue:** MICRO 2025  
**Authors:** Gilead Posluns, Mark C. Jeffrey  
**Confidence:** low (abstract only)

## Problem
Hardware task-parallel systems with fine-grained tasks (~100-cycle execution) cannot benefit from conventional hardware prefetchers because tasks complete before prefetches finish, leaving DRAM latency fully exposed and limiting performance on memory-intensive irregular workloads.

## Motivation
Many-core hardware task-parallel systems (100s of cores) already provide 3-60x speedup over software, but DRAM latency exposure due to ultra-short tasks is the remaining bottleneck; no existing prefetcher is aware of task-function memory patterns or the upcoming dispatch schedule.

## Method
When the hardware task scheduler queues a task, the Task-Seeded Prefetcher (TSP) uses the task descriptor (the instruction packet identifying the task type) to load the required memory addresses into the cache before the task runs. The Memory Response Task Scheduler (MRS) monitors which tasks have completed prefetching and prioritizes assigning those tasks to available cores, ensuring their data is cached before execution starts. Together they create a feedback loop: the scheduler queue signals TSP to prefetch early, and TSP's prefetch completion status guides MRS's scheduling decisions.

## Key Novelty
A task-descriptor-triggered prefetcher paired with a prefetch-status-aware task scheduler creates a feedback loop that hides memory latency in fine-grained task parallelism.

## Contributions
1. TSP: learns and prefetches per-task-function data patterns seeded from task descriptors before dispatch
2. MRS: uses TSP prefetch status to optimize core dispatch decisions
3. Up to 3.1x speedup (geometric mean up to 1.4x) across 13 benchmarks on 256-core task-parallel systems
4. Additive benefit on systems already 3-60x faster than parallel software

## Hardware Target
CPU, ASIC (many-core task-parallel hardware)

## Techniques
prefetching, scheduling, memory-system

## Workloads
graph-analytics, HPC (irregular task-parallel applications)

## Key Metrics
- Speedup: up to 3.1x; geometric mean up to 1.4x across 13 benchmarks on 256-core systems

## Baselines
Hardware task-parallel systems without prefetching (themselves already 3-60x over parallel software)

## Limitations
Not discussed (abstract-only analysis).

## Tags
task-parallelism, prefetching, scheduling, many-core, data-locality, irregular-workloads
