# Snowplow: Effective Kernel Fuzzing with a Learned White-box Test Mutator

**Venue:** ASPLOS 2025  
**ID:** asplos-2025-050  
**Confidence:** low (abstract only)

## Problem
Kernel fuzzers rely on program mutation to generate test programs exploring new code paths, but existing heuristics cannot navigate complex kernel control/data flow to trigger target code locations or increase coverage effectively.

## Motivation
OS kernel bugs are critical security vulnerabilities; manual mutation heuristics cannot keep pace with modern kernel complexity, leaving large code regions unexplored.

## Method
Snowplow uses a machine learning model trained on white-box kernel code analysis (control flow graphs, data flow) to guide mutation decisions—predicting which syscall insertions, argument changes, or structural mutations are most likely to reach uncovered code. White-box kernel source analysis provides the coverage feedback signal for training and inference.

## Key Novelty
A machine learning model trained on white-box kernel code analysis to guide test mutation, replacing manual heuristics with learned policies targeting specific kernel code locations.

## Contributions
- Snowplow: kernel fuzzer with learned white-box mutation model replacing hand-crafted heuristics.
- Higher kernel code coverage and improved bug-finding vs state-of-the-art fuzzers.
- Training methodology using kernel control/data flow as white-box features.

## Hardware Target
CPU

## Techniques
security

## Tags
fuzzing, kernel, machine-learning, white-box, security-testing
