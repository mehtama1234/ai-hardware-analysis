# FIGLUT: An Energy-Efficient Accelerator Design for FP-INT GEMM Using Look-Up Tables

**Venue:** HPCA  
**Confidence:** low (abstract-only)

## Problem
Weight-only quantization for LLM deployment produces FP-INT mixed-precision GEMM operations where one operand is floating-point (activations) and the other is low-bit integer (weights); this FP-INT compute is difficult to implement efficiently on conventional GPU multiply-accumulate hardware.

## Motivation
Weight-only quantization is a leading approach to reduce LLM memory footprint and bandwidth, but hardware that efficiently executes the resulting FP-INT GEMM is lacking, limiting achievable TOPS/W.

## Method
FIGLUT implements GEMM using a look-up table (LUT) approach: rather than performing FP-INT multiplications directly, the hardware precomputes all possible products of the floating-point activation values with each possible weight pattern and stores them in LUTs. At inference time, the LUT is indexed by weight bits to retrieve the precomputed partial product. A half-size LUT variant is introduced along with a dedicated decoding and multiplexing unit to reduce LUT storage overhead. The design supports multiple bit precisions and quantization methods in a single fixed hardware configuration.

## Key Novelty
A half-size LUT with dedicated decode/mux that replaces FP-INT multipliers with table lookups, enabling configurable multi-precision weight-only quantized GEMM with 59% higher TOPS/W than prior LUT-based accelerators.

## Contributions
- FIGLUT LUT-based GEMM architecture replacing FP-INT multipliers with precomputed lookup tables
- Half-size LUT design with dedicated decoding and multiplexing unit reducing storage area
- Single fixed hardware configuration supporting multiple bit precisions and quantization methods
- 59% higher TOPS/W and 20% lower perplexity than state-of-the-art at 3-bit; 98% higher TOPS/W at matched perplexity using 2.4-bit operations

## Hardware Target
ASIC

## Techniques
quantization, circuit-design, dataflow

## Workloads
LLM-inference

## Metrics
- Energy: 59% higher TOPS/W at 3-bit; 98% higher TOPS/W at matched perplexity (2.4-bit)
- Accuracy: 20% lower perplexity vs state-of-the-art at 3-bit weight precision

## Baselines
State-of-the-art LUT-based accelerator for weight-only quantization

## Limitations
Not discussed; LUT precomputation may add latency overhead for small batch sizes or rapidly changing activation distributions.

## Tags
weight-only-quantization, lut, fp-int-gemm, llm-inference, energy-efficiency

## Primary Theme
LUT-based FP-INT GEMM for weight-only quantized LLM inference
