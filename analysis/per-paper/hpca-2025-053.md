# Marching Page Walks: Batching and Concurrent Page Table Walks for Enhancing GPU Throughput

**Venue:** HPCA  
**Confidence:** low (abstract-only)

## Problem
GPU page table walkers handle only a single walk request at a time, creating severe queuing latency when thousands of concurrent threads simultaneously demand address translation across wide virtual address ranges.

## Motivation
Virtual memory support is critical for GPU programmability, but the mismatch between single-request-per-walker hardware and massively parallel GPU thread address translation demands causes a major performance bottleneck.

## Method
Marching Page Walks (MPW) scans the pool of pending walk requests to identify groups that can share memory accesses. It batches these requests and concurrently issues multiple memory instructions per walker to service several walks simultaneously, exploiting the fact that GPU workloads access pages in wide but often co-located address ranges.

## Key Novelty
Enabling a single GPU page table walker to issue multiple memory instructions concurrently by batching pending walk requests that share page table structure accesses, directly attacking queuing latency rather than walker count.

## Contributions
1. Identification of walker queuing latency as the dominant bottleneck in GPU address translation
2. MPW design that scans and groups pending walk requests sharing page table memory accesses
3. Concurrent multi-request issuance per walker mechanism
4. 86.7% reduction in page walk queuing latency and 55.6% average GPU performance improvement

## Hardware Target
GPU

## Technique Categories
memory-system, cache, prefetching

## Workloads
CNN, HPC, graph-analytics

## Metrics
- Speedup: 55.6% average GPU performance improvement over baseline
- Page walk queuing latency reduction: 86.7%

## Baselines
- Baseline GPU with single-request page walkers

## Limitations
Effectiveness depends on GPU workloads exhibiting co-located page access patterns; irregular workloads with fully disjoint addresses may see limited benefit.

## Tags
`gpu-mmu` `page-table-walk` `address-translation` `virtual-memory` `throughput`
