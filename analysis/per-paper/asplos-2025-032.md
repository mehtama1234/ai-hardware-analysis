# Treelet Accelerated Ray Tracing on GPUs

**ID:** asplos-2025-032  
**Venue:** ASPLOS  
**Confidence:** low (abstract-only)  
**Primary Theme:** Treelet-based BVH traversal with ray virtualization for GPU ray tracing

## Problem
GPU ray tracing suffers from poor memory access locality because rays traverse a BVH tree in divergent order, causing excessive DRAM traffic; while treelet-based traversal (grouping BVH subtrees and batching rays per treelet) reduces memory traffic on specialized accelerators, applying it to GPUs is non-trivial and naive implementations are ineffective.

## Motivation
Real-time ray tracing in games and rendering engines is limited by frame-rate penalties from memory bandwidth bottlenecks during BVH traversal, causing users to disable it; GPU-native treelet traversal could close this gap without specialized hardware.

## Method
The paper proposes Virtualized Treelet Queues with two main components. First, ray virtualization terminates raygen shaders after issuing their trace-ray instruction to free CUDA cores, increasing the number of rays concurrently in flight and enabling more cache reuse within treelets. Second, a dynamic treelet queue architecture switches between traversal modes (intra-treelet vs. inter-treelet) at runtime to maximize efficiency. Additionally, warp repacking is applied to the RT unit to improve SIMT efficiency during treelet traversal.

## Key Novelty
Ray virtualization that reclaims CUDA cores from waiting raygen shaders to multiply in-flight rays, paired with a dynamic treelet queue to exploit the resulting cache reuse—achieving 95% average speedup on a baseline GPU with hardware RT acceleration.

## Contributions
- Ray virtualization mechanism that terminates post-trace raygen shaders to increase concurrent in-flight rays and cache reuse.
- Dynamic treelet queue architecture that switches traversal modes to balance intra- and inter-treelet work.
- Warp repacking technique for improving SIMT efficiency in the GPU's hardware RT unit during treelet traversal.
- Average 95% speedup over baseline GPU RT across all scenes in LumiBench with path tracing.

## Hardware Target
GPU

## Technique Categories
memory-system, cache, scheduling, dataflow

## Workloads
vision

## Metrics
- **Speedup:** 95% average speedup vs. baseline GPU with RT acceleration (LumiBench, path tracing, 1 spp, 3 bounces)

## Baselines
- baseline GPU with hardware ray tracing acceleration (no treelet)

## Limitations
Not discussed; simulation-based results may not fully account for all GPU architectural constraints in real silicon.

## Tags
ray-tracing, gpu, bvh, treelet, memory-bandwidth, rendering
