# asplos-2025-059 — Exo 2: Growing a Scheduling Language

**Venue:** ASPLOS  
**Confidence:** low (abstract-only)

## Problem
User-schedulable languages (USLs) fix a single design point for what programmers control and what is automated, but no universal control/automation split exists even in narrow domains, causing USLs to be inextensible when new hardware targets or optimization strategies emerge.

## Motivation
High-performance kernel authors for different hardware targets need different scheduling operations, and forcing them to use a fixed built-in set either over-constrains or under-automates their workflow, increasing scheduling code volume and effort.

## Method
Exo 2 lets programmers add new code-optimization strategies (scheduling operations) outside the compiler by combining small building blocks—actions that modify code, inspections that examine code structure, and references that point to locations. It introduces Cursors, a unifying abstraction that lets code references work the same way across all these building blocks, keeping user-written transformations safe and separated from the compiler's core. Programmers then build reusable scheduling libraries that apply the same transformation across many related kernels (compute-intensive code patterns), instead of requiring compiler changes each time.

## Key Novelty
Cursors enable programmers to safely externalize and compose reusable scheduling libraries, letting the language grow without modifying the compiler's core.

## Contributions
- Exo 2 architecture for externally extensible user-schedulable languages via composable trusted primitives.
- Cursor abstraction unifying code actions, inspection, and references to support user-defined scheduling operations.
- Scheduling libraries demonstrated across 80+ high-performance kernels reducing scheduling code by an order of magnitude.
- Performance competitive with state-of-the-art implementations on three different hardware platforms.

## Hardware Target
CPU, ASIC

## Technique Categories
compiler, scheduling, dataflow

## Workloads
CNN, LLM-inference

## Metrics
- Performance competitive with state-of-the-art on 3 platforms
- Scheduling code reduced by ~10x across 80+ kernels

## Baselines
- State-of-the-art hand-optimized kernel libraries
- Existing USLs (Halide, TVM)

## Limitations
Correctness of user-defined scheduling operations still depends on correct composition of trusted primitives; unsound compositions can violate safety.

## Tags
scheduling-language, compiler, kernel-optimization, extensibility, user-schedulable

## Primary Theme
extensible user-schedulable compiler language
