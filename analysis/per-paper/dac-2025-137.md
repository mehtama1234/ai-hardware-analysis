# NDFT: LR-TDDFT Acceleration via Near-Data Computing

## Problem
Linear-response time-dependent DFT (LR-TDDFT) is memory-bound with constant data movement between host and heterogeneous accelerators (GPU, FPGA, Sunway), creating substantial data movement overhead that dominates compute-optimized kernels.

## Method
Near-data processing framework that partitions LR-TDDFT tasks across CPU and NDP units optimally, offloading each computation phase to the most suitable processing element. Critical kernels undergo hardware/software co-optimization on the CPU-NDP system to further reduce data movement and latency.

## Key Results
- 5.2x speedup over CPU baseline
- 2.5x speedup over GPU baseline
- Effective for large physical systems with reduced data movement overhead

## Hardware Target
CPU, PIM

## Technique
Near-data Processing, Scheduling, Memory System

## Workload
HPC

## Tags
near-data-processing, dft, hpc-acceleration, memory-bandwidth, co-design

---
*Confidence: low (abstract only)*
