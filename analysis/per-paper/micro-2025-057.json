{
  "id": "micro-2025-057",
  "title": "Pimba: A Processing-in-Memory Acceleration for Post-Transformer Large Language Model Serving",
  "venue": "MICRO",
  "problem": "Post-transformer LLMs (SSMs such as Mamba-2, linear attention, RNNs) and transformer-based LLMs both become memory-bandwidth-bound during batched inference due to state update and attention operations respectively, but the diverse primitives in state updates (element-wise multiply, outer product, GEMV) make per-bank PIM acceleration area-prohibitive, and existing LLM quantization formats degrade accuracy severely on SU-LLMs due to the swamping effect.",
  "motivation": "Hyperscalers are exploring post-transformer architectures as bandwidth-efficient complements to transformers for long-context serving; a unified hardware solution supporting both model families does not yet exist.",
  "method": "Pimba places a specialized processor (SPU) directly in memory to handle math-heavy operations that become bottlenecks during AI model generation. It shares this single processor between two memory banks using interleaved access—while one bank reads data, the other writes results simultaneously, keeping the processor constantly busy without needing separate hardware per bank. The processor contains custom MX8 math units (floating-point format with 8-bit shared exponent and 1-bit microexponents per element group, using stochastic rounding) and processes data through four pipeline stages: state fetch, state decay and outer product in parallel, accumulation, and GEMV output with write-back. The system offloads state updates and attention operations to the PIM accelerator during the generation phase while the GPU handles the initial data pass (prefill).",
  "key_novelty": "Sharing a single pipelined processor between two memory banks via interleaved access (reading from one bank while the other writes simultaneously) halves the chip area needed for processing-in-memory acceleration compared to per-bank designs, while maintaining the same throughput.",
  "contributions": [
    "Systematic workload characterization showing state update operations in SU-LLMs are memory-bandwidth-bound and dominate generation latency at large batch sizes",
    "Quantization study identifying MX8 with stochastic rounding as Pareto-optimal for SU-LLMs, where fp8 formats fail due to the swamping effect in iterative state updates",
    "Pimba architecture: SPU shared between two banks with access interleaving and a 4-stage MX8 pipeline, achieving up to 4.1x throughput over LLM-optimized GPUs with minimal area overhead",
    "End-to-end system design supporting both transformer (via attention mode) and post-transformer LLMs in a unified PIM framework"
  ],
  "hardware_target": [
    "PIM (processing-in-memory)",
    "GPU"
  ],
  "technique_category": [
    "near-data-processing",
    "quantization",
    "memory-system",
    "dataflow"
  ],
  "workloads": [
    "LLM-inference",
    "attention",
    "MoE"
  ],
  "metrics": {
    "speedup": "up to 4.1x token generation throughput over LLM-optimized GPU; up to 2.1x over GPU+PIM baseline; 14.6x lower state-update latency vs GPU",
    "other": "Area overhead described as minimal on DRAM device; MX8 achieves near fp16 perplexity on Mamba-2/RetNet/GLA/HGRN2"
  },
  "baselines": [
    "A100 GPU (LLM-optimized)",
    "GPU+PIM systems (per-bank time-multiplexed and pipelined PIM designs)"
  ],
  "limitations": "Evaluation is simulation-based; real DRAM integration with SPUs has not been taped out, and full system energy is not reported.",
  "tags": [
    "pim",
    "llm-inference",
    "ssm",
    "mamba",
    "quantization",
    "mx8",
    "post-transformer"
  ],
  "primary_theme": "PIM accelerator for post-transformer LLM serving with interleaved bank sharing",
  "confidence": "high"
}