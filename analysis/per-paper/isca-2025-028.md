# isca-2025-028 — MagiCache: A Virtual In-Cache Computing Engine

**Venue:** ISCA 2025  
**Confidence:** low (abstract only)

## Problem
Array-level in-cache computing architectures commit entire SRAM arrays to compute roles, leaving most computing lines idle at runtime and causing severe on-chip capacity loss. Bursty memory accesses in data-parallel applications also incur high in-cache data movement latency.

## Motivation
Data-parallel workloads need in-cache computation to eliminate cache-to-processor data movement, but current coarse-grained array-level designs waste capacity because only a few rows per array are active at any time.

## Method
MagiCache allows each cache array to dynamically switch individual rows between being compute lines or regular cache lines on the fly, rather than dedicating entire arrays to one role. A virtual engine (software running on the chip) monitors what the program needs at each moment—how much storage versus how much computing—and reallocates rows accordingly. To handle sudden bursts of memory requests without stalling, MagiCache uses instruction chaining: it schedules compute operations and memory requests out-of-order and in parallel, so the long wait for data from HBM/DRAM (the main memory) overlaps with ongoing computation.

## Key Novelty
MagiCache dynamically reallocates individual cache rows between compute and storage roles and uses instruction chaining to hide memory latency, eliminating the fixed capacity loss of array-level in-cache architectures.

## Contributions
1. Cacheline-level in-cache architecture with per-row dynamic assignment (compute vs. cache).
2. Virtual engine for runtime space management based on observed compute/storage demand.
3. Instruction chaining for asynchronous computing array execution to overlap memory latency.
4. 1.19–1.61x speedup over state-of-the-art in-cache computing with only 6.5 KB extra storage.

## Hardware Target
CIM (Compute-in-Memory / in-cache computing)

## Metrics
| Metric | Value |
|--------|-------|
| Speedup | 1.19–1.61x over state-of-the-art in-cache computing |
| Additional storage | 6.5 KB |

## Baselines
- State-of-the-art array-level in-cache computing architectures

## Limitations
Moderate speedup range (1.19–1.61x); energy efficiency and comparison to near-memory processing alternatives not discussed in abstract.

## Tags
in-cache-computing, compute-in-memory, cache-management, data-parallel, virtual-engine
