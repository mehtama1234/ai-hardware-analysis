# WarmCache: Exploiting STT-RAM Cache for Low-Power Intermittent Systems

**ID:** isca-2025-045 | **Venue:** ISCA | **Confidence:** low (abstract only)

## Problem
Energy harvesting systems (EHS) suffer from compulsory cache misses after power failures because volatile SRAM caches lose all data across power outages. Existing non-volatile cache designs impose high write energy and latency overhead.

## Motivation
Intermittent computing on energy-harvested devices is increasingly important for IoT/edge applications, but power-outage-induced compulsory misses impose prohibitive performance penalties with traditional caches.

## Method
WarmCache caches data in STT-RAM (magnetic memory that survives power outages but with reduced data retention—it loses accuracy over time but lasts longer than regular SRAM). The compiler breaks the program into regions and checks cache data at each region boundary for retention errors (data corruption from the reduced retention time). If errors are found, that region is re-executed to restore correct cached data. The compiler aligns region boundaries with the STT-RAM's refresh interval by stitching short regions together and splitting long ones, which reduces how often corruption occurs.

## Key Novelty
WarmCache preserves cache across power failures using relaxed-retention STT-RAM (magnetic memory) instead of fully persistent storage, paired with compiler-driven error detection and re-execution to avoid write-back overhead.

## Contributions
1. Insight that STT-RAM retention time need only exceed power outage duration, not persist indefinitely.
2. Region-level error correction scheme: scrubbing at region boundaries and re-executing on detected multi-bit errors.
3. Compiler region formation with stitching and splitting to match region size to scrubbing interval.
4. 1.3-1.4x average performance improvement over state-of-the-art EHS cache designs.

## Hardware Target
ASIC, SoC

## Techniques
cache, reliability, compiler, circuit-design

## Workloads
HPC

## Metrics
- **Speedup:** 1.3-1.4x over state-of-the-art EHS cache design

## Baselines
State-of-the-art STT-RAM cache design for EHS

## Limitations
Relies on power outage durations being shorter than the relaxed STT-RAM retention time; longer outages would cause data loss and correctness failures.

## Tags
STT-RAM, intermittent-computing, energy-harvesting, non-volatile-cache, error-correction, compiler
