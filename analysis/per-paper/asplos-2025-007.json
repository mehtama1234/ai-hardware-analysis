{
  "id": "asplos-2025-007",
  "title": "AnyKey: A Key-Value SSD for All Workload Types",
  "venue": "ASPLOS",
  "problem": "Existing Key-Value SSD (KV-SSD) designs are optimized only for workloads where values are much larger than keys; when key sizes are relatively large (an unexplored but real workload class), the expanded key metadata causes significantly degraded performance and potentially unbounded metadata growth.",
  "motivation": "Large-scale KV store applications are increasingly targeting KV-SSDs for cost-effective and scalable storage, but a hidden assumption of small-key workloads limits existing designs to only one workload type, creating a deployment gap for real-world diverse workloads.",
  "method": "AnyKey redesigns the KV-SSD metadata management architecture to prevent metadata size from scaling with key size. It introduces a metadata-bounded key representation that separates key identity from the full key payload, storing only fixed-size key references (hashes or compact descriptors) in the primary metadata structures while managing full key payloads separately. This ensures metadata overhead remains bounded regardless of key size distribution.",
  "key_novelty": "A metadata-bounded KV-SSD architecture that decouples key metadata size from actual key size, enabling uniform performance across both small-key and large-key workload types.",
  "contributions": [
    "First evaluation of KV-SSD designs under large-key workloads, exposing significant performance degradation in existing designs.",
    "AnyKey's metadata-bounded key management architecture that prevents metadata bloat under varying key sizes.",
    "Broad evaluation across diverse real-world workloads demonstrating consistent performance over existing KV-SSD designs."
  ],
  "hardware_target": ["ASIC"],
  "technique_category": ["memory-system", "cache"],
  "workloads": ["database"],
  "metrics": null,
  "baselines": ["state-of-the-art KV-SSD design"],
  "limitations": "Not discussed; potential additional latency or area cost from the metadata indirection scheme is not quantified.",
  "tags": ["kv-ssd", "storage", "key-value", "metadata-management", "workload-diversity"],
  "primary_theme": "Workload-agnostic KV-SSD metadata management",
  "confidence": "low"
}
