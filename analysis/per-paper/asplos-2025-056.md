# asplos-2025-056 — ShadowLoad: Injecting State into Hardware Prefetchers

**Venue:** ASPLOS  
**Confidence:** low (abstract-only)

## Problem
Hardware prefetchers in modern CPUs maintain internal state (stride tables, stream detectors) that persists across security boundaries, and prior work showed this state leaks information across processes, user/kernel space, and TEEs. This paper extends the threat by demonstrating that an attacker can actively inject crafted state into a victim's prefetcher, turning it into a covert channel or interference primitive.

## Motivation
As microarchitectural side-channel attacks grow in sophistication, the ability to actively manipulate—not merely observe—hardware prefetcher state represents a new class of cross-boundary interference that existing mitigations (context-switch prefetcher flushes) do not fully address.

## Method
ShadowLoad crafts memory access patterns that train the CPU's prefetcher (a hardware unit predicting which memory locations will be needed next) to a desired state by manipulating its internal prediction structures: stride tables and stream buffers. When a victim process runs, it inherits this attacker-controlled prefetcher state, causing speculative memory fetches that alter the victim's cache contents and execution timing. The attack works across process, user/kernel, and secure-enclave boundaries because CPUs don't clear or isolate prefetcher state during privilege transitions.

## Key Novelty
The core novelty is actively injecting attacker-controlled state into a victim's prefetcher to influence execution, as opposed to passively observing prefetcher side-channel leaks.

## Contributions
- Demonstration of prefetcher state injection as an active cross-boundary attack primitive on real CPUs.
- Analysis of prefetcher state persistence across process, user-kernel, and TEE security boundaries.
- Concrete attack scenarios using ShadowLoad for covert channels and cross-privilege interference.
- Discussion of mitigation strategies and their limitations.

## Hardware Target
CPU

## Technique Categories
prefetching, security, cache

## Workloads
(none)

## Metrics
- Covert channel bandwidth and cross-boundary injection success rates (details in full paper).

## Baselines
- Passive prefetcher side-channel attacks
- Existing TEE isolation

## Limitations
Scope is limited to specific prefetcher microarchitectures; generality across vendors not fully established from abstract.

## Tags
hardware-prefetcher, side-channel, microarchitecture, security, tee

## Primary Theme
active prefetcher state injection attack
