# GraalNN: Context-Sensitive Static Profiling with Graph Neural Networks

**Venue:** CGO
**Confidence:** low

## Problem
Static profile prediction (without runtime profiles) is needed for optimal optimization, but existing methods lose precision by not exploiting complex CFG structure or calling context information. Accurate prediction requires better feature engineering.

## Motivation
Dynamic profiles are not always available; static profile prediction enables optimization before first execution. Context-sensitive profiling is critical for inlining and specialization decisions.

## Method
GraalNN uses Graph Neural Networks to learn structural information directly from control-flow graphs without handcrafted features. Two-stage: context-insensitive parsing prediction, then contextual refinement using call graph during inlining.

## Key Novelty
Direct learning of CFG structure via GNN instead of handcrafted features, combined with two-stage context refinement via call graph.

## Contributions
- GNN-based static profiling framework learning from CFG structure
- Two-stage context-insensitive + context-sensitive prediction
- 10.13% runtime speedup across industry benchmarks, 2.5x better than prior static profiling
- 3.7% throughput improvement on microservices

## Hardware Targets
CPU

## Technique Categories
compiler

## Workloads
database

## Metrics
{'speedup': '10.13% runtime, 3.7% throughput on microservices'}

## Baselines
Prior static profiling methods

## Limitations
Evaluation on JVM-based systems; generalization to other runtimes not discussed.

## Tags
static-profiling, gnn, compiler-optimization

## Primary Theme
GNN-based context-sensitive static profiling
