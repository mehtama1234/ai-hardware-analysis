{
  "id": "dac-2025-288",
  "title": "Me-MPK: Accelerating Krylov Subspace Solvers via Memory-efficient Matrix-Power Kernel",
  "venue": "DAC",
  "problem": "Matrix-Power Kernels (MPK) for Krylov subspace methods rely on repeated SpMVs with the same matrix but suffer from data dependencies that block cache reuse and symmetric SpMVs incur data conflicts during shared-memory parallelization, limiting speedup on multi-core CPUs.",
  "motivation": "Large sparse linear systems in circuit simulation, HPC, and scientific computing require efficient Krylov solvers; existing methods optimize matrix reuse OR symmetry independently, missing the opportunity to exploit both simultaneously.",
  "method": "Me-MPK first creates a unified dependency graph—a complete map of all data dependencies and conflicts in the sparse matrix. It then recursively partitions this matrix into independent chunks separated by a buffer zone, each optimized for the CPU's specific architecture. Since the chunks run in a specific parallel order and have no conflicts between them, the CPU can reuse data in its cache while eliminating synchronization delays between processors.",
  "key_novelty": "A unified dependency graph combined with recursive partitioning solves parallelization conflicts in sparse matrix operations—including symmetric matrices—while maximizing cache reuse.",
  "contributions": [
    "Unified dependency graph model capturing cache reuse and symmetry conflict information",
    "Architecture-aware recursive partitioning algorithm for conflict decoupling",
    "Ordered parallel execution strategy optimizing cache reuse across subgraphs",
    "1.65× and 1.58× overall solver speedup on X86 and ARM respectively (2.0× and 1.86× on MPK kernel alone)"
  ],
  "hardware_target": [
    "CPU"
  ],
  "technique_category": [
    "cache",
    "parallelism",
    "memory-system"
  ],
  "workloads": [
    "HPC",
    "database"
  ],
  "metrics": {
    "speedup": "1.65× overall (X86), 1.58× (ARM); 2.00× on MPK (X86), 1.86× (ARM)"
  },
  "baselines": [
    "state-of-the-art sparse solver implementations",
    "prior matrix-reuse and symmetry-aware SpMV approaches"
  ],
  "limitations": "Overhead of partitioning and conflict detection not characterized; scalability to GPU/heterogeneous architectures not addressed.",
  "tags": [
    "sparse-linear-solvers",
    "krylov-methods",
    "cache-optimization",
    "circuit-simulation",
    "multi-core"
  ],
  "primary_theme": "memory-efficient sparse solver via unified dependency-driven partitioning",
  "confidence": "low"
}