# ReaL: Efficient RLHF Training of Large Language Models with Parameter Reallocation

**ID:** mlsys-2025-030 | **Venue:** MLSYS 2025 | **Confidence:** high

## Problem
RLHF training involves multiple model functions (actor, critic, reference, reward) with radically different compute/memory profiles, but existing systems use static parallelization that leaves GPU resources underutilized.

## Method
Parameter reallocation: model weights are redistributed across GPUs between pipeline stages so each stage uses its optimal parallelization config. An MCMC-based planner searches the joint parallelization + scheduling space to find the best execution plan.

## Key Novelty
First system to dynamically reallocate LLM parameters between RLHF pipeline stages, enabling per-stage optimal parallelism without redundant weight copies.

## Metrics
- **Speedup:** 3.58x over DeepSpeed-Chat/OpenRLHF/NeMo-Aligner/veRL; 81% over Megatron-LM heuristic (8192 ctx, H100 128-GPU)
- **Accuracy:** No degradation in reward model quality

## Baselines
DeepSpeed-Chat, OpenRLHF, NeMo-Aligner, veRL, Megatron-LM

## Tags
`rlhf` `parameter-reallocation` `parallelism` `mcmc` `llm-training` `scheduling`
