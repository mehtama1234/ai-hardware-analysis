{
  "id": "asplos-2025-040",
  "title": "ARC: Warp-level Adaptive Atomic Reduction in GPUs to Accelerate Differentiable Rendering",
  "venue": "ASPLOS",
  "problem": "During the backward pass of rasterization-based differentiable rendering (e.g., 3D Gaussian Splatting), gradient computation requires a large volume of atomic add operations to accumulate gradients from many Gaussian primitives into shared parameters, overwhelming the atomic units in the GPU L2 cache and causing long stalls that dominate training time.",
  "motivation": "3D Gaussian Splatting training is widely used for novel-view synthesis and 3D scene reconstruction, but long backward-pass stalls from atomic contention block real-time or interactive use on modern GPU hardware.",
  "method": "In 3D rendering models that compute gradients for learning, many GPU threads simultaneously try to add their values to the same shared locations, creating contention (competition for the same resource). These requests pile up at the atomic units in the GPU's L2 cache (the hardware coordinating simultaneous updates), creating stalls that slow training. ARC intercepts these requests at the warp level—a group of 32 threads that communicate quickly with each other—before they reach that bottleneck. Threads updating the same memory location first combine their updates into one using fast warp communication, then send that single combined update instead of many separate ones, with automatic detection to apply this combining only when contention is detected.",
  "key_novelty": "ARC combines atomic operations at the warp level before they reach the GPU's bottleneck, with automatic detection to enable this combining only when many operations are trying to happen simultaneously.",
  "contributions": [
    "Identification and characterization of atomic operation contention as the dominant bottleneck in 3D Gaussian Splatting backward pass on GPUs.",
    "ARC: a warp-level adaptive atomic reduction mechanism that coalesces intra-warp atomics targeting the same address before issuing to L2.",
    "Demonstration of significant training speedup for differentiable rendering workloads without loss of rendering quality."
  ],
  "hardware_target": [
    "GPU"
  ],
  "technique_category": [
    "memory-system",
    "kernel-fusion",
    "scheduling"
  ],
  "workloads": [
    "diffusion",
    "vision"
  ],
  "metrics": {
    "speedup": "not stated in abstract",
    "energy_or_tops_w": null,
    "area": null,
    "ppa": null,
    "accuracy": null,
    "other": null
  },
  "baselines": [
    "baseline GPU without ARC"
  ],
  "limitations": "Not discussed; the approach targets atomic-heavy rendering kernels and may not generalize to sparse or non-contiguous atomic patterns.",
  "tags": [
    "differentiable-rendering",
    "atomic-reduction",
    "GPU",
    "3D-gaussian-splatting",
    "memory-contention"
  ],
  "primary_theme": "warp-level atomic reduction for differentiable rendering",
  "confidence": "low"
}