# Supply-Chain Attacks in Machine Learning Frameworks

**Problem:** ML frameworks have ~20x more dependencies than comparable non-ML packages, but existing supply-chain defenses only detect OS-level threats (system calls, network traffic), leaving ML-specific attacks undetected.

**Method:** Two parts: (1) Attack demonstrations using Python's shared memory model - global variable overwriting (mutating PyTorch/TorchVision globals at import time) and local variable overwriting (using inspect + undocumented PyFrame_LocalsToFast to modify call-stack locals). Three attacks: backdoor via model forward-function hijacking, pipeline vulnerability via interpolation enum swap, model-stealing via steganographic output encoding. (2) Security awareness study: LLM-assisted analysis of 549,635 GitHub issues/PRs from Top-50 ML repos using GPT-3.5 filter then GPT-4 classifier (97.8% recall, 78.9% precision).

**Key novelty:** First systematic ML-specific supply-chain attack class exploiting Python's shared memory; comprehensive GitHub security awareness study at scale using LLMs.

**Metrics:**
- Analysis pipeline: 97.8% recall, 78.9% precision, 0.85 Cohen's kappa
- 549,635 issues/PRs analyzed across Top-50 ML repos
- ML community security awareness statistically similar to Linux community (despite higher risk)

**Tags:** security, supply-chain, Python, adversarial-ML, backdoor, model-stealing, GitHub-analysis

**Confidence:** high
