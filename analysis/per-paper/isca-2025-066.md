# isca-2025-066 — ANSMET: Approximate Nearest Neighbor Search with Near-Memory Processing and Hybrid Early Termination

**Venue:** ISCA  
**Confidence:** low (abstract only)

## Problem
ANNS over large high-dimensional vector databases is both highly memory-bound and computationally wasteful: many fetched vectors and their distance computations are ultimately discarded because they are far from the query, resulting in excess memory traffic and low data utilization.

## Motivation
Vector databases are growing rapidly for LLM retrieval-augmented generation and recommendation systems; ANNS at scale requires processing billions of high-dimensional vectors with tight latency budgets, making bandwidth waste and unnecessary computation a critical bottleneck.

## Method
ANSMET co-designs a rank-level near-data processing (NDP) architecture with a hybrid partial-dimension/bit early termination strategy. Distance calculations are offloaded to NDP units inside memory ranks. As a vector streams from memory, ANSMET estimates a conservative distance lower bound using partial dimensions and/or partial bits; if this bound exceeds a threshold, the fetch terminates early. Data layout within and across ranks is co-optimized for early termination granularity, with lightweight host-NDP coordination for index traversal and result sorting.

## Key Novelty
The hybrid partial-dimension/bit early termination strategy conservatively estimates distance lower bounds during streaming to skip unnecessary data fetches, combined with rank-level NDP to eliminate host memory bandwidth consumption for rejected ANNS candidates.

## Contributions
1. A rank-level NDP architecture offloading ANNS distance calculation, eliminating off-chip data movement for rejected vectors.
2. A hybrid early termination strategy combining partial-dimension and partial-bit lower-bound estimation.
3. Co-optimized data layout within and across memory ranks for early termination granularity.
4. 5.26x average speedup from NDP; additional 1.52x from hybrid early termination.

## Hardware Target
PIM (processing-in-memory)

## Technique Category
near-data-processing, memory-system, approximation

## Workloads
database, LLM-inference

## Metrics
- **Speedup:** 5.26x from NDP; additional 1.52x from hybrid early termination (~8x combined)

## Baselines
general-purpose computing platform (CPU/GPU for ANNS)

## Limitations
Not discussed; impact of early termination on retrieval recall quality is not quantified.

## Tags
anns, vector-database, near-data-processing, early-termination, memory-rank, approximate-search
