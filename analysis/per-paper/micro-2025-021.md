# LLMulator: Generalizable Cost Modeling for Dataflow Accelerators with Input-Adaptive Control Flow

**Venue:** MICRO 2025  
**Confidence:** High (analyzed from full text)

## Problem
Existing performance prediction methods for dataflow-based accelerators (TPUs, NPUs, FPGAs) fail to generalize across three dimensions: unseen applications with extreme loop scales (errors exceeding 40%), input-dependent control flows where branch probabilities shift at runtime (errors up to 60%), and diverse hardware architectures with varying memory delays and loop mapping primitives. No prior method unifies all three generalization axes.

## Motivation
Accurate, fast cost modeling is the critical bottleneck in design-space exploration for dataflow accelerators, and the growing complexity of hardware-software stacks (HLS, spatial mappings, dynamic operators) makes hand-crafted analytical models and static GNN approaches obsolete.

## Method
LLMulator uses a pre-trained LLM (LLaMA-3.2-1B fine-tuned via LoRA) as the backbone for cost prediction with three novel mechanisms:
1. **Progressive numeric encoding** that tokenizes each decimal digit independently and uses categorical cross-entropy rather than regression — predicts performance as digit-wise classification with beam-search error correction, enabling range-agnostic estimates.
2. **Dynamic Prediction Calibration (DPC)** using Direct Preference Optimization (DPO)-style reinforcement learning with a replay buffer, where the model is updated online from Verilator simulation feedback to adapt to runtime input variations in loop bounds and control-flow branching.
3. **Progressive dataset synthesizer** that generates multi-level training data via AST-based generation, dataflow-specific loop-tree mutation, and LLM-based augmentation, then formats data with intermediate RTL reasoning chains (via SiliconCompiler and OpenROAD) to teach the model compilation-level reasoning.

## Key Novelty
Treating hardware performance values as digit-wise categorical token sequences enables confidence-aware, range-agnostic prediction that generalizes to out-of-distribution loop scales, combined with RL-based online calibration for input-adaptive control flows.

## Contributions
- Progressive numeric modeling: digit-by-digit categorical output with beam search, reducing edge-value errors by 2.3%–13.6% over regression baselines.
- DPO-based Dynamic Prediction Calibration converges to 11.2% cycle error, a 9.7% improvement over static models.
- Progressive data augmentation covering AST generation, loop mutation, LLM self-augmentation, and hardware mapping parameters.
- 12.2% MAPE on ASIC design scenarios, 16.7%–7.8% error reduction vs GNNHLS and TLP.

## Hardware Target
ASIC, NPU, TPU

## Technique Categories
compiler, dataflow, scheduling

## Workloads
CNN, transformer, LLM-inference

## Metrics
- **Accuracy:** 12.2% MAPE on ASIC; 9.7% DPC improvement over static; 16.7%–7.8% vs GNNHLS/TLP

## Baselines
GNNHLS, TLP, Timeloop, Tenset/TVM MLP

## Limitations
Calibration via DPO requires repeated Verilator simulation runs per input, adding profiling overhead that may be prohibitive for very large dataflow programs; the method targets HLS-level modeling and does not address post-place-and-route variability.

## Tags
cost-modeling, llm-for-hardware, dataflow-accelerator, rl-calibration, design-space-exploration, numeric-tokenization
