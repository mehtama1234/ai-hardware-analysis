# dac-2025-278: Simulation-Based Autotuning via ML Prediction

**Problem:** Autotuning ML workloads requires target hardware; limited availability constrains parallel exploration.

**Motivation:** ML optimization spaces are exponentially large; simulation-based exploration accelerates autotuning.

**Method:** The framework runs different versions of code on simulated computers—programs that mimic exactly how real processors execute instructions—instead of needing actual hardware. Machine learning models trained on statistics from these simulations predict how each code version would perform on real hardware. When tested on simulators for x86, ARM, and RISC-V processors (three common processor types), the models consistently ranked the fastest implementation in the top 3% of all tested versions.

**Key Novelty:** ML-based performance prediction from simulator statistics to replace target hardware execution.

**Contributions:**
- Simulation-based autotuning framework
- Top-3% accuracy in identifying best implementations
- Outperforms native execution for embedded with 3+ parallel simulations

**Confidence:** low (abstract-only)

**Tags:** autotuning, simulation, performance-prediction, ml-workloads
