# SC-2025-417: MetoHash - Memory-Efficient Indexing for Hybrid PMem-DRAM

## Summary
MetoHash optimizes hashing indexes for hybrid persistent memory (PMem) and DRAM systems through staged insertion and traffic-aware fingerprinting, achieving 86-257% throughput improvement.

## Problem
Existing PMem-aware hashing indexes suffer from traffic amplification between memory tiers and inefficient memory utilization, negating the benefits of persistent memory's high capacity.

## Method
MetoHash spreads an index (data lookup table) across three memory levels: fast CPU caches, intermediate DRAM, and persistent memory (PMem) for permanent storage. It batches items together in the cache before moving them down to DRAM and eventually to PMem, reducing how many trips to slow storage are needed. It uses fingerprinting (a compact summary of each item) to avoid checking PMem unnecessarily, and removes duplicate copies when reorganizing how data is stored in buckets (storage containers).

## Key Results
- 86.1-257.6% throughput improvement over state-of-the-art PMem indexes
- Across various workloads
- Effective traffic reduction between memory tiers

## Hardware Target
- Intel Optane persistent memory
- Hybrid PMem-DRAM systems
- Multi-tiered memory hierarchies
- Enterprise data centers

## Technique Categories
- Data structure optimization
- Memory hierarchy optimization
- Storage systems design
- Index structures for persistent memory

## Workload Impact
- Key-value stores on persistent memory
- Database indexes leveraging PMem capacity
- In-memory analytics with persistence
- Fault-tolerant data structures

## Key Design Principles
- Staged writes reduce repeated PMem access
- Fingerprinting localizes lookups to faster tiers
- Batch processing reduces coherency traffic
- Layout optimization improves cache locality

## Limitations
- Analysis based on abstract only
- Specific workload characteristics not detailed
- Comparison details with competing approaches limited
- Scalability to very large working sets unclear

## System Impact
MetoHash represents important progress in PMem system design by demonstrating how careful staging and traffic awareness can unlock the performance potential of byte-addressable persistent memory, a critical technology for exascale systems.
