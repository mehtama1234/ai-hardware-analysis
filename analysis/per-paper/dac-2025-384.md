# NSFlow: An End-to-End FPGA Framework with Scalable Dataflow Architecture for Neuro-Symbolic AI

**Venue:** DAC
**ID:** dac-2025-384

## Problem
Neuro-Symbolic AI workloads exhibit heterogeneous kernels and unique memory access patterns unsuitable for general-purpose ML accelerators (CPU/GPU/TPU).

## Motivation
NSAI systems integrate neural networks and symbolic reasoning; their distinctive computation and memory patterns require specialized acceleration.

## Method
NSFlow features a design architecture generator identifying data dependencies and creating optimized dataflows, plus a reconfigurable FPGA array with flexible compute units, reorganizable memory, and mixed-precision capabilities.

## Key Novelty
Architecture generator synthesizing optimized FPGA dataflows from NSAI workload data dependencies, enabling end-to-end acceleration.

## Contributions
1. Design architecture generator identifying workload dependencies and creating optimized dataflow architectures
2. Reconfigurable FPGA array with flexible compute units, memory reorganization, and mixed-precision
3. 31x speedup over Jetson TX2, >2x over GPU, 8x over TPU-like systolic array, >3x over Xilinx DPU
4. Enhanced scalability: only 4x runtime increase when symbolic workloads scale by 150x

## Hardware Target
FPGA

## Technique Categories
dataflow, parallelism, compiler

## Workloads
LLM-inference

## Metrics
- speedup: 31x vs Jetson TX2, >2x vs GPU, 8x vs TPU, >3x vs Xilinx DPU

## Baselines
CPU (Jetson TX2); GPU; TPU-like systolic array; Xilinx DPU

## Limitations
FPGA resource utilization and scalability to very large NSAI models not thoroughly analyzed.

## Tags
neuro-symbolic, fpga, dataflow, acceleration, ai

## Primary Theme
FPGA dataflow framework for neuro-symbolic AI acceleration

## Confidence
low
