# asplos-2025-006: TempGraph: An Efficient Chain-driven Temporal Graph Computing Framework on the GPU

**Venue:** ASPLOS  
**Confidence:** low (abstract-only)

## Problem
Temporal graph path problems require vertices to be processed sequentially along time-dependent chains, creating inherently poor parallelism and slow convergence that prevents existing GPU solutions from exploiting massive thread-level parallelism and high memory bandwidth.

## Motivation
Time-sensitive applications depend on efficient temporal path computation, but the sequential temporal dependency between vertices in temporal graphs fundamentally conflicts with the GPU's SIMT execution model, leaving GPU resources severely underutilized.

## Method
TempGraph transforms the temporal graph into disjoint time-dependent chains that expose temporal dependencies explicitly, then employs a Generate-Activate-Compute (GAC) execution model to decouple temporal dependencies between chains by maintaining shortcut references. This allows multiple chains to be processed concurrently by massive GPU thread pools while shortcuts handle cross-chain dependency resolution.

## Key Novelty
The Generate-Activate-Compute model with inter-chain shortcut references breaks temporal dependencies across disjoint chains, enabling concurrent GPU execution of temporally-coupled graph workloads.

## Contributions
- Chain-driven temporal graph transformation that exposes temporal dependencies as disjoint processable units.
- Generate-Activate-Compute execution model with shortcut references for cross-chain dependency decoupling.
- 3.0-16.2x speedup over state-of-the-art GPU-based temporal graph solutions on an A100 GPU.
- 33.9-368.9x speedup over CPU-based TeGraph on a 128-core CPU machine.

## Hardware Target
GPU (A100)

## Technique Categories
dataflow, parallelism, scheduling

## Workloads
graph-analytics, GNN

## Metrics
Speedup: 3.0-16.2x over GPU baselines; 33.9-368.9x over TeGraph CPU baseline.

## Baselines
TeGraph (128-core CPU), state-of-the-art GPU-based temporal graph solutions

## Limitations
Not discussed; shortcut maintenance overhead and memory cost for large temporal graphs are not evaluated.

## Tags
temporal-graph, gpu, graph-analytics, parallelism, chain-driven

## Primary Theme
GPU temporal graph execution via chain decomposition and GAC model
