# Push Multicast: A Speculative and Coherent Interconnect for Mitigating Manycore CPU Communication Bottleneck
**Venue:** HPCA 2025 | **ID:** hpca-2025-001 | **Confidence:** low (abstract only)

## Problem
In manycore processors running parallel workloads, multiple cores repeatedly fetch the same shared cacheline at different times, consuming redundant LLC and NoC bandwidth through independent demand requests. The reactive cache hierarchy does not exploit temporal redundancy in same-data accesses across cores.

## Motivation
As CPU core counts scale, NoC bandwidth becomes the performance bottleneck because the reactive hierarchy wastes bandwidth serving identical data to many cores sequentially.

## Method
When a core requests data from the last-level cache (the shared memory closest to all cores), Push Multicast doesn't just send it to that one core—it also predicts which other cores will need the same data and sends it to them too, using multicast (one transmission to multiple destinations), saving bandwidth. To prevent wasted duplicate deliveries, routers have a coherent filter that recognizes when an incoming request matches data already being pushed and cancels that request. When the network gets congested, a pause/resume mechanism temporarily disables pushing to avoid interfering with regular data requests.

## Key Novelty
Instead of the last-level cache just responding to requests, it actively predicts which other cores will need the same data and sends it to them at the same time, while routers intelligently cancel any redundant requests for data that's already being delivered.

## Contributions
1. Speculative LLC-to-sharer push mechanism that multicasts data upon demand-request reply
2. Coherent in-network request filter in routers that prunes outstanding requests for cachelines already being pushed
3. Dynamic pause/resume control for pushing to avoid interference under varying traffic loads
4. 33%/43% NoC BW savings in 16/64-core systems; up to 1.56x/2.08x speedup

## Metrics
- **Speedup:** geomean 1.02x (max 1.56x) 16-core; geomean 1.11x (max 2.08x) 64-core
- **BW saving:** 33% avg (16-core), 43% avg (64-core)

## Baselines
L1 Bingo data prefetcher + L2 Stride prefetcher

## Hardware Target
CPU

## Technique Categories
interconnect, cache, prefetching, coherence

## Workloads
HPC (parallel programs)

## Limitations
Not discussed; coherence correctness with speculative push in the presence of write-invalidations may impose additional complexity.

## Tags
`noc` `multicast` `cache-coherence` `manycore` `bandwidth` `prefetching`
