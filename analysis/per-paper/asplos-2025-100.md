# asplos-2025-100: Vela: A Virtualized LLM Training System with GPU Direct RoCE

**Venue:** ASPLOS  
**Confidence:** low (abstract only)

## Problem
LLM training at scale typically requires dedicated bare-metal GPU clusters with specialized interconnects, but deploying training on cloud-native virtualized infrastructure using commodity hardware and standard Ethernet-based RDMA (RoCE) introduces performance challenges from virtualization overhead and suboptimal network stack interactions.

## Motivation
Making LLM training infrastructure accessible on commodity cloud hardware (RDMA over Converged Ethernet, KVM virtualization) broadens access and reduces cost, but achieving acceptable efficiency requires careful co-design of the virtualization and network layers.

## Method
Vela runs virtual machines (using KVM) where GPUs on different machines send data directly to each other (GPU Direct) without involving the host CPU. It uses SRIOV to give each virtual machine direct network access, and RoCE (a protocol that runs on standard Ethernet) for communication—avoiding specialized interconnects like InfiniBand. The team tested this on NVIDIA A100 GPUs in IBM Cloud with roughly 1500 machines, using model parallelism to train a 50-billion-parameter decoder-based language model. Insights from 2.5 years of production operation guided the design recommendations.

## Key Novelty
Vela shows that virtualized AI training with direct GPU communication and commodity Ethernet hardware achieves about 80% of bare-metal performance at 1500-GPU scale without specialized interconnects.

## Contributions
- A production-scale cloud-native LLM training system architecture using KVM virtualization, SRIOV NICs, and GPU Direct RoCE.
- ~80% of ideal throughput training a 50B-parameter model at ~1500 GPU scale; ~70% per-GPU FLOPS vs. single-VM HPL.
- 2.5 years of operational insights on designing, building, and operating virtualized AI infrastructure.
- Identification of bottlenecks and improvement opportunities for future virtualized AI systems.

## Hardware Target
GPU

## Technique Categories
interconnect, virtualization, parallelism

## Workloads
LLM-training

## Metrics
- Throughput: ~80% of ideal at 1500-GPU scale (50B param model)
- FLOPS: ~70% per-GPU vs. single-VM High-Performance Linpack

## Baselines
- Ideal (non-virtualized) throughput
- Single VM with High-Performance Linpack (HPL)

## Limitations
The 80% efficiency figure leaves a 20% gap whose sources are acknowledged but not fully resolved.

## Tags
llm-training, rdma, roce, virtualization, cloud, a100

## Primary Theme
Cloud-native LLM training on virtualized GPU infrastructure with GPU Direct RoCE
