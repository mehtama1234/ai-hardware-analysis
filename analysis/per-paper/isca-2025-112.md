# isca-2025-112 — GPUs All Grown-Up: Fully Device-Driven SpMV Using GPU Work Graphs

**Venue:** ISCA  
**Confidence:** low (abstract-only)

## Problem
Sparse matrix-vector multiplication (SpMV) on GPUs requires expensive host-side preprocessing to select per-row algorithms based on non-zero distribution, forcing costly CPU-GPU round-trips and limiting cache locality. This preprocessing adds latency that is particularly problematic for iterative solvers with repeated SpMV calls.

## Motivation
SpMV is a fundamental kernel in HPC and graph analytics, and its irregular sparsity patterns make static per-row algorithm selection at compile time impossible, yet existing GPU approaches pay high overhead for dynamic selection through host involvement.

## Method
The approach uses GPU Work Graphs—a feature in AMD GPUs that lets workgroups schedule and coordinate directly on the device—to fuse preprocessing with per-row sparse-matrix-vector multiplication. Instead of preprocessing on the host and then launching computation, both steps happen together on the GPU: as preprocessing generates metadata about which algorithm suits each row, specialized kernels automatically launch. This keeps more data in the GPU's fast L1 and L2 caches and eliminates expensive CPU-GPU communication. The GPU needs only a fixed 25 MiB of supporting data regardless of matrix size, unlike rocSPARSE LRB which requires more memory as matrices grow.

## Key Novelty
The key innovation is applying GPU Work Graphs—a graphics-world feature for on-device task scheduling—to fuse SpMV preprocessing and per-row computation on the GPU, eliminating host CPU coordination and improving cache locality.

## Contributions
- First application of GPU Work Graphs to sparse linear algebra (SpMV), enabling fully device-driven preprocessing and execution.
- Novel Work Graphs SpMV kernels that self-schedule per-row algorithms based on dynamically determined non-zero structure.
- Up to 7.19x speedup (mean 3.35x) over rocSPARSE LRB for single SpMV across 59 sparse matrices.
- 75% reduction in code complexity and a fixed 25 MiB memory footprint independent of matrix size.

## Metrics
- **Speedup:** up to 7.19x (mean 3.35x, SD 1.89) over rocSPARSE LRB for single SpMV; beats rocSPARSE CSR-Adaptive for up to 92 consecutive SpMV calls
- **Other:** 75% reduction in code complexity; fixed 25 MiB data structure vs. matrix-size-scaling rocSPARSE LRB structures

## Hardware Target
GPU

## Technique Category
scheduling, dataflow, memory-system

## Workloads
graph-analytics, HPC

## Baselines
- rocSPARSE LRB
- rocSPARSE CSR-General
- rocSPARSE CSR-Adaptive

## Limitations
Evaluation is limited to AMD GPUs supporting Work Graphs (rocSPARSE/ROCm ecosystem); portability to NVIDIA CUDA and generalization to SpMM or other sparse kernels is not demonstrated.

## Tags
spmv, gpu, work-graphs, self-scheduling, sparse-linear-algebra, hpc

## Primary Theme
device-driven GPU self-scheduling for sparse matrix-vector multiplication
