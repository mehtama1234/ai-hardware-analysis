# dac-2025-275: Fine-Tuning LLMs for Test Stimuli Generation

**Problem:** LLM-based test stimulus generation lacks domain-specific optimization; prompt engineering and feedback methods are insufficient.

**Motivation:** Automated test generation with high coverage and correctness reduces hardware verification time.

**Method:** The method teaches LLMs to generate hardware test stimuli (test inputs designed to find bugs in chips) using two stages. First, it builds a structured dataset formatted as tables, which teaches the LLM to produce consistent, predictable outputs. Second, it applies reinforcement learning—the LLM receives feedback rewarding tests that achieve good coverage (thoroughly exercise the hardware) and correctness—so the model learns from these rewards and improves over time.

**Key Novelty:** Domain-specific fine-tuning via table-shaped dataset generation + RL feedback for test generation.

**Contributions:**
- Table-shaped dataset generation strategy
- Two-stage fine-tuning with RL feedback
- Improved syntax correctness and coverage vs. commercial models

**Confidence:** low (abstract-only)

**Tags:** llm-finetuning, test-generation, reinforcement-learning, hardware-verification
