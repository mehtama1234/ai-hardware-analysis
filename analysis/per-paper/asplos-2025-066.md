# asplos-2025-066 — BQSim: GPU-accelerated Batch Quantum Circuit Simulation using Decision Diagram

**Venue:** ASPLOS  
**Confidence:** low (abstract-only)

## Problem
Existing quantum circuit simulators process one input state at a time, but applications such as quantum testing and verification require simulating a circuit under multiple input states (batch simulation). No GPU-accelerated batch quantum circuit simulator exists that exploits shared structure across inputs.

## Motivation
Testing and verification of quantum algorithms requires reasoning over many input scenarios simultaneously, and current CPU-based or single-input simulators are far too slow to support this at scale, blocking quantum software development workflows.

## Method
BQSim represents quantum gates using decision diagrams (compressed representations that identify repeated patterns), making it easy to spot identical operations across a batch of inputs. It converts this representation into a format that GPUs can process in parallel, then uses a task graph (which maps out how computations depend on each other) to group related operations into single GPU kernel launches instead of many small ones, reducing overhead. By also eliminating redundant calculations shared across the batch and overlapping data transfers with computation, it minimizes total work and keeps the GPU continuously busy.

## Key Novelty
It combines decision diagrams (which identify shared patterns across inputs) with GPU task-graph execution (which batches operations) to create the first GPU-accelerated batch quantum circuit simulator.

## Contributions
- BQSim system: first GPU-accelerated batch quantum circuit simulator.
- GPU-efficient data structure derived from decision diagrams to enable parallel batch simulation.
- Task-graph execution strategy minimizing repetitive kernel call overhead and overlapping computation with data transfer.
- 3.25x speedup over cuQuantum, 159x over Qiskit Aer, 311x over FlatDD on average.

## Hardware Target
GPU

## Technique Categories
parallelism, scheduling, dataflow

## Workloads
(quantum circuit simulation — no standard DL workload)

## Metrics
- Speedup: 3.25x over cuQuantum; 159.06x over Qiskit Aer; 311.42x over FlatDD (average)

## Baselines
- cuQuantum
- Qiskit Aer
- FlatDD

## Limitations
Decision diagram size may grow exponentially for certain quantum circuits, potentially limiting scalability to deep or highly entangled circuits.

## Tags
quantum-simulation, gpu, decision-diagram, batch-simulation, quantum-circuit

## Primary Theme
GPU-accelerated batch quantum circuit simulation via decision diagrams
