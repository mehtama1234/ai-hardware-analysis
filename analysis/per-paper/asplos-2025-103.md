# asplos-2025-103: Segue & ColorGuard: Optimizing SFI Performance and Scalability on Modern Architectures

**Venue:** ASPLOS  
**Confidence:** low (abstract only)

## Problem
Software-based fault isolation (SFI) for WebAssembly (Wasm) incurs significant performance overhead from memory-access instrumentation and limits the number of Wasm instances that can run concurrently in a single address space due to address-space partitioning constraints.

## Motivation
Wasm sandboxing is critical for safe, high-performance web and server-side execution, but its instrumentation overhead and scalability limits are barriers to broad deployment in latency-sensitive and high-concurrency applications.

## Method
Segue exploits x86-64 segment registers (FS/GS base) to encode sandbox memory bounds directly in hardware, eliminating explicit bounds-check instrumentation for most memory accesses — removing 44.7% of Wasm overhead on a SPEC CPU 2006 subset and reducing Firefox Wasm font-rendering overhead by 75%. ColorGuard leverages memory protection keys (MPK/memory tagging) to assign each Wasm instance a distinct color tag, enabling up to 15x more concurrent instances per address space without separate address spaces.

## Key Novelty
Using x86-64 segment registers as hardware-enforced sandbox bounds to eliminate SFI instrumentation overhead, combined with memory protection key coloring for 15x concurrency scaling.

## Contributions
- Segue: eliminates up to 44.7% of Wasm SFI overhead on SPEC CPU 2006; 75% overhead reduction in Firefox font rendering.
- ColorGuard: up to 15x more concurrent Wasm instances per address space via memory tagging.
- Integration and evaluation in three production Wasm toolchains: Wasm2c, WAMR, and Wasmtime.

## Hardware Target
CPU

## Technique Categories
security, virtualization, compiler

## Workloads
SPEC CPU 2006 (Wasm-compatible subset), Firefox font rendering

## Metrics
- Overhead reduction: 44.7% of Wasm SFI overhead eliminated on SPEC CPU 2006
- Latency: 75% overhead reduction for Firefox Wasm font rendering
- Scalability: up to 15x more concurrent Wasm instances

## Baselines
- Standard Wasm SFI without Segue/ColorGuard
- Firefox Wasm sandbox without Segue

## Limitations
Segue relies on x86-64 segmentation registers not available on ARM; portability to non-x86 architectures is limited.

## Tags
webassembly, sfi, sandboxing, x86-segmentation, memory-tagging, security

## Primary Theme
Hardware-assisted SFI performance and scalability for WebAssembly via segmentation and memory tagging
