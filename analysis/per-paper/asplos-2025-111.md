# asplos-2025-111: PICACHU: Plug-In CGRA Handling Upcoming Nonlinear Operations in LLMs

**Venue:** ASPLOS  
**Confidence:** low (abstract only)  
**Primary Theme:** CGRA plug-in accelerator for LLM nonlinear operations

## Problem
Nonlinear operations in LLMs (softmax, layer norm, GELU, RoPE) contribute significantly to inference latency and are poorly served by fixed-function accelerators designed primarily for matrix multiply/convolution, as they require user-defined precision and flexible dataflow to handle diverse operator shapes and precision requirements.

## Motivation
As LLMs become the dominant workload, the nonlinear operations that standard accelerators handle inefficiently create disproportionate latency bottlenecks, and no existing hardware provides both the efficiency and flexibility needed to handle this diverse, precision-sensitive set of operators.

## Method
PICACHU adds a flexible, programmable processor (called a Coarse-Grained Reconfigurable Array, or CGRA) designed to speed up the nonlinear operations in language models—like softmax, layer norm, and GELU—which don't work well with standard accelerators. The CGRA has small compute units arranged in a grid that can be rewired and adjusted for different data types for each operation. It connects to existing accelerators (like GPUs), handling only the nonlinear operations while they do the matrix math. Before running the model, a compiler tool assigns compute units, configures their connections, and sets precision for each operation.

## Key Novelty
PICACHU is a plug-in reconfigurable processor that adapts its internal wiring and precision for each nonlinear operation (softmax, layer norm, GELU) in language models—operations that fixed accelerators cannot handle efficiently.

## Contributions
- Profiling analysis identifying nonlinear operations as a significant LLM inference latency bottleneck not well addressed by existing accelerators.
- PICACHU CGRA architecture design with user-configurable precision PEs for LLM nonlinear operator diversity.
- Compiler mapping tool for scheduling nonlinear ops onto the CGRA dataflow grid.
- Integration strategy for plug-in deployment alongside existing LLM accelerators.

## Hardware Target
ASIC, FPGA

## Technique Category
dataflow, circuit-design, compiler

## Workloads
LLM-inference, transformer, attention

## Metrics
Latency reduction for nonlinear ops; specific numbers not in abstract.

## Baselines
- GPU
- Fixed-function LLM accelerators

## Limitations
The plug-in CGRA may introduce integration overhead with the host accelerator's memory hierarchy and control plane; specific area/power cost not available from abstract.

## Tags
cgra, llm-inference, nonlinear-ops, reconfigurable, accelerator, softmax
