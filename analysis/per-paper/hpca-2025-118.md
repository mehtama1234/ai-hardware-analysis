# hpca-2025-118: NearFetch: Saving Inter-Module Bandwidth in Many-Chip-Module GPUs

**Venue:** HPCA  
**Confidence:** low (abstract-only)

## Problem
In many-chip-module (MCM) GPUs, inter-module bandwidth is physically constrained by organic substrate or silicon interposer manufacturing limits, and fetching data from remote modules causes bandwidth contention that reduces effective achieved bandwidth, degrading GPU performance on high-sharing workloads.

## Motivation
As single-die GPU scaling approaches lithographic limits, multi-chip-module designs are necessary but introduce a new class of inter-module bandwidth bottleneck that is orthogonal to traditional memory latency—GPUs are uniquely bandwidth-sensitive due to their high thread-level parallelism.

## Method
NearFetch is a routing-based solution that exploits data locality across GPU modules on the routing path between a requesting module and remote memory. When a memory request traverses intermediate GPU modules, NearFetch checks if those intermediate modules already hold the requested data locally; if so, data is forwarded from the nearer intermediate module rather than from the distant memory partition. A topology-aware Miss Status Handling Register (MSHR) coalescing scheme records memory address information to support future forwarding decisions on cache misses. NearFetch introduces a new Inter-Module Bandwidth per Access (IBPA) metric to quantify bandwidth waste from multi-hop fetches.

## Key Novelty
Routing-path data forwarding that exploits data sharing among modules along the inter-module network path, reducing effective hop count for memory fetches without modifying the MCM topology.

## Contributions
- IBPA (Inter-Module Bandwidth per Access) metric for quantifying multi-hop bandwidth waste in MCM GPUs
- Data forwarding scheme enabling shorter-path data supply from intermediate routing modules
- Topology-aware MSHR coalescing for tracking forwarding candidates across cache misses
- 42.6% IBPA reduction and 52.2% average (up to 98.1%) performance improvement for high-sharing workloads

## Hardware Target
GPU, chiplet

## Technique Category
interconnect, memory-system, cache, prefetching

## Workloads
HPC, graph-analytics

## Metrics
- **Speedup:** 52.2% average performance improvement (up to 98.1%) for high-sharing workloads
- **Other:** 42.6% reduction in Inter-Module Bandwidth per Access (IBPA)

## Baselines
baseline MCM GPU without NearFetch (standard routing)

## Limitations
Benefits are concentrated on high-sharing workloads; low-sharing workloads show minimal improvement, limiting general applicability.

## Tags
mcm-gpu, chiplet, inter-module-bandwidth, data-forwarding, noc, bandwidth-efficiency
