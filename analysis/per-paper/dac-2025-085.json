{
  "id": "dac-2025-085",
  "title": "VersaSlot: Efficient Fine-grained FPGA Sharing with Big.Little Slots and Live Migration in FPGA Cluster",
  "venue": "DAC",
  "problem": "Current FPGA sharing via dynamic partial reconfiguration (DPR) suffers from partial reconfiguration contention and task execution blocking that degrade application performance in shared FPGA environments.",
  "motivation": "FPGAs are increasingly deployed for on-demand acceleration in data centers, but fine-grained multiplexing via DPR introduces contention bottlenecks; efficient cluster-level sharing with low blocking is critical.",
  "method": "VersaSlot divides each FPGA into variable-sized slots—some large, some small—so different-length tasks can run at the same time instead of queuing while the FPGA reconfigures (reprograms itself). It uses smart allocation to match each task to the right-size slot and live migration (moving a running task between FPGAs) to spread workload across a cluster. On Xilinx UltraScale+ FPGAs, this eliminates the performance bottleneck where reconfiguration delays become the limiting factor, the primary source of slowdown in shared FPGA environments.",
  "key_novelty": "The core idea is the Big.Little slot architecture—mixing different-sized slots on each FPGA with live migration across the cluster—to prevent the task queuing caused by reconfiguration bottlenecks.",
  "contributions": [
    "Big.Little heterogeneous slot architecture resolving partial reconfiguration contention and task blocking",
    "Efficient slot allocation and scheduling algorithm optimizing resource utilization",
    "Cross-board live migration enabling FPGA cluster-wide multiplexing achieving 13.66x lower response time vs temporal FPGA multiplexing"
  ],
  "hardware_target": [
    "FPGA"
  ],
  "technique_category": [
    "virtualization",
    "scheduling"
  ],
  "workloads": [
    "datacenter",
    "accelerator-sharing"
  ],
  "metrics": {
    "speedup": "13.66x lower response time vs temporal; 2.19x vs SOTA spatio-temporal",
    "area": "35% LUT, 29% FF resource utilization improvement"
  },
  "baselines": [
    "temporal FPGA multiplexing",
    "state-of-the-art spatio-temporal sharing"
  ],
  "limitations": "Power consumption and reconfiguration bandwidth not detailed.",
  "tags": [
    "fpga-virtualization",
    "dynamic-reconfiguration",
    "live-migration",
    "cluster-scheduling"
  ],
  "primary_theme": "Big.Little heterogeneous FPGA sharing with live migration",
  "confidence": "low"
}