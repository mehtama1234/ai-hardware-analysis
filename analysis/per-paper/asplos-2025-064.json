{
  "id": "asplos-2025-064",
  "title": "Accelerating LLM Serving for Multi-turn Dialogues with Efficient Resource Management",
  "venue": "ASPLOS",
  "problem": "Current LLM serving frameworks recompute attention keys and values for all prior turns on each new query in a multi-turn dialogue, incurring substantial redundant compute overhead. Additionally, the long accumulated prompt lengths from multi-turn context cause head-of-line blocking under FCFS scheduling, underutilizing GPU resources.",
  "motivation": "Multi-turn chat interactions are the dominant LLM serving pattern, but serving frameworks optimized for single-request throughput suffer disproportionate latency and GPU underutilization as conversation length grows.",
  "method": "The system keeps computed intermediate results (KV-cache—saved calculations from neural network attention) from prior conversation turns, eliminating wasteful recalculation when a new message arrives. Instead of processing requests in strict arrival order (FCFS), a smarter scheduler reorders or interrupts long requests to prevent them from blocking shorter ones and wasting GPU capacity. Memory allocation dynamically balances how much of these old results to keep in storage versus when to discard and recompute them.",
  "key_novelty": "The core innovation is combining KV-cache retention across conversation turns (reusing prior computed attention results) with a head-of-line blocking-aware scheduler (one that deprioritizes long requests so short ones don't wait) specifically designed for multi-turn dialogue serving.",
  "contributions": [
    "Characterization of multi-turn dialogue as a distinct LLM serving workload with unique KV-cache and scheduling challenges.",
    "Cross-turn KV-cache management to eliminate redundant attention recomputation across dialogue turns.",
    "Scheduling policy that mitigates head-of-line blocking for long accumulated prompts in multi-turn settings.",
    "GPU resource utilization improvements demonstrated on multi-turn chat benchmarks."
  ],
  "hardware_target": [
    "GPU"
  ],
  "technique_category": [
    "scheduling",
    "memory-system",
    "cache",
    "kernel-fusion"
  ],
  "workloads": [
    "LLM-inference",
    "attention"
  ],
  "metrics": {
    "speedup": null,
    "energy_or_tops_w": null,
    "area": null,
    "ppa": null,
    "accuracy": null,
    "other": "GPU utilization improvement and latency reduction for multi-turn dialogues (details in full paper)"
  },
  "baselines": [
    "vLLM",
    "Orca",
    "PagedAttention-based serving frameworks"
  ],
  "limitations": "KV-cache retention across turns increases memory pressure; the tradeoff with batch size and concurrent session count is not quantified in the abstract.",
  "tags": [
    "llm-serving",
    "kv-cache",
    "multi-turn-dialogue",
    "scheduling",
    "head-of-line-blocking"
  ],
  "primary_theme": "multi-turn KV-cache management and scheduling for LLM serving",
  "confidence": "low"
}