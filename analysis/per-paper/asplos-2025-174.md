# asplos-2025-174: DeepContext: A Context-aware, Cross-platform, and Cross-framework Tool for Performance Profiling and Analysis of Deep Learning Workloads

**Venue:** ASPLOS  
**Confidence:** low (abstract-only)

## Problem
Existing DL profiling tools fail to bridge the semantic gap between high-level Python/framework operations and low-level GPU execution, making it difficult to identify root-cause performance bottlenecks across the full software stack. No existing tool provides correlated context-aware profiling spanning Python, DL frameworks, C/C++ libraries, and GPU kernels simultaneously.

## Motivation
Performance optimization of DL models requires holistic visibility across heterogeneous software and hardware layers, but current profilers provide only siloed views that leave critical cross-stack bottlenecks hidden.

## Method
DeepContext uses a framework-agnostic shim layer that intercepts calls between the DL framework and underlying C/C++ libraries, correlating them with GPU hardware performance metrics and Python-level program context. This shim enables causal linking of high-level operator invocations to their low-level GPU execution without modifying framework source code. An automated performance analyzer processes the correlated trace to generate actionable optimization guidance.

## Key Novelty
Framework-agnostic shim layer that automatically correlates program context across all layers of the DL software stack (Python, framework, C/C++, GPU) to expose cross-layer performance bottlenecks.

## Contributions
- Framework-agnostic shim layer enabling cross-stack correlation of DL program context with hardware performance metrics
- Automated performance analyzer providing actionable optimization guidance from holistic stack-wide profiling data
- Cross-platform and cross-framework support with low overhead on mainstream DL frameworks and CPU+GPU architectures
- Discovery and fixing of previously hidden bottlenecks achieving 1.06x to 1.66x speedups on real-world DL applications

## Hardware Target
GPU, CPU

## Technique Category
compiler, memory-system, kernel-fusion

## Workloads
LLM-inference, LLM-training, CNN, transformer

## Metrics
- Speedup: 1.06x to 1.66x on real-world DL applications after guided optimization
- Low profiling overhead (not quantified)

## Baselines
Existing profiling tools (unspecified)

## Limitations
Not discussed; profiling overhead magnitude and specific framework/platform coverage are not quantified in the abstract.

## Tags
profiling, cross-stack, performance-analysis, deep-learning, bottleneck-detection

## Primary Theme
Cross-stack DL performance profiling with context correlation
