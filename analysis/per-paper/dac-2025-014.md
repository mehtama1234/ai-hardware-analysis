# Dynamic Local Usage: An Accurate Model for Usage of Tile-Internal Wiring in Global Routing

**Problem:** Static pre-estimated local wiring models fail to account for exact wire geometry and pin-shape interactions, causing detailed routing failures.

**Method:** The DLU model calculates how much space wires need inside routing tiles by analyzing their exact shapes and positions, instead of using a fixed guess. It adds tip-to-tip penalty extensions—extra costs when wires come close together—to accurately predict how tightly wires can pack. Routing algorithms then use these real congestion measurements to choose better paths for wires. The model also handles incremental updates, such as when connection points move.

**Key Contribution:** Exact-geometry-based model replaces static estimates, improving pre-detailed-routing predictions.

**Results:** Fewer vias, design-rule violations, and timing issues after detailed routing on 3nm/5nm nodes vs industrial router.

**Tags:** routing, global-routing, physical-design, local-routing, 3nm, 5nm
