{
  "id": "dac-2025-330",
  "title": "VEDA: Efficient LLM Generation Through Voting-based KV Cache Eviction and Dataflow-flexible Accelerator",
  "venue": "DAC",
  "problem": "LLM inference on edge devices faces memory and computational bottlenecks due to intensive KV cache requirements and variable sequence lengths, limiting deployment feasibility.",
  "motivation": "Edge deployment demands efficient inference; KV cache memory pressure and varying sequence dimensions in generation phase create hardware complexity and latency.",
  "method": "The system uses three coordinated optimizations to reduce memory and hardware needs. First, it uses a voting mechanism to identify which cached key-value data (information the model stores during text generation) is useful, discarding unimportant data while preserving accuracy. Second, the hardware includes a reconfigurable array of processing elements that adjusts at runtime to efficiently handle multiplication operations of varying sizes, and processes certain mathematical operations (softmax and layernorm) sequentially. This coordination reduces hardware complexity from O(N) to O(1).",
  "key_novelty": "It pairs adaptive voting to selectively discard cached key-value data (information stored during text generation) with hardware that reconfigures to handle the resulting variable-sized operations.",
  "contributions": [
    "Voting-based KV cache eviction algorithm for adaptive importance filtering",
    "Flexible-product dataflow with runtime-reconfigurable PE supporting variable dimensions",
    "Element-serial scheduling for softmax/layernorm with O(1) hardware complexity",
    "VEDA accelerator achieving superior latency/throughput on edge devices"
  ],
  "hardware_target": [
    "ASIC",
    "NPU"
  ],
  "technique_category": [
    "memory-system",
    "dataflow",
    "quantization"
  ],
  "workloads": [
    "LLM-inference"
  ],
  "metrics": {
    "latency": "reduced",
    "hardware_complexity": "O(1) vs O(N)",
    "energy": "improved"
  },
  "baselines": [
    "Existing edge LLM platforms",
    "GPU/CPU inference"
  ],
  "limitations": "Specific quantitative comparisons and device baselines not detailed in abstract; applicability to larger models unclear.",
  "tags": [
    "llm-inference",
    "kv-cache",
    "edge-deployment",
    "dataflow",
    "efficient-inference"
  ],
  "primary_theme": "Adaptive KV cache and flexible dataflow for edge LLM inference",
  "confidence": "low"
}