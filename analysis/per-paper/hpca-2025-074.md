# Architecting Value Prediction around In-Order Execution

**Venue:** HPCA  
**Confidence:** low (abstract-only)

## Problem
In-order processors suffer from stalls when long-latency instructions block younger independent instructions from issuing, and the compiler cannot always find sufficient independent work to fill the pipeline. Conventional branch prediction techniques fail to resolve hard-to-predict (delinquent) branches quickly enough to prevent pipeline bubbles.

## Motivation
In-order pipelines offer area/power advantages over out-of-order designs, but their performance falls short because the simplicity that makes them efficient also makes them vulnerable to cache misses and branch mispredictions—value prediction can bridge this gap without requiring full OOO machinery.

## Method
The system adds two types of value prediction (guessing the result of a computation before it finishes) to an in-order processor pipeline built on a CVA6 RISC-V core. Because instructions execute in strict order, the processor can aggressively guess values without needing complex recovery logic—if a guess is wrong, the real result simply overwrites it when the computation finishes. The cache system is redesigned with a fast first-level cache that can accept these speculative guesses. The core uses a 32-entry scoreboard to track instruction dependencies and issues 4 instructions per cycle, all without traditional register renaming or out-of-order memory hazard detection.

## Key Novelty
In-order processors can do aggressive value guessing without performance penalties (because wrong guesses get overwritten), matching the speed of complex out-of-order designs without their expensive register renaming or memory hazard detection.

## Contributions
1. First study of both address-based and direct value prediction integrated into an in-order pipeline
2. Analysis showing that in-order execution properties enable speculation without recovery penalty
3. Reworked speculative-write fast L1 cache hierarchy for in-order processors
4. 38.4% geomean IPC improvement over baseline in-order CVA6, reaching 86.7%/46.3% of the gains from comparable 32/16-entry and 64/32-entry ROB OOO processors

## Hardware Target
RISC-V, CPU

## Technique Category
prefetching, cache, scheduling

## Workloads
HPC

## Metrics
- **Speedup:** 38.4% geomean IPC improvement; 86.7% of OOO (32/16 ROB) gains; 46.3% of OOO (64/32 ROB) gains

## Baselines
- Baseline 4-issue in-order CVA6 (32-entry scoreboard)
- OOO with 32/16-entry ROB+scheduler
- OOO with 64/32-entry ROB+scheduler

## Limitations
Not discussed; evaluation scope limited to specific benchmark sets on a CVA6-derived model without silicon area or power breakdown.

## Tags
value-prediction, in-order-execution, risc-v, speculation, cva6

## Primary Theme
value prediction for in-order processor performance
