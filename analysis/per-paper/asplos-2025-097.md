# asplos-2025-097: Helix: Serving Large Language Models over Heterogeneous GPUs and Network via Max-Flow

**Venue:** ASPLOS  
**Confidence:** low (abstract only)

## Problem
Serving LLMs across heterogeneous GPU clusters (differing GPU types, memory capacities, and interconnect speeds) is inefficient because existing systems cannot jointly optimize model placement and request scheduling to account for both GPU and network heterogeneity simultaneously.

## Motivation
Real-world ML infrastructure increasingly mixes GPU generations and interconnect bandwidths; existing homogeneity-assuming serving systems leave significant throughput and latency on the table.

## Method
Helix models LLM inference over a heterogeneous cluster as a max-flow problem on a directed, weighted graph where nodes are GPU instances and edge capacities encode GPU compute throughput and network bandwidth. A Mixed Integer Linear Programming (MILP) solver operates on this graph to jointly discover optimized model placement (which layers/shards go where) and request scheduling (how tokens are routed), treating both tasks as a single unified optimization.

## Key Novelty
Formulating heterogeneous LLM serving as a max-flow problem that jointly optimizes model placement and request scheduling in a single MILP, exposing trade-offs invisible to decoupled approaches.

## Contributions
- A max-flow graph formulation capturing both GPU and network heterogeneity via directed weighted edges.
- A MILP algorithm that jointly solves model placement and request scheduling on heterogeneous clusters.
- Up to 3.3x throughput improvement and up to 66%/24% reduction in prompting/decoding latency over existing approaches on 24-42 GPU heterogeneous clusters.
- Open-source Helix system.

## Hardware Target
GPU

## Technique Categories
scheduling, parallelism

## Workloads
LLM-inference

## Metrics
- Throughput: up to 3.3x improvement
- Prompting latency: up to 66% reduction
- Decoding latency: up to 24% reduction

## Baselines
Existing heterogeneous LLM serving approaches (unnamed in abstract).

## Limitations
MILP solving time and scalability beyond 42-GPU clusters are not discussed.

## Tags
llm-serving, heterogeneous-cluster, max-flow, milp, model-placement, scheduling

## Primary Theme
Max-flow-based joint model placement and scheduling for heterogeneous LLM serving
