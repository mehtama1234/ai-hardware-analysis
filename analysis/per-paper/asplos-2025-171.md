# asplos-2025-171: Tally: Non-Intrusive Performance Isolation for Concurrent Deep Learning Workloads

**Venue:** ASPLOS  
**Confidence:** low (abstract-only)

## Problem
GPU sharing for concurrent DL workloads suffers from high interference, causing up to 188.9% latency overhead on high-priority inference tasks. Existing mechanisms impose high integration costs, inadequate isolation, and limited compatibility.

## Motivation
GPU underutilization in production DL clusters causes prolonged job queues and increased costs, making efficient multi-tenant sharing critical.

## Method
Tally shares a GPU among multiple programs by intercepting the work they send to it. Instead of running all of one program's work then all of another's, Tally dynamically schedules individual thread blocks (small units of parallel computation, much smaller than a full kernel) from high-priority inference and lower-priority training tasks, controlling how much the programs interfere with each other. All of this works without requiring any changes to the programs or frameworks themselves.

## Key Novelty
Thread-block-level scheduling isolates concurrent programs' performance on a shared GPU without modifying the applications or frameworks.

## Contributions
- Non-intrusive GPU sharing requiring no application or framework modification
- Fine-grained thread-block-level scheduling to mitigate co-execution interference
- Only 7.2% P99 latency overhead on inference vs. 188.9% for TGS
- >80% throughput retention relative to TGS

## Hardware Target
GPU

## Technique Category
scheduling, virtualization, parallelism

## Workloads
LLM-inference, LLM-training, CNN

## Metrics
- P99 latency overhead: 7.2% (Tally) vs. 188.9% (TGS)
- Throughput: >80% of TGS

## Baselines
TGS (state-of-the-art GPU sharing system)

## Limitations
Not discussed; throughput is still below exclusive execution.

## Tags
gpu-sharing, performance-isolation, thread-block-scheduling, multi-tenancy, inference-training-colocation

## Primary Theme
GPU multi-tenant scheduling with performance isolation
