# micro-2025-092: FALA: Locality-Aware PIM-Host Cooperation for Graph Processing with Fine-Grained Column Access

**Venue:** MICRO 2025  
**Authors:** Changmin Shin, Jaeyong Song, Seongmin Na, Jun Sung, Hongsun Jang, Jinho Lee  
**Confidence:** low (abstract only)

## Problem
Graph processing has irregular memory access patterns causing memory bandwidth waste: cache-based graph tiling suffers from overfetching (DRAM burst size >> useful data per access), while PIM-only approaches fail to exploit cache locality for reused vertex data.

## Motivation
Graph processing underlies social networks, recommendation systems, and analytics; neither pure cache-based nor pure PIM approaches solve the dual challenge of sparse access overfetching and cache underutilization for reused data.

## Method
FALA is a hybrid PIM-cache architecture with two key mechanisms: (1) fine-grained column access within DRAM banks enabling selective access to individual vertex data without loading full DRAM rows; (2) a locality-aware PIM-host cooperation policy routing high-reuse vertex accesses through host CPU caches and low-reuse irregular accesses through PIM. The design jointly eliminates overfetching and maximizes both internal DRAM bandwidth and on-chip cache utilization.

## Key Novelty
Fine-grained DRAM column access within memory banks enabling PIM to access individual graph vertices without full-row overfetching, combined with a locality-aware routing policy between PIM and host cache hierarchy.

## Contributions
1. Fine-grained column access mechanism within DRAM banks for sparse graph vertex access
2. Locality-aware PIM-host cooperation policy for reuse-based routing
3. 1.56x geometric mean speedup and 35.3% energy reduction vs. prior art
4. Hybrid architecture overcoming limitations of both pure cache-based and pure PIM graph accelerators

## Hardware Target
PIM (processing-in-memory), CPU

## Techniques
near-data-processing, memory-system, cache

## Workloads
graph-analytics, recommendation systems

## Key Metrics
- Speedup: 1.56x geometric mean vs. prior art
- Energy: 35.3% reduction vs. prior art

## Baselines
State-of-the-art cache-based, PIM-based, and hybrid graph processing accelerators

## Limitations
Not discussed (abstract-only analysis).

## Tags
graph-processing, pim, near-data-processing, dram, cache-locality, fine-grained-access
