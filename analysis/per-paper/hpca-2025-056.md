# Adyna: Accelerating Dynamic Neural Networks with Adaptive Scheduling

**Venue:** HPCA  
**Confidence:** low (abstract-only)

## Problem
Dynamic neural networks (DynNNs) conditionally skip computation based on input difficulty, but current GPUs and specialized accelerators lack dataflow and architecture support to realize theoretical efficiency gains because dynamic execution patterns defeat static resource allocation.

## Motivation
DynNNs offer a promising way to reduce average inference cost by matching computation to per-sample difficulty, but without hardware-aware scheduling these savings are not realized on real silicon.

## Method
Adyna uses a unified representation to model diverse DynNN variants and introduces a dynamism-aware multi-kernel selection paradigm. At runtime, a dataflow scheduler observes the distribution of dynamic size values and selects among multiple precompiled kernels, each optimized for a specific dynamic-size bucket. An effective kernel sampling algorithm chooses the minimal set of kernels to precompile and cache on the accelerator. The hardware supports multi-tile and multi-tenant execution for concurrent dynamic workloads.

## Key Novelty
A dynamism-aware multi-kernel selection paradigm that precompiles and caches a small set of specialized kernels covering the distribution of dynamic computation sizes, enabling optimal hardware utilization across varying input-dependent workload sizes.

## Contributions
1. Unified representation capturing diverse DynNN architectures for general hardware design
2. Dynamism-aware dataflow scheduler that selects kernels based on runtime dynamic-size distributions
3. Kernel sampling algorithm to choose optimal precompiled kernel sets for on-chip storage
4. 1.70x/1.57x average speedup over multi-tile/multi-tenant accelerators (up to 2.32x/2.01x)

## Hardware Target
ASIC, GPU

## Technique Categories
scheduling, dataflow, compiler

## Workloads
transformer, CNN, LLM-inference

## Metrics
- Speedup: 1.70x avg over multi-tile accelerator (up to 2.32x)
- Speedup: 1.57x avg over multi-tenant accelerator (up to 2.01x)

## Baselines
- State-of-the-art multi-tile accelerator
- State-of-the-art multi-tenant accelerator

## Limitations
Precompiling multiple kernels increases compilation time and on-chip memory pressure; the sampling algorithm's effectiveness depends on the dynamic-size distribution being predictable.

## Tags
`dynamic-neural-networks` `adaptive-scheduling` `multi-kernel` `early-exit` `conditional-computation`
