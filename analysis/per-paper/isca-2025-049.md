# AQB8: Energy-Efficient Ray Tracing Accelerator through Multi-Level Quantization

**ID:** isca-2025-049
**Venue:** ISCA
**Confidence:** low

## Problem
Ray tracing BVH traversal is bottlenecked by DRAM bandwidth consumed by 32-bit floating-point bounding boxes, and prior 8-bit compression schemes still require decompression back to FP32 for intersection tests, retaining the arithmetic cost of complex FP32 operations.

## Motivation
Real-time high-fidelity ray tracing requires massive ray-object intersection tests whose memory traffic and arithmetic cost scale with scene complexity, making energy and bandwidth efficiency critical for GPU RT accelerators.

## Method
AQB8 shrinks bounding boxes (the 3D bounds that guide ray-tracing searches) from 32-bit floating-point to 8-bit integers at every level of the scene hierarchy tree. Unlike prior compression schemes that expand these integers back to floating-point for ray intersection tests, AQB8 performs all tests directly in 8-bit integer arithmetic. The hardware replaces expensive floating-point math circuits with simpler, lower-power integer units, eliminating both memory bandwidth waste and arithmetic cost. To avoid incorrectly discarding rays due to rounding errors, the design propagates and adjusts quantization bounds across hierarchy levels to prevent false negatives.

## Key Novelty
AQB8's core idea is using 8-bit quantization at every level of the BVH hierarchy tree so ray intersection tests run entirely in integer arithmetic, eliminating the expensive decompression to floating-point that prior work required.

## Contributions
1. Multi-level quantization technique for constructing quantized BVH trees that preserves intersection correctness across hierarchy levels.
2. RT accelerator hardware (AQB8) that operates entirely on 8-bit integers during BVH traversal, eliminating FP32 arithmetic units.
3. 70% reduction in DRAM accesses and 49% energy reduction over modern GPU RT accelerators across diverse scenes.
4. 27% hardware area reduction and 1.82x performance speedup versus GPU RT baselines.

## Hardware Target
GPU, ASIC

## Technique Category
quantization, circuit-design, memory-system

## Workloads
vision

## Metrics
- speedup: 1.82x over modern GPU RT accelerators
- energy: 49% reduction in energy consumption
- area: 27% hardware area reduction
- other: 70% reduction in DRAM accesses

## Baselines
- modern GPU RT accelerators (FP32 BVH traversal)

## Limitations
Not discussed; accuracy impact of quantization on edge-case scenes not quantified.

## Tags
ray-tracing, BVH, quantization, integer-arithmetic, graphics, energy-efficiency

## Primary Theme
Integer-native quantized BVH traversal for energy-efficient ray tracing
