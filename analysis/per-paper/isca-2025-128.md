# SwitchQNet: Optimizing Distributed Quantum Computing for Quantum Data Centers with Switch Networks

**Venue:** ISCA  
**Confidence:** low (abstract-only)

## Problem
In quantum data centers (QDCs), quantum processor units (QPUs) in different racks are interconnected via reconfigurable optical switch networks, and cross-rack quantum communications incur both long latency and dynamic switch reconfiguration delays, significantly increasing total circuit execution time and degrading quantum state fidelity.

## Motivation
Near-term distributed quantum computing requires scaling beyond single QPU sizes, but the latency overhead of inter-rack optical switching is a critical bottleneck that compounds decoherence and reduces the effective circuit depth achievable with fault-tolerant quantum error correction.

## Method
SwitchQNet is a compiler that jointly optimizes quantum computation at two levels: the program layer (how quantum gates and qubits are arranged) and the network layer (when optical switches connecting different quantum processor racks reconfigure). It models the network topology and switch reconfiguration delays, then schedules quantum operations to batch and reorder communications so that one switch reconfiguration spreads its setup cost across multiple operations. The compiler integrates with quantum error correction for fault-tolerant quantum computing and is open-sourced at https://zenodo.org/records/15377656.

## Key Novelty
The core idea is optimizing quantum gate scheduling and optical switch reconfiguration as one problem instead of separately, achieving 8.02x speedup in quantum data centers by sharing the cost of each switch reconfiguration across multiple operations.

## Contributions
- Cross-layer compiler co-optimizing quantum program communication scheduling and optical switch network reconfiguration scheduling in QDCs.
- 8.02x reduction in overall distributed quantum circuit latency compared to prior approaches with small compilation overhead.
- Integration with quantum error correction (QEC) to support fault-tolerant quantum computing in switch-networked QDCs.
- Open-source release of the compiler implementation.

## Hardware Target
ASIC (quantum processor context; not a conventional ASIC)

## Technique Categories
compiler, scheduling, interconnect

## Workloads
HPC (quantum circuits)

## Metrics
- Latency: 8.02x reduction over prior approaches

## Baselines
- Prior distributed quantum computing communication scheduling approaches

## Limitations
Evaluation is simulation-based without real QDC hardware; practical fidelity improvements under realistic noise models and switch imperfections are not characterized.

## Tags
quantum-computing, distributed-quantum, optical-switch, compiler, quantum-data-center, fault-tolerant
