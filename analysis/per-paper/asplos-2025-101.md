# asplos-2025-101: Data Cache for Intermittent Computing Systems with Non-Volatile Main Memory

**Venue:** ASPLOS  
**Confidence:** low (abstract only)

## Problem
Energy-harvesting embedded systems with non-volatile main memory (NVM) must checkpoint frequently to cover Write After Read (WAR) data hazards, incurring both high runtime overhead and excessive NVM writes; existing data cache solutions do not integrate WAR detection and require additional hardware or software memory trackers.

## Motivation
Intermittent computing on energy-harvesting platforms is gaining importance for batteryless IoT sensors and edge devices, but NVM's slow access and high write energy make naive caching approaches inefficient without WAR-aware management.

## Method
NACHO integrates WAR dependency detection directly into the data cache controller, eliminating the need for a separate memory tracker. When a cached dirty line is read after being written by NVM (a WAR hazard), the cache itself identifies and handles the dependency. This reduces checkpoint frequency and NVM writes by buffering dirty data in the volatile cache and writing to NVM only at checkpoint time or on eviction.

## Key Novelty
Embedding WAR dependency detection logic directly into the data cache, removing the external memory tracker required by prior intermittent computing systems.

## Contributions
- NACHO: a data cache architecture with integrated WAR detection for intermittent NVM-based platforms.
- 54% average runtime overhead reduction vs. state-of-the-art cache-based systems.
- 82% reduction in NVM writes vs. cache-less system; 18% average reduction vs. prior cache-based systems.

## Hardware Target
CPU

## Technique Categories
cache, memory-system, reliability

## Workloads
Embedded/IoT intermittent computing workloads (not named).

## Metrics
- Runtime: 54% average overhead reduction vs. state-of-the-art cache-based intermittent computing
- NVM writes: 82% reduction vs. cache-less; 18% average reduction vs. prior cache-based

## Baselines
- Cache-less intermittent system
- State-of-the-art cache-based intermittent computing systems

## Limitations
Not discussed.

## Tags
intermittent-computing, nvm, cache, energy-harvesting, war-detection, checkpoint

## Primary Theme
WAR-integrated data cache for intermittent NVM-based embedded systems
