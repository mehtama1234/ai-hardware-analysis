# asplos-2025-110: Pruner: A Draft-then-Verify Exploration Mechanism to Accelerate Tensor Program Tuning

**Venue:** ASPLOS  
**Confidence:** low (abstract only)  
**Primary Theme:** draft-then-verify two-stage tensor program search

## Problem
Search-based tensor program tuning for deep neural network deployment is slow because the exploration mechanism applies an accurate-but-slow learned cost model to all candidate schedules, taking hours or days to find optimal programs. Additionally, learned cost models trained on one hardware platform cannot adapt online to a new platform, causing performance degradation in cross-platform deployment scenarios.

## Motivation
Efficient deployment of deep neural networks on diverse hardware requires fast tensor program search; current learned cost model-driven approaches are a computational bottleneck that limits the practical speed of autotuning frameworks like TVM.

## Method
Pruner uses a two-stage 'Draft-then-Verify' process to find better tensor program schedules (how computations are arranged on hardware) faster. In the first 'Draft' stage, a fast Symbol-based Analyzer (a tool using mathematical rules) rapidly filters thousands of schedule candidates down to the most promising ones. In the 'Verify' stage, only these finalists are checked by the expensive learned cost model—an AI system trained to predict program performance. An extended version called MoA-Pruner adds online adaptation using momentum-based updates (iterative adjustments building on previous changes) to gradually adjust the learned model to new hardware during tuning, eliminating the need for complete retraining.

## Key Novelty
Draft-then-Verify uses a fast symbolic analysis filter to eliminate unpromising candidates, so the expensive learned cost model only needs to evaluate the most promising options, drastically reducing the number of expensive evaluations.

## Contributions
- Identification and analysis of cross-platform online unawareness as a failure mode of learned cost models in tensor program tuning.
- Pruner's Draft-then-Verify mechanism using a Symbol-based Analyzer to pre-screen candidates before invoking the learned cost model.
- MoA-Pruner's Momentum online Adaptation strategy for cross-platform cost model adaptation without offline retraining.
- Empirical demonstration of significant speedup in schedule search compared to standard TVM-style cost model-only exploration.

## Hardware Target
GPU, CPU, ASIC

## Technique Category
compiler, scheduling

## Workloads
CNN, transformer, LLM-inference

## Metrics
Search time reduction not quantified in abstract; hours-to-days improvement claimed.

## Baselines
- TVM (Ansor)
- Standard learned cost model exploration

## Limitations
The Symbol-based Analyzer may miss promising candidates filtered out before the learned model, and adaptation accuracy for very different architectures is not characterized from the abstract.

## Tags
tensor-program-tuning, autotuning, cost-model, schedule-search, cross-platform, tvm
