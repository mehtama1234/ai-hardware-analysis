# asplos-2025-054 — PAPI: Exploiting Dynamic Parallelism in Large Language Model Decoding with a Processing-In-Memory-Enabled Computing System

**Venue:** ASPLOS  
**Confidence:** low (abstract-only)

## Problem
LLM decoding kernels dynamically shift between memory-bound and compute-bound behavior as request parameters (batch size, sequence length, sampling strategy) change at runtime, making static assignment of kernels to either PIM units or GPU-like compute accelerators suboptimal. Furthermore, existing PIM units are designed with a one-size-fits-all approach that fails to exploit the heterogeneity within memory-bound LLM kernels.

## Motivation
LLM inference is increasingly deployed at scale and throughput/latency are bottlenecked by the need to move billions of model parameters across the memory bus every decoding step, yet the dynamic workload character invalidates static hardware partitioning strategies.

## Method
PAPI combines two types of processors: PIM units (processors built directly into memory chips to reduce how much data moves around) and traditional compute accelerators (like GPUs), plus a runtime system that watches what the LLM is doing and automatically sends each piece of work to whichever processor is best suited for it at that moment. The PIM units are designed specifically to handle different kinds of memory-heavy operations—like multiplying matrices by single vectors, computing attention (where the model weighs which past tokens matter most), and applying gates (switches that control information flow)—because these operations behave differently. A scheduler watches as parameters change during execution (like batch size or sequence length) and steers work to either the PIM or compute units based on what would be fastest right now.

## Key Novelty
The system automatically reassigns work between memory-embedded and traditional processors based on real-time workload changes, using specialized memory-embedded processor designs that efficiently handle different types of memory-intensive operations.

## Contributions
- Characterization of dynamic compute/memory-bound transitions in LLM decoding kernels under varying runtime parameters.
- Heterogeneity-aware PIM unit architecture tailored to the diversity within memory-bound LLM kernels.
- Dynamic scheduling framework (PAPI) that remaps kernels to PIM or compute accelerators at runtime.
- Demonstration that dynamic mapping outperforms static heterogeneous mapping for LLM decoding throughput.

## Hardware Target
PIM (processing-in-memory), GPU

## Technique Categories
near-data-processing, scheduling, parallelism, memory-system

## Workloads
LLM-inference, attention

## Metrics
- Quantitative results not stated in abstract.

## Baselines
- Static PIM+GPU heterogeneous mapping systems
- GPU-only LLM serving

## Limitations
Quantitative speedup and energy results are not reported in the abstract.

## Tags
pim, llm-decoding, dynamic-scheduling, heterogeneous-architecture, memory-bound

## Primary Theme
dynamic PIM-GPU scheduling for LLM decoding
