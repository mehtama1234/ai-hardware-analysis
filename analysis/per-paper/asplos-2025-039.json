{
  "id": "asplos-2025-039",
  "title": "Hopps: Leveraging Sparsity to Accelerate Automata Processing",
  "venue": "ASPLOS",
  "problem": "Automata processing (AP) workloads execute many finite state machines (FSMs) in parallel over streaming symbol data, but existing AP accelerators suffer poor utilization because most FSMs are inactive (in a non-matching state) for any given input symbol. The irregular, state-dependent activation pattern makes it hard to exploit spatial parallelism efficiently.",
  "motivation": "Pattern matching against large rule sets (e.g., network intrusion detection, genomics scanning) demands high-throughput AP, but the inherent sparsity of active FSM transitions wastes memory bandwidth and compute cycles in existing DRAM- and SRAM-based AP engines.",
  "method": "Hopps exploits the observation that only a sparse subset of FSMs transition on each input symbol. It uses a sparsity-aware representation that tracks only active states across all FSMs and a compressed activation bitmap to skip idle FSMs entirely during each symbol step. The architecture couples this with a tiled SRAM storage layout so that state-transition tables for active FSMs are fetched in bursts, reducing off-chip memory traffic proportional to the active-FSM fraction.",
  "key_novelty": "Hopps is the first AP accelerator to exploit cross-FSM input-driven sparsity at runtime by compressing the active-state set into a compact bitmap and skipping computation for inactive automata entirely.",
  "contributions": [
    "Characterization of sparsity in AP workloads showing that the fraction of active FSMs per symbol step is typically low.",
    "A sparsity-aware hardware architecture (Hopps) that uses activation bitmaps to skip inactive FSMs and reduce memory traffic.",
    "Evaluation demonstrating significant speedup and energy reduction over prior AP accelerators on data analytics and scientific computing workloads."
  ],
  "hardware_target": ["ASIC"],
  "technique_category": ["sparsity", "memory-system", "near-data-processing"],
  "workloads": ["graph-analytics", "database"],
  "metrics": {
    "speedup": "not stated quantitatively in abstract",
    "energy_or_tops_w": null,
    "area": null,
    "ppa": null,
    "accuracy": null,
    "other": null
  },
  "baselines": [],
  "limitations": "Not discussed in the abstract; full quantitative results unavailable without full text.",
  "tags": ["automata-processing", "sparsity", "finite-state-machine", "pattern-matching", "accelerator"],
  "primary_theme": "sparsity-driven automata processing acceleration",
  "confidence": "low"
}
