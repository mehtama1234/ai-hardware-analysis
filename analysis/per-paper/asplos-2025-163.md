# KernelGPT: Enhanced Kernel Fuzzing via Large Language Models

**Venue:** ASPLOS  
**Confidence:** low (abstract-only)

## Problem
Kernel fuzzing effectiveness depends on syscall specifications defining syscall syntax and semantics, but generating these specifications is largely manual work, leaving many important syscalls uncovered and limiting automated kernel vulnerability discovery.

## Motivation
OS kernel bugs affect billions of devices globally, and the primary bottleneck for automated kernel fuzzing (e.g., via Syzkaller) is incomplete syscall specification coverage.

## Method
KernelGPT feeds kernel source code and documentation to a large language model, asking it to write specifications for system calls (the interfaces that let programs request operations from the kernel) in the format Syzkaller—a fuzzing tool—expects. When fuzzing runs and finds problems, the tool sends those results back to the LLM to fix incomplete or incorrect specifications, refining them through repeated iterations. This approach automatically covers system calls previously left out of fuzzing and ensures the generated specifications correctly describe how system calls actually behave.

## Key Novelty
Using a language model to automatically write and repair system-call specifications through iterative feedback from fuzzing, covering system calls previously left out of existing specifications.

## Contributions
- KernelGPT: LLM-based automatic generation of syscall specifications for kernel fuzzing
- Iterative specification repair loop using fuzzing execution feedback as an LLM oracle
- Expanded syscall coverage vs. existing manually written Syzkaller specifications
- Discovery of new kernel bugs enabled by increased specification coverage

## Hardware Target
CPU

## Techniques
security

## Metrics
- Other: increased syscall specification coverage; new kernel bugs discovered

## Baselines
Syzkaller with manually written specifications, prior automated specification generation

## Limitations
LLM-generated specifications may contain semantic errors not caught by syntactic validation; quality depends on LLM training coverage of kernel internals.

## Tags
kernel-fuzzing, llm, syscall, security, specification-generation
