# DPUaudit: DPU-assisted Pull-based Architecture for Near-Zero Cost System Auditing
**Venue:** HPCA 2025 | **ID:** hpca-2025-008 | **Confidence:** low (abstract only)

## Problem
Existing hardware-based system auditing frameworks use push-based architectures that require a log sender on the monitored host, necessitating heavy software protection mechanisms that impose >10% runtime overhead, defeating the efficiency goal.

## Motivation
Data center security requires low-overhead system auditing for intrusion detection; push-based designs impose unacceptable performance costs on the monitored host.

## Method
DPUaudit replaces the on-host log sender with a DPU (SmartNIC) that actively pulls system events from the monitored host's memory over the network interconnect. Removing the log sender from the host eliminates its heavy software protection overhead. The DPU handles log collection, processing, and integrity verification in an isolated environment physically separated from the host.

## Key Novelty
Inverting auditing data flow from host-pushed to DPU-pulled, eliminating the log sender and its protection overhead, achieving 2.1% average runtime overhead versus one order-of-magnitude higher in prior work.

## Contributions
1. Root-cause analysis: push-based log sender is the overhead bottleneck in hardware auditing
2. DPU-assisted pull-based architecture removing log sender from monitored host
3. Average 2.1% runtime overhead for 6 mainstream data center applications (vs >10x higher in prior work)
4. Maintained audit log integrity guarantees

## Metrics
- **Overhead:** Average 2.1% runtime slowdown (at least 10x less than existing approaches)

## Baselines
Existing hardware-based push-based auditing frameworks

## Hardware Target
DPU, SmartNIC

## Technique Categories
security, near-data-processing, virtualization

## Workloads
Database / data center application workloads

## Limitations
Not discussed; pull-based DPU access may be constrained by interconnect bandwidth under high event rates.

## Tags
`security` `auditing` `dpu` `smartnic` `data-center` `pull-based`
