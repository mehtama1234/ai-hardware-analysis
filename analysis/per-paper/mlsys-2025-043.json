{
  "id": "mlsys-2025-043",
  "title": "HyC-LoRA: Memory Efficient LoRA Fine-tuning with Hybrid Activation Compression",
  "venue": "MLSYS",
  "problem": "LoRA fine-tuning dramatically reduces trainable weight count but the buffered activations needed for backpropagation still dominate memory—69.7% of total in Llama-2-7B—and non-linear operator activations (RMSNorm, RoPE) are the hardest to compress due to outliers.",
  "motivation": "Existing activation compression methods (quantization, sparsity, recomputation) focus on linear layers; non-linear activations resist uniform quantization due to channel-wise outliers. Achieving near-2-bit average quantization across all operators requires operator-specific hybrid strategies.",
  "method": "HyC-LoRA compresses activations using two coordinated strategies. Within individual operations, it identifies data channels with extreme outliers and keeps those in full precision (FP16) while compressing the rest to 2-bit (INT2); for attention queries/keys, it compresses before the rotation step (RoPE) where values are smoother. Across operations, it reorders quantization and LoRA adapter application so the activation-times-adapter product (XA) stays in memory while the main branch is compressed separately, enabling backward passes to regenerate results instead of storing them; it also fuses quantization with adjacent operations using Triton kernels to reduce processing overhead.",
  "key_novelty": "It combines outlier preservation within operations (keeping extreme values at full precision, compressing others to 2-bit) with LoRA-aware compression reordering that enables backward passes to regenerate data instead of storing it.",
  "contributions": [
    "Identification that non-linear buffered activations are 69.7% of LoRA training memory in Llama-2-7B",
    "Intra-operator hybrid compression: structured outlier extraction for RMSNorm + pre-RoPE quantization for Q/K",
    "Inter-operator LoRA Reorder Computing enabling selective recomputation of YAB during backward",
    "Triton kernel fusion integrating quantization with adjacent operators",
    "Up to 3.97x end-to-end memory reduction and 8x activation memory reduction vs baseline LoRA",
    "Negligible accuracy degradation across Llama-2-7B, Llama-3-8B, and other models"
  ],
  "hardware_target": "GPU (NVIDIA; Triton-compatible); targets on-device fine-tuning scenarios",
  "technique_category": "training-efficiency",
  "workloads": [
    "LoRA fine-tuning of LLMs (Llama-2-7B, Llama-3-8B)",
    "Downstream NLP tasks (commonsense reasoning, instruction following)"
  ],
  "metrics": [
    "End-to-end memory reduction (x)",
    "Activation memory reduction (x)",
    "Accuracy on downstream tasks",
    "Training throughput"
  ],
  "baselines": [
    "QLoRA",
    "SparseBP",
    "BackRazor",
    "LoRA-FA",
    "QST"
  ],
  "limitations": [
    "Triton kernels add engineering complexity and may not generalize to all GPU architectures without retuning",
    "Pre-RoPE quantization assumes RoPE is applied after Q/K projection; architectures that differ may need separate handling",
    "Recomputation in inter-operator tier adds compute overhead which may reduce training speed"
  ],
  "tags": [
    "lora",
    "fine-tuning",
    "activation-compression",
    "quantization",
    "memory-efficiency",
    "kernel-fusion",
    "llm",
    "triton"
  ],
  "primary_theme": "Training Efficiency & Memory",
  "confidence": "high"
}