# IntelliGen: Instruction-Level Auto-tuning for Tensor Program with Monotonic Memory Optimization

**Venue:** CGO
**Confidence:** low

## Problem
Tensor compilers must optimize memory performance (throughput, latency) for DNN code generation, but existing auto-tuning either uses coarse-grained descriptions (missing opportunities) or explores vast search spaces (impractical). Fine-grained auto-tuning has been infeasible.

## Motivation
Memory performance is the bottleneck in tensor compilation; enabling fine-grained auto-tuning without exploding search space is critical for next-generation compilers.

## Method
IntelliGen performs instruction-level graph description and monotonic memory optimization. Leverages monotonic property of memory optimization to reduce search space efficiently. Auto-tuning explores instruction-level parameters on NVIDIA/AMD/Cambricon hardware.

## Key Novelty
Monotonic memory optimization enabling efficient fine-grained auto-tuning by reducing search space from exponential to tractable.

## Contributions
- Instruction-level graph IR for tensor programs
- Monotonic optimization method for memory performance
- Up to 3.13x, 3.55x, 16.9x speedups on NVIDIA, AMD, Cambricon respectively

## Hardware Targets
GPU, ASIC

## Technique Categories
compiler, scheduling

## Workloads
CNN, transformer, attention

## Metrics
{'speedup': '1.46-3.13x NVIDIA, 1.85-3.55x AMD, 2.30-16.9x Cambricon'}

## Baselines
NVIDIA CUTLASS, TVM, existing tensor frameworks

## Limitations
Evaluation limited to three hardware targets; generalization to other accelerators not discussed.

## Tags
tensor-compilation, auto-tuning, memory-optimization

## Primary Theme
monotonic memory optimization for tensor compiler auto-tuning
