# asplos-2025-172: Be CIM or Be Memory: A Dual-mode-aware DNN Compiler for CIM Accelerators

**Venue:** ASPLOS  
**Confidence:** low (abstract-only)

## Problem
CIM accelerators can switch dynamically between compute and memory modes, but existing DNN compilers ignore this capability and statically assign resources, leaving performance untapped especially for LLMs with variable per-layer memory/compute demands.

## Motivation
Emerging LLMs have highly variable memory and compute requirements across layers; exploiting CIM mode-switching is crucial to fully utilize CIM accelerators for real-world DNNs.

## Method
CMSwitch introduces a hardware abstraction attribute exposing the compute-memory mode switch to the compiler optimization space. A compilation pass uses dynamic programming and mixed-integer programming (MIP) to find optimal network segment boundaries and mode-resource allocations. A tailored meta-operator expresses results in a hardware-agnostic generalized form.

## Key Novelty
First CIM compiler to co-optimize compute-mode and memory-mode resource allocation using DP and MIP, adapting to each DNN layer's resource demands.

## Contributions
- Novel hardware abstraction attribute exposing CIM mode-switching to compiler optimization
- Compilation pass combining dynamic programming and MIP for optimal mode/resource allocation
- Tailored meta-operator for generalized hardware-agnostic CIM compilation output
- 1.31x average speedup over SOTA CIM compilers across diverse DNN workloads including LLMs

## Hardware Target
CIM (compute-in-memory)

## Technique Category
compiler, dataflow, memory-system, scheduling

## Workloads
LLM-inference, CNN, transformer

## Metrics
- Speedup: 1.31x average over SOTA CIM compilation methods

## Baselines
State-of-the-art CIM compilation methods (unnamed in abstract)

## Limitations
Not discussed; specific baseline compilers are not named in the abstract.

## Tags
cim, compiler, mode-switching, dnn-acceleration, mixed-integer-programming

## Primary Theme
CIM compiler with adaptive compute-memory mode switching
