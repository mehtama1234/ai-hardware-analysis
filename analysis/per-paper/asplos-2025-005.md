# asplos-2025-005: EXIST: Enabling Extremely Efficient Intra-Service Tracing Observability in Datacenters

**Venue:** ASPLOS  
**Confidence:** low (abstract-only)

## Problem
Intra-service application-level tracing imposes 5-10% average runtime overhead (up to 18% worst case) on traced applications, making it impractical for continuous deployment in shared, stressed datacenters where sub-percent-level overhead is required.

## Motivation
Modern cloud applications require both inter-service and intra-service traces to precisely reason about event causality and diagnose performance anomalies; reducing intra-service overhead to the per-mille level is essential for practical datacenter observability.

## Method
EXIST reduces tracing overhead by redesigning how the system collects trace data. It adds minimal measurement points that avoid expensive operations like context-switching (switching between different execution contexts) or waiting for locks. It collects and stores the trace data asynchronously (in the background, not during the app's main execution) so tracing doesn't slow down the actual work. Finally, it compresses trace data intelligently—combining or removing redundant information while preserving the cause-and-effect chain of events.

## Key Novelty
EXIST redesigns the tracing pipeline end-to-end—from measurement to storage—to cut overhead to roughly one-tenth of a percent (about 10-50x lower than existing systems) while preserving the cause-and-effect relationships between events.

## Contributions
- Characterization showing that 5-10% average (18% worst-case) overhead is dominated by synchronization and data copy costs.
- EXIST's lightweight instrumentation and asynchronous off-path collection architecture reducing overhead to per-mille level.
- Evaluation in production-like datacenter environments demonstrating practical observability with negligible overhead.

## Hardware Target
CPU

## Technique Categories
scheduling, parallelism

## Workloads
database

## Metrics
Overhead reduced from 5-10% average (up to 18% worst case) to per-mille level.

## Baselines
Not specified in abstract.

## Limitations
Not discussed; per-mille overhead target and any accuracy trade-offs in event compression are not quantified.

## Tags
distributed-tracing, observability, intra-service, overhead-reduction, datacenter

## Primary Theme
Ultra-low-overhead intra-service tracing for datacenters
