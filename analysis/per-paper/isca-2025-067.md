# isca-2025-067 — HYTE: Flexible Tiling for Sparse Accelerators via Hybrid Static-Dynamic Approaches

**Venue:** ISCA  
**Confidence:** low (abstract only)

## Problem
Tiling very large sparse tensors on hardware accelerators is challenged by the inability of purely static schemes (simple heuristics) and purely dynamic schemes (high design complexity) to jointly find near-optimal tile size/shape, iteration order, buffer allocation, and metadata management under highly variable local sparsity patterns.

## Motivation
Sparse tensor operations dominate many ML and scientific workloads; as tensor sizes exceed on-chip buffer capacity, tiling becomes mandatory, but high sparsity variability makes a single static tiling scheme leave significant performance on the table, while fully dynamic schemes add unacceptable hardware complexity.

## Method
HYTE is a hybrid static-dynamic tiling framework. An offline static scheduler samples the sparse matrix to estimate a near-optimal initial tiling configuration (tile size/shape, dimension iteration order, buffer allocation policies) without full tensor analysis. At runtime, a dynamic tuner adjusts tile shape on-the-fly to maintain high buffer utilization under variable local sparsity. HYTE also efficiently manages tiling metadata in both off-chip memory and on-chip buffer.

## Key Novelty
HYTE's combination of lightweight offline sampling-based static scheduling with runtime tile-shape dynamic tuning is the first to jointly optimize all tiling dimensions while actively adapting to local sparsity variation.

## Contributions
1. A static offline scheduler using lightweight sampling to identify near-optimal tiling configurations.
2. A dynamic runtime tile-shape tuner that maintains high buffer utilization under variable local sparsity.
3. Efficient on-chip and off-chip metadata management for sparse accelerator tiling.
4. 3.3x to 6.2x geomean speedup over state-of-the-art sparse tiling strategies.

## Hardware Target
ASIC

## Technique Category
sparsity, dataflow, scheduling, compiler

## Workloads
GNN, transformer, CNN

## Metrics
- **Speedup:** 3.3x to 6.2x geomean over state-of-the-art sparse tiling

## Baselines
state-of-the-art static and dynamic sparse tiling strategies

## Limitations
Not discussed; evaluation limited to sparse matrices and may not generalize to higher-order sparse tensors.

## Tags
sparse-accelerator, tiling, hybrid-scheduling, sparsity, dataflow, buffer-management
