{
  "id": "dac-2025-359",
  "title": "SSDTrain: An Activation Offloading Framework to SSDs for Faster Large Language Model Training",
  "venue": "DAC",
  "problem": "GPU memory capacity growth lags LLM size growth; activations (intermediate tensors from forward pass) dominate GPU memory use during training, limiting micro-batch size and increasing training overhead through expensive weight updates.",
  "motivation": "Training large LLMs requires offloading activations to reduce GPU memory pressure while maintaining training throughput through efficient I/O overlap.",
  "method": "SSDTrain moves intermediate calculation results (activations) from the GPU to ultra-fast solid-state storage (NVMe SSDs), freeing up GPU memory. It overlaps data transfer with GPU computation—while the GPU computes, storage reads and writes happen simultaneously, eliminating idle waiting. The system also deduplicates stored data (keeping one copy instead of many identical blocks) and optimizes the forward calculation pass, integrating with PyTorch, Megatron, and DeepSpeed.",
  "key_novelty": "SSDTrain's key innovation is perfectly overlapping storage transfers with GPU computation, allowing temporary calculation results (activations) to be stored on fast SSDs rather than GPU memory, enabling larger batches without sacrificing training speed.",
  "contributions": [
    "Adaptive activation offloading framework to NVMe SSDs",
    "I/O-computation overlap techniques eliminating offloading overhead",
    "Tensor deduplication and forwarding optimizations",
    "Framework integration with PyTorch, Megatron, DeepSpeed",
    "47% reduction in activation peak memory with negligible throughput loss"
  ],
  "hardware_target": [
    "GPU",
    "SoC"
  ],
  "technique_category": [
    "memory-system",
    "dataflow"
  ],
  "workloads": [
    "LLM-training"
  ],
  "metrics": {
    "memory_reduction": "47% activation peak memory",
    "throughput_loss": "negligible",
    "io_overhead": "perfectly overlapped"
  },
  "baselines": [
    "on-GPU activation storage",
    "layerwise full recomputation"
  ],
  "limitations": "Not discussed; focus is on memory savings and throughput preservation.",
  "tags": [
    "llm-training",
    "activation-offloading",
    "nvme",
    "ssd",
    "memory-efficiency",
    "gpu"
  ],
  "primary_theme": "Activation offloading to SSD for efficient LLM training",
  "confidence": "low"
}