# Concerto: Automatic Communication Optimization and Scheduling for Large-Scale Deep Learning

**ID:** asplos-2025-028  
**Venue:** ASPLOS  
**Confidence:** low (abstract-only)  
**Primary Theme:** Automatic computation-communication overlap scheduling for distributed DL training

## Problem
In large-scale distributed DNN training, collective communication (all-reduce, all-gather, etc.) remains a major bottleneck that existing frameworks address only through hand-crafted, framework-specific overlap strategies tightly coupled to particular parallelism schemes. There is no general, automatic mechanism to overlap computation and communication across arbitrary parallelism configurations.

## Motivation
As DL models grow and distributed training scales to thousands of accelerators, communication overhead increasingly dominates training time, yet existing solutions sacrifice programmability and generality by embedding communication optimizations inside specific parallel runtimes.

## Method
Concerto is a compiler that automatically schedules communication in distributed deep learning without being tied to any specific framework or way of splitting work across machines. It uses a general algorithm to solve the scheduling problem—like organizing a project with limited computational resources and time—automatically breaking up the large synchronous communication operations (moments when all machines pause to exchange data) into smaller pieces that can run alongside the actual GPU computations. The result is a schedule that works with any training setup, requiring no manual tuning.

## Key Novelty
Concerto automatically breaks up synchronous communication operations to enable computation overlap using a general scheduling algorithm that adapts to any training configuration without requiring manual tuning.

## Contributions
- A general compiler framework (Concerto) that decouples communication scheduling from parallel execution strategy.
- Formulation of DL communication scheduling as RCPSP and use of a solver for near-optimal overlap schedules.
- Auto-decomposition technique that transforms critical synchronous collectives to enable overlapping with computation.
- Empirical demonstration matching or outperforming Megatron-LM, JAX/XLA, DeepSpeed, and Alpa without hand-crafted optimizations.

## Hardware Target
GPU

## Technique Categories
compiler, scheduling, parallelism, interconnect

## Workloads
LLM-training, transformer

## Metrics
- **Speedup:** matches or outperforms state-of-the-art frameworks including Megatron-LM, JAX/XLA, DeepSpeed, Alpa (specific numbers not in abstract)

## Baselines
- Megatron-LM
- JAX/XLA
- DeepSpeed
- Alpa

## Limitations
Not discussed; solver-based scheduling may have scalability limits for very large graphs or many parallel dimensions.

## Tags
distributed-training, communication-overlap, compiler, scheduling, parallelism, collective-communication
