# HardHarvest: Hardware-Supported Core Harvesting for Microservices

**Venue:** ISCA 2025
**Confidence:** low (abstract-only)

## Problem
Software core harvesting—batch 'Harvest VMs' temporarily stealing idle cores from latency-critical 'Primary VMs'—incurs prohibitive overheads (hypervisor calls, TLB/cache flushes, context switches) that are tolerable for monolithic applications but unacceptable for sub-millisecond microservices.

## Motivation
Cloud servers running microservices suffer chronically low core utilization because VMs are sized for peak load; software harvesting is too slow to exploit the short idle windows between microservice requests.

## Method
HardHarvest implements core harvesting in hardware. In-hardware request scheduling logic detects idle Primary VM cores and dispatches Harvest VM tasks directly, bypassing hypervisor calls. TLBs and caches are partitioned between Primary and Harvest VMs via a smart replacement algorithm that preserves Primary VM working sets, eliminating full TLB/cache flushes during core transfers. The hardware scheduler monitors Primary VM request queues and preempts Harvest VM work proactively before Primary VM tail latency is violated.

## Key Novelty
First hardware architecture for VM core harvesting in microservice environments, replacing software overheads with in-hardware request scheduling and TLB/cache partitioning.

## Contributions
1. First hardware architecture for core harvesting targeting sub-millisecond microservice SLOs
2. In-hardware request scheduling that transfers idle Primary VM cores to Harvest VM tasks without hypervisor intervention
3. TLB and cache partitioning with smart replacement policy to protect Primary VM state
4. 1.5x core utilization, 1.8x Harvest VM throughput, 6.0x Primary VM tail latency reduction vs. software core harvesting

## Hardware Target
CPU, SoC

## Technique Category
scheduling, virtualization, cache, parallelism

## Workloads
database (microservice workloads)

## Key Metrics
- Utilization: 1.5x core utilization vs. software harvesting
- Speedup: 1.8x Harvest VM throughput
- Latency: 6.0x Primary VM tail latency reduction

## Baselines
State-of-the-art software core harvesting for microservices

## Limitations
Simulation-based evaluation; overhead for diverse VM configuration mixes and workload ratios not explored.

## Tags
core-harvesting, microservices, virtualization, scheduling, tlb, cloud
