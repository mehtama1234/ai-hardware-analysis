# isscc-2025-210: Miniature Biomedical Implant Secured by Two-Factor Authentication with Emergency Access

## Quick Summary
Addresses the critical challenge of securing implantable medical devices (IMDs) that handle life-critical therapies and sensitive data while enabling emergency responders to access devices without server-stored credentials.

## Problem
IMDs require Internet connectivity for remote patient and doctor access, but two major challenges emerge:
1. In emergencies, responders lack server-stored pre-shared keys (PSKs), preventing device programming
2. If a doctor's account is compromised (e.g., phishing), attackers can hijack the implant

## Method
The system protects the implanted device using two separate authentication steps: a doctor enters their password and then provides a second verification (such as a code from their phone). During emergencies—when the authentication server is unreachable, a doctor's account is compromised, or no one knows the account password—authorized responders can use a special key stored directly on the device itself to access it immediately, without needing to reach the central server or retrieve the backup security keys stored there.

## Key Novelty
Emergency responders can access the implant even when the central authentication server is offline or breached, because the emergency authorization key is stored on the device itself rather than only on the server.

## Contributions
- Two-factor authentication for IMDs
- Emergency override without credential requirements
- Protection against compromised doctor accounts
- Emergency accessibility without server dependency

## Hardware Target
ASIC, SoC

## Technique
Security

## Confidence
Low (abstract-only)
