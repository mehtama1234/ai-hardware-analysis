# Lincoln: Real-Time 50~100B LLM Inference on Consumer Devices with LPDDR-Interfaced, Compute-Enabled Flash Memory

**Venue:** HPCA  
**Confidence:** low (abstract-only)

## Problem
Consumer devices cannot run large 50-100B LLM models in real time because: (1) Flash storage has insufficient internal read bandwidth for per-iteration weight loading, and (2) the conventional Flash interface (NVMe/UFS) offers far lower transmission bandwidth to the NPU than DRAM interfaces, making both the prefill and decode phases severely bandwidth-limited.

## Motivation
Privacy and cost concerns drive demand for on-device LLM inference, but current approaches are limited to small, lower-quality models because the weight-loading bottleneck from Flash memory makes large-model inference far too slow for real-time use.

## Method
Lincoln is a device-architecture co-design with two complementary solutions for the two LLM phases. For the compute-intensive prefill phase, Flash devices use the existing high-speed LPDDR interface (normally used for DRAM) to transmit weights to the NPU at much higher bandwidth than conventional Flash interfaces, maintaining cost/area efficiency. For the memory-intensive decode phase, hybrid-bonding-based near-Flash computing performs matrix-vector operations directly on Flash dies to exploit the full internal Flash bandwidth, coupled with speculative decoding to further reduce latency. Flash internal bandwidth is increased via enhanced array shrinking and more parallel Flash planes per die using 3D hybrid bonding.

## Key Novelty
Phase-specific dual strategy: using the LPDDR high-speed interface for prefill-phase weight streaming from Flash, and hybrid-bonding near-Flash compute for decode-phase GEMV execution directly on Flash dies.

## Contributions
1. Device-level Flash bandwidth improvement via enhanced array shrinking and 3D hybrid bonding for more parallel Flash planes
2. LPDDR-interfaced Flash for prefill phase: repurposing the high-speed DRAM interface for Flash weight transmission to NPU
3. Near-Flash computing via hybrid bonding for decode phase: GEMV execution on Flash dies to eliminate transmission bottleneck
4. Integration with speculative decoding; up to 13.23x prefill and 254.1x generation speedup over conventional SSD-based systems

## Hardware Target
NPU, SoC

## Technique Category
near-data-processing, memory-system, dataflow

## Workloads
LLM-inference

## Metrics
- **Speedup:** Up to 13.23x prefill speedup, 254.1x generation speedup over conventional SSD-based systems

## Baselines
- Conventional SSD-based on-device LLM inference system

## Limitations
The hybrid-bonding near-Flash compute introduces significant Flash die complexity and may affect Flash endurance or cost; these tradeoffs are not fully characterized.

## Tags
on-device-llm, flash-memory, near-flash-computing, lpddr-interface, speculative-decoding, edge-inference

## Primary Theme
LPDDR-interfaced compute-enabled Flash for on-device LLM inference
