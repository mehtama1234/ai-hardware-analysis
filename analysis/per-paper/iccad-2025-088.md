# LLM-on-the-Palm: Mobile LLM Inference with PIM-Enhanced NAND Flash Memory

**Venue:** ICCAD  
**ID:** iccad-2025-088

## Problem
LLM inference on mobile devices is severely limited by the inability to fit billion-parameter models in DRAM, forcing reliance on slow NAND flash memory, resulting in poor user experience and high latency.

## Motivation
Enabling on-device LLM inference is crucial for privacy, security, and user experience, but requires solving the memory access bottleneck when parameters must be stored in slower flash memory.

## Method
LLM-on-the-Palm proposes PIM-enhanced NAND flash memory with minimal hardware modifications: integrating multiply-accumulate (MAC) units directly in flash memory planes (one MAC per plane for 256 GB SSD means 256 total MACs). Processing moves to where data resides, eliminating excessive data movement. The system enables fast inference of billion-parameter LLMs without compromising NAND cell density.

## Key Novelty
In-memory computing directly in NAND flash storage with minimal MAC overhead per plane, enabling on-device LLM inference with minimal hardware modifications.

## Contributions
- PIM-enhanced NAND flash architecture with one MAC unit per plane maintaining cell density
- Innovative data movement reduction through compute-in-storage design
- Demonstrates approximately 100 ms per token generation on 6.7B model with iPhone-15-class specifications
- Hardware modification minimal and pragmatic for smartphone deployment

## Hardware Targets
- SoC
- PIM

## Technique Categories
- near-data-processing
- memory-system

## Workloads
- LLM-inference

## Metrics
| Metric | Value |
|--------|-------|
| Speedup | ~100 ms per token on 6.7B model |
| Energy/TOPS/W | — |
| Area | minimal hardware overhead per plane |
| PPA | — |
| Accuracy | full model accuracy maintained |
| Other | enables on-device multi-billion parameter inference |

## Baselines
- standard NAND flash mobile storage
- cloud LLM inference

## Limitations
Simulation-only results; practical implementation on actual smartphones not demonstrated; tokenization latency and memory bandwidth limitations not fully addressed.

## Tags
`pim` `mobile-ai` `nand-flash` `llm-inference` `on-device-ai`

## Primary Theme
On-device LLM inference via flash PIM

## Confidence
Low
