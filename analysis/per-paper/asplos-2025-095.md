# asplos-2025-095: PhasePrint: Exposing Cloud FPGA Fingerprints by Inducing Timing Faults at Runtime

**Venue:** ASPLOS  
**Confidence:** low (abstract only)

## Problem
Cloud FPGA vendors prevent tenants from localizing (identifying) specific FPGA instances to thwart attacks exploiting data remanence and analog covert channels. This paper addresses how to fingerprint and locate individual FPGAs in a shared cloud environment despite vendor-imposed restrictions on low-level hardware information.

## Motivation
FPGA localization is a double-edged capability: it enables adversaries to mount targeted attacks but also lets defenders rotate workloads away from long-term-occupied FPGAs that may leak data through remanence.

## Method
PhasePrint induces controlled timing faults at runtime by deliberately stressing the FPGA fabric (e.g., via ring oscillators or voltage/frequency manipulation) to elicit device-specific phase signatures in timing behavior. These signatures arise from process-variation-induced differences in propagation delays across FPGA instances. The resulting fault patterns are collected and used as a fingerprint to distinguish one physical FPGA from another without requiring privileged low-level access.

## Key Novelty
Using runtime-induced timing faults as a side-channel to generate per-FPGA physical fingerprints that bypass cloud vendor restrictions on hardware identification.

## Contributions
- A methodology to fingerprint cloud FPGA instances by eliciting and measuring runtime timing-fault patterns caused by process variation.
- Demonstration that FPGA localization is feasible even under cloud vendor restrictions that hide low-level hardware information.
- Analysis of the dual-use security implications of FPGA fingerprinting for both attackers (targeting) and defenders (workload rotation).

## Hardware Target
FPGA

## Technique Categories
security, circuit-design

## Workloads
(none specified)

## Metrics
Not stated.

## Baselines
Not discussed.

## Limitations
Evaluation details and fingerprinting accuracy/success rates are not available from the abstract alone.

## Tags
fpga, fingerprinting, security, timing-fault, cloud, side-channel

## Primary Theme
FPGA physical fingerprinting via induced timing faults
