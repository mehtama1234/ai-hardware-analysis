# micro-2025-071: Efficient Security Support for CXL Memory through Adaptive Incremental Offloaded (Re-)Encryption

**Venue:** MICRO  
**Confidence:** low (abstract-only)

## Problem
CXL memory expansion adopted in cloud TEE environments (Intel SGX/TDX, AMD SEV) requires XTS encryption for data confidentiality via CXL IDE, but XTS encryption's per-page sequential latency causes significant performance overhead on memory-intensive workloads. Dynamically switching encryption modes and re-encrypting pages adds further overhead.

## Motivation
CXL memory is increasingly deployed in cloud computing to expand DRAM bandwidth and capacity, but integrating it with TEE-protected enclaves creates a security overhead bottleneck that limits adoption in latency-sensitive workloads.

## Method
AIORE (Adaptive Incremental Offloaded Re-Encryption) combines CTR-mode encryption (low latency, parallelizable) for hot/frequently-accessed pages with XTS encryption (CXL IDE compliant) for cold pages. An access frequency monitor dynamically tracks per-page hotness and triggers encryption mode switches. Re-encryption is performed incrementally (only the affected page, not the whole region) and offloaded to compute resources within the CXL memory node, overlapping with normal memory traffic. Evaluated via gem5 full-system simulation across diverse benchmarks.

## Key Novelty
Offloading incremental per-page re-encryption to CXL memory node compute, dynamically adapting between CTR and XTS encryption per page based on runtime access frequency.

## Contributions
- Adaptive per-page encryption policy dynamically selecting CTR vs. XTS based on access frequency
- Incremental re-encryption limiting scope to changed pages rather than full memory regions
- Re-encryption offloaded to CXL memory node compute to hide latency
- Security overhead reduced 62.8% on average; residual overhead within 3.7% vs. insecure baseline

## Hardware Target
SoC (host processor + CXL memory node), chiplet (CXL device)

## Technique Category
security, memory-system, near-data-processing

## Workloads
HPC, database

## Metrics
- Other: Security overhead reduced 62.8% on average; overhead within 3.7% vs. insecure baseline (gem5)

## Baselines
CXL IDE with XTS-only encryption (secure baseline); insecure baseline (no encryption)

## Limitations
Evaluation is simulation-based (gem5); hardware area/power costs of CXL node compute for re-encryption are not discussed.

## Tags
cxl-memory, memory-encryption, tee, cloud-security, adaptive-security, near-data-processing

## Primary Theme
adaptive encryption for CXL memory in trusted execution environments
