# SOLA: Optimizing SLO Attainment for Large Language Model Serving with State-Aware Scheduling

**Venue:** MLSys 2025 | **ID:** mlsys-2025-018 | **Confidence:** high

## Problem
LLM serving systems use fixed scheduling policies that cause latency distribution bias between TTFT and TPOT and high inter-request variance, resulting in poor SLO attainment (as low as 45.5%).

## Method
Models scheduling as a per-iteration constrained optimization over execution order (Fi), request count (ni), and token count (ki). State Monitor tracks per-request TTFT/TPOT and system memory. Strategy Generator switches between two optimization problems (minimize max TPOT subject to TTFT, or vice versa) based on real-time SLO fulfillment ratios. Online polynomial cost models (Cp, Cd) predict iteration latency and are tuned via scaling ratio. Implemented on vLLM v0.4.2; evaluated on 8x A100 80GB.

## Novelty
First serving system formalizing LLM scheduling as per-iteration constrained optimization with real-time SLO state awareness and dynamic strategy switching.

## Metrics
- SLO attainment improved from 45.5% to 99.4% (Llama3-70B, ShareGPT)
- 1.04-1.27x more requests served at 90% SLO attainment
- 0.40-0.45% scheduling overhead

## Tags
`LLM-serving` `SLO` `scheduling` `TTFT` `TPOT` `constrained-optimization` `vLLM` `continuous-batching` `state-aware` `goodput`

**Primary theme:** inference-efficiency
