# asplos-2025-178: Gigaflow: Pipeline-Aware Sub-Traversal Caching for Modern SmartNICs

**Venue:** ASPLOS  
**Confidence:** low (abstract-only)

## Problem
vSwitch offloading to SmartNICs suffers from high cache miss rates and poor scalability as the flow rule space grows. Existing solutions cannot efficiently store or traverse large, complex flow rule tables required by modern cloud tenants, bottlenecking packet processing throughput.

## Motivation
Modern public and edge clouds rely on high-performance end-host network stacks for diverse tenant workloads (distributed training, fast inference), making vSwitch SmartNIC offloading a key scalability lever, undermined by inability to handle growing flow rule spaces.

## Method
Gigaflow breaks down packet processing into stages and caches the filtering results from each stage, rather than only caching the final result. When the SmartNIC processes new packets, it can reuse these cached intermediate results for stages that match, avoiding redundant searches through the flow rule tables. By knowing which intermediate results are safe to reuse, Gigaflow improves cache performance and maintains high throughput even when flow rule tables are very large.

## Key Novelty
Gigaflow caches intermediate filtering results from each pipeline stage instead of just the final lookup, allowing SmartNICs to reuse results across packets and handle very large flow tables efficiently.

## Contributions
- Pipeline-aware decomposition of vSwitch processing into cacheable sub-traversal units
- Sub-traversal caching mechanism improving hit rates for large flow rule spaces on SmartNICs
- Scalable flow rule matching for growing flow tables in modern multi-tenant clouds
- Improved throughput and reduced miss rates compared to existing SmartNIC offload solutions

## Hardware Target
SmartNIC, DPU

## Technique Category
cache, memory-system, scheduling, near-data-processing

## Workloads
LLM-training, LLM-inference, database

## Metrics
- Improved packet processing throughput and reduced cache miss rates (specific numbers not in abstract)

## Baselines
Existing SmartNIC vSwitch offload solutions

## Limitations
Not discussed; specific hardware platforms tested and quantitative throughput results are not provided in the abstract.

## Tags
smartnic, vswitch, flow-caching, packet-processing, cloud-networking

## Primary Theme
Pipeline-aware caching for scalable vSwitch offload on SmartNICs
