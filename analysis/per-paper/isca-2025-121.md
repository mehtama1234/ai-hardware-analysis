# ArtMem: Adaptive Migration in Reinforcement Learning-Enabled Tiered Memory

**Venue:** ISCA  
**Confidence:** low (abstract only)

## Problem
Tiered memory systems struggle with three key inefficiencies: existing migration policies perform inconsistently across workloads, fail to adapt when the fast-tier access rate is low, and miss opportunities to tune migration scope dynamically based on workload patterns, causing unnecessary page migrations.

## Motivation
As DRAM capacity growth stalls and emerging applications demand more memory, tiered memory systems (mixing HBM/DRAM with NVM/CXL) become essential for data centers, but static or rule-based migration policies leave significant performance on the table.

## Method
ArtMem is a reinforcement learning (RL)-driven tiered memory management framework that dynamically adapts migration policy to evolving workload behavior. The RL agent observes fast-tier access rates and workload access patterns at runtime to decide page placement, migration scope, and when to suppress migration entirely when the fast tier is underutilized. This closed-loop control replaces static thresholds with learned policies.

## Key Novelty
Using a reinforcement learning agent to jointly control migration strategy selection, scope tuning, and suppression in response to live workload evolution in tiered memory.

## Contributions
1. Identification of three structural limitations in existing tiered memory migration solutions
2. RL-driven framework (ArtMem) that dynamically adapts migration strategy to workload evolution
3. Adaptive migration scope tuning based on fast-tier access rate feedback
4. 35-172% performance improvement over state-of-the-art tiering systems across diverse workloads

## Hardware Target
CPU

## Technique Categories
memory-system, scheduling, near-data-processing

## Workloads
HPC, database

## Metrics
- Speedup: 35-172% performance improvement over state-of-the-art tiering systems

## Baselines
State-of-the-art tiered memory systems (specific names not given in abstract)

## Limitations
RL training cost and convergence behavior under rapidly shifting workloads are not discussed.

## Tags
tiered-memory, reinforcement-learning, page-migration, memory-management, data-center
