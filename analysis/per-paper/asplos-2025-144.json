{
  "id": "asplos-2025-144",
  "title": "FlexSP: Accelerating Large Language Model Training via Flexible Sequence Parallelism",
  "venue": "ASPLOS",
  "problem": "Existing sequence parallelism methods for long-context LLM training assume homogeneous (equal-length) sequences and apply a single static scattering strategy, causing severe load imbalance when real training corpora have heterogeneous, long-tail sequence length distributions.",
  "motivation": "Long-context LLM training requires sequence parallelism across devices to handle sequences exceeding single-device memory, but the mismatch between static homogeneous scattering and real-world variable-length sequences wastes device utilization and increases training time.",
  "method": "FlexSP improves sequence parallelism—splitting long sequences across multiple devices—by adjusting how many devices each sequence is split across based on its actual length. The system dynamically determines this for each batch: short sequences are split across fewer devices, long sequences across more. This keeps the workload balanced so no device sits idle. The system also optimizes how devices communicate with each other and how attention works to match this flexible strategy.",
  "key_novelty": "The core innovation is adjusting how many devices split each sequence based on that sequence's length, rather than using the same split for all sequences.",
  "contributions": [
    "Analysis of load imbalance caused by applying static sequence parallelism to heterogeneous-length training corpora",
    "FlexSP framework enabling flexible, per-sequence scattering strategies in sequence parallelism",
    "Demonstration of training throughput improvements over static sequence parallelism baselines on long-tail length distributions"
  ],
  "hardware_target": [
    "GPU"
  ],
  "technique_category": [
    "parallelism",
    "scheduling"
  ],
  "workloads": [
    "LLM-training",
    "transformer"
  ],
  "metrics": {
    "speedup": null,
    "energy_or_tops_w": null,
    "area": null,
    "ppa": null,
    "accuracy": null,
    "other": "improved training throughput over static sequence parallelism (specific numbers not in abstract)"
  },
  "baselines": [
    "existing static sequence parallelism methods (e.g., Megatron-SP, DeepSpeed-Ulysses)"
  ],
  "limitations": "The adaptive scattering strategy may introduce additional scheduling overhead for highly variable-length batches; this trade-off is not quantified in the abstract.",
  "tags": [
    "llm-training",
    "sequence-parallelism",
    "load-balancing",
    "long-context",
    "distributed-training",
    "heterogeneous-workload"
  ],
  "primary_theme": "Flexible sequence parallelism for heterogeneous-length LLM training",
  "confidence": "low"
}