# Medusa: Accelerating Serverless LLM Inference with Materialization

**Venue:** ASPLOS  
**Confidence:** low (abstract-only)

## Problem
Serverless LLM inference suffers from excessive cold-start latency from three stages: model weight loading, KV cache initialization (profiling memory reservations), and CUDA graph capturing for different batch sizes—all repeated on every cold start.

## Motivation
Serverless promises scalable and cost-efficient model inference, but LLMs introduce unique cold-start overheads (KV cache profiling and CUDA graph construction) not addressed by prior serverless optimization work.

## Method
Medusa pre-computes and persists KV cache initialization state and CUDA graphs for all relevant batch sizes as serialized snapshots on fast storage, so cold starts bypass both stages and load pre-built state directly to GPU. A catalog manages snapshots per model and batch configuration, with fast deserialization on cold start. Materialization is done offline or lazily on first warm start.

## Key Novelty
Materializing both the KV cache initialization state and pre-built CUDA graphs as persistent snapshots to eliminate the two dominant LLM-specific cold-start overheads in serverless inference.

## Contributions
- Characterization of KV cache initialization and CUDA graph capturing as dominant LLM cold-start overheads in serverless settings
- Medusa materialization framework pre-computing and persisting these states as reusable snapshots
- Snapshot management system for catalog, versioning, and fast GPU memory restore
- Significant reduction in LLM cold-start latency enabling practical serverless LLM serving

## Hardware Target
GPU

## Techniques
memory-system, scheduling, virtualization

## Workloads
LLM-inference

## Metrics
- Other: significant cold-start latency reduction for serverless LLM inference

## Baselines
Standard serverless LLM inference without materialization

## Limitations
Materialized snapshots may not be reusable across hardware configurations or driver versions; storage cost not fully discussed.

## Tags
serverless, llm-inference, cold-start, cuda-graphs, kv-cache, materialization
