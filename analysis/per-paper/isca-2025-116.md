# SpecEE: Accelerating Large Language Model Inference with Speculative Early Exiting

**Venue:** ISCA  
**Confidence:** low (abstract only)

## Problem
Early exiting for LLM inference is bottlenecked by the predictor, which must search over a vocabulary of ~30,000 tokens per layer and incurs up to 20% overall inference latency overhead. Existing predictors also fail to integrate with speculative decoding token trees without exponential mapping complexity.

## Motivation
Early exiting can skip expensive upper Transformer layers for easy tokens, but the vocabulary-scale predictor overhead and poor interaction with speculative decoding prevent its practical deployment at scale.

## Method
SpecEE introduces a speculative-model-based paradigm that uses a small draft model to reduce the token vocabulary search space for the early-exit predictor. It redesigns the predictor to exploit inherent data variation and GPU parallelism to cut computational complexity, uses layer-wise activation frequency profiling to weight predictor deployment non-uniformly across layers, and develops a tree-aware mapping that handles the full speculative decoding token tree without exponential blowup.

## Key Novelty
Using a speculative draft model to shrink the predictor's runtime search space so that early exiting and speculative decoding compose efficiently.

## Contributions
1. Identification of vocabulary size as the dominant source of early-exit predictor overhead (up to 20% latency)
2. Speculative-model-based predictor that reduces vocabulary search space and improves GPU parallelization
3. Layer-wise non-uniform predictor deployment guided by per-layer activation frequency statistics
4. Tree-aware early-exit mapping compatible with speculative decoding token trees at linear rather than exponential complexity

## Hardware Target
GPU

## Technique Categories
sparsity, scheduling, parallelism

## Workloads
LLM-inference

## Metrics
- Predictor overhead reduced from ~20%; specific end-to-end speedup not stated in abstract

## Baselines
- Standard early exiting LLM inference systems

## Limitations
Quantitative end-to-end speedup numbers and accuracy impact are not reported in the abstract.

## Tags
early-exit, speculative-decoding, llm-inference, predictor, gpu
