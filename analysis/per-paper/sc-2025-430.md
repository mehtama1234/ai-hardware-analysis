# Enabling Unstructured Sparse Fine-Tuning and Inference for Foundation Models on Wafer-Scale Engine

**Paper ID:** sc-2025-430
**Venue:** SC
**Authors:** Haoyu Zheng, Yifan Zeng, Linghao Song, Murali Emani, Wenqian Dong

## Problem

Foundation models fine-tuned on dense weights don't benefit from sparse pre-training; dense fine-tuning negates sparsity savings, requiring full-precision inference despite underlying sparse structure.

## Motivation

Downstream task adaptation (fine-tuning) on sparse pre-trained models should preserve sparsity to reduce inference cost; Cerebras wafer-scale engine efficiently handles unstructured sparsity.

## Method

Phoenix merges sparse pre-trained weights with low-rank adapters during fine-tuning. Preserves sparsity structure while adapting to downstream tasks. Leverages Cerebras CS-2 unstructured sparsity support for efficient inference.

## Key Novelty

First method to maintain sparsity during fine-tuning via sparse-weight + low-rank adapter merging; enables 50–60% sparsity on Cerebras without accuracy loss.

## Contributions

- Sparse weight + low-rank adapter merging for sparsity-preserving fine-tuning
- Demonstrated unstructured sparse inference on Cerebras CS-2
- Near 2× FLOP reduction at 50–60% sparsity with maintained accuracy
- 1.7× inference throughput improvement vs A100 GPU

## Hardware Targets

- Cerebras CS-2 (wafer-scale engine)
- NVIDIA A100 (baseline)

## Technique Categories

- model compression
- sparse inference
- fine-tuning
- foundation models

## Workloads

- large language model inference
- downstream task adaptation

## Metrics

- **sparsity_level:** 50–60%
- **flop_reduction:** near 2×
- **throughput_vs_a100:** 1.7×
- **accuracy:** comparable to dense baseline

## Baselines

- dense fine-tuning
- NVIDIA A100

## Limitations

Comparison limited to A100; Cerebras cost not discussed; scalability to larger models unclear; applicability to other sparse pre-training methods not evaluated.

## Tags

sparse-inference, foundation-models, Cerebras, model-compression, fine-tuning

## Primary Theme

Sparse inference for fine-tuned foundation models on wafer-scale

## Confidence

high
