# ByteFS: System Support for (CXL-based) Memory-Semantic Solid-State Drives

**Venue:** ASPLOS 2025  
**Confidence:** low (abstract-only)

## Problem
Memory-semantic SSDs expose both byte-addressable (PCIe/CXL) and block interfaces, but existing software systems designed for NVM-on-memory-bus or block-SSDs cannot exploit their dual-interface characteristics and distinct performance profiles.

## Motivation
CXL-attached memory-semantic SSDs offer scalable, affordable byte-addressable capacity, but realizing their potential requires new OS/filesystem abstractions tailored to their asymmetric dual-interface performance.

## Method
ByteFS is a filesystem for CXL memory-semantic SSDs that routes operations to the appropriate interface: byte interface for metadata and small random accesses; block interface for large sequential transfers. New OS abstractions handle allocation, caching, and I/O path with awareness of asymmetric byte vs. block performance characteristics and persistent byte-addressable semantics.

## Key Novelty
Dual-interface routing between byte and block paths of memory-semantic SSDs, with OS abstractions designed specifically for CXL-attached NAND flash memory semantics.

## Contributions
- Characterization of memory-semantic SSD dual-interface performance implications
- ByteFS file system with dual-interface routing
- New OS abstractions for CXL memory-semantic SSDs (allocation, caching, I/O path)

## Hardware Target
SoC (CXL-connected SSD)

## Technique Categories
memory-system, near-data-processing, cache

## Workloads
database, HPC

## Metrics
Not stated in abstract.

## Baselines
Existing NVM file systems; conventional block-interface SSDs

## Limitations
Performance characteristics depend on specific device implementations; firmware generality not discussed.

## Tags
cxl, memory-semantic-ssd, filesystem, byte-addressable, nand-flash
