# Micro-MAMA: Multi-Agent Reinforcement Learning for Multicore Prefetching

**Venue:** MICRO  
**Confidence:** low (abstract-only)

## Problem
Independent RL-based prefetchers on each core of a multicore system compete for shared memory bandwidth without coordination, causing each agent to greedily claim excessive bandwidth and converging to sub-optimal system-wide policies. This problem is an instance of the multi-agent credit assignment and non-stationarity challenge in MARL.

## Motivation
As core counts grow and memory bandwidth becomes increasingly shared, bandwidth-unaware per-core prefetchers degrade system throughput and fairness, motivating a coordinated multi-agent approach.

## Method
MAMA uses a two-tier architecture: distributed local multi-armed bandit agents on each core narrow the joint-action search space by selecting among local prefetch aggressiveness options, while a central supervisor agent with global bandwidth visibility learns joint policies that maximize system-wide throughput or fairness. Key local agents receive system-level perspective signals to discourage actions harming other cores. The supervisor uses a lightweight bandit over the local agents' reduced action space, avoiding exponential joint-action blow-up of fully centralized control.

## Key Novelty
A hierarchical supervisor that provides global bandwidth perspective to local bandit prefetchers, enabling coordinated joint policies without requiring centralized control of all per-core decisions.

## Contributions
- Identification of bandwidth competition as the core MARL failure mode for multicore RL prefetching
- MAMA hierarchical architecture combining distributed local bandit agents with a global supervisor
- 2.1% average throughput improvement and 10.4% fairness improvement over independently operating agents on an 8-core system
- Demonstration that gains increase further in bandwidth-constrained configurations

## Hardware Target
CPU

## Technique Categories
prefetching, scheduling

## Workloads
HPC

## Metrics
- Throughput: 2.1% improvement over independent agents
- Fairness: 10.4% improvement
- System: 8-core simulated evaluation

## Baselines
Independently operating per-core RL bandit prefetchers

## Limitations
Evaluation is on an 8-core simulated system; scalability to larger core counts and interaction with hardware prefetchers are not fully characterized.

## Tags
prefetching, reinforcement-learning, multicore, multi-agent, bandwidth, fairness
