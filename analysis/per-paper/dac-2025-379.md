# AASD: Accelerate Inference by Aligning Speculative Decoding in Multimodal Large Language Models

**Venue:** DAC
**ID:** dac-2025-379

## Problem
Multimodal LLM inference is slow due to auto-regressive decoding with long token sequences; traditional acceleration methods compromise quality or multimodal integration.

## Motivation
Speculative decoding can accelerate inference; alignment between draft and target models in multimodal settings is challenging due to complex token dependencies.

## Method
AASD leverages cached KV pairs from target model to generate draft tokens efficiently. A KV Projector compresses KV cache while preserving fidelity. Target-Draft Attention mechanism optimizes alignment between draft and target models.

## Key Novelty
Target-Draft Attention mechanism achieving alignment-aware speculative decoding for multimodal LLMs with minimal computational overhead.

## Contributions
1. Speculative decoding framework using target model KV pairs for efficient draft token generation
2. KV Projector for lossless KV cache compression maintaining representational fidelity
3. Target-Draft Attention mechanism optimizing draft-target alignment
4. Up to 2x inference speedup without accuracy loss across mainstream MLLMs

## Hardware Target
GPU

## Technique Categories
kernel-fusion, quantization

## Workloads
LLM-inference, vision

## Metrics
- speedup: 2x

## Baselines
MLLM baseline inference; speculative decoding without alignment

## Limitations
Applicability to other modalities (audio, video) and very large MLLMs (e.g., >70B params) not discussed.

## Tags
mllm, speculative-decoding, inference-acceleration, kv-cache, attention

## Primary Theme
Aligned speculative decoding for efficient multimodal LLM inference

## Confidence
low
