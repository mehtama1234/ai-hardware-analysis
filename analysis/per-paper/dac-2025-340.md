# Leopard: Hardware Pass-Through Remote Storage Access with Queue Concurrency for Edge Intelligent Workstations
**Venue:** DAC

## Problem
Edge workstations access remote cloud storage via complex software stacks and limited CPUs, causing 75% performance degradation by failing to saturate NVMe queue concurrency.

## Motivation
Edge AI workloads require high-throughput remote storage access; eliminating CPU-mediated I/O overhead enables near-local-storage performance for cloud-backed systems.

## Method
Leopard implements a custom NVMe controller on SmartNIC FPGA that emulates NVMe device and directly handles remote storage operations via parallelized hardware circuits; passes NVMe commands directly from edge OS to FPGA, bypassing CPU stacks.

## Key Novelty
Hardware pass-through I/O controller for remote NVMe using SmartNIC FPGA, eliminating CPU-mediated software stacks.

## Contributions
1. 1.09-6.04x lower remote storage access latency
2. Direct hardware queue concurrency management via FPGA
3. Eliminates complex remote storage software stacks

## Hardware Target
SmartNIC, FPGA

## Technique Categories
interconnect, near-data-processing

## Workloads
N/A

## Metrics
- latency_reduction: 1.09-6.04x

## Baselines
SOTA remote storage solutions

## Limitations
Evaluation limited to realistic workloads for edge; large-scale cluster storage not evaluated.

## Tags
smartnic, fpga, i-o, edge-computing, remote-storage

## Primary Theme
FPGA-based pass-through remote storage controller

## Confidence
low
