# SC-2025-407: Fast on-demand Memory Mapping for Shared Memory and Disaggregated Systems

## Summary
This work addresses memory mapping synchronization overhead in disaggregated systems by optimizing the XPMEM-inspired shared-memory driver. Three key optimizations—parallel batched pinning, cache-policy lookup bypass, and dynamic re-registration—reduce cold-start memory copy by up to 13.22x over XPMEM.

## Problem
Efficient memory mapping between processes is increasingly costly as systems become disaggregated and heterogeneous. Fault-driven mapping models require expensive page table walks and updates, creating a bottleneck for shared memory export.

## Method
- **Parallel batched on-demand pinning**: Enables concurrent pinning operations
- **Cache-policy lookup bypass**: Eliminates unnecessary lookups in PFN mapping
- **Dynamic re-registration**: Expands registered regions without tearing down existing mappings, preserving mapping state

## Key Results
- 13.22x speedup in cold-start memory copy over XPMEM for multi-process workloads
- Particularly effective for collective communication patterns
- Benefits demonstrated for rapidly resizing buffers

## Hardware Target
- Multi-processor systems
- Disaggregated memory architectures
- GPU systems with shared memory
- Heterogeneous memory environments

## Technique Categories
- Memory management optimization
- Page table optimization
- System interconnect design
- Fault-driven mapping models

## Limitations
- Analysis based on abstract only (no full text available)
- Evaluation scope not detailed beyond general workload classes
- Applicability to specific GPU disaggregation models not fully explored

## Connection to Hardware Analysis
This work bridges traditional system memory management (page tables, synchronization) with emerging disaggregated hardware architectures, addressing a critical infrastructure challenge for modern HPC and AI systems where memory may be physically remote.
