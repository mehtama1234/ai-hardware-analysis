# AMALI: An Analytical Model for Accurately Modeling LLM Inference on Modern GPUs

**Venue:** ISCA  
**Confidence:** low (abstract-only analysis)

## Problem
Existing GPU analytical models cannot accurately predict LLM inference performance because they mismodel tensor core throughput, ignore constant and instruction cache effects, and abstract away warp-level execution details critical to LLM kernels.

## Motivation
Architects rely on analytical models to quickly identify bottlenecks and explore design spaces; errors exceeding 100% MAPE in current models make them unreliable for LLM inference workloads on modern GPUs such as A100.

## Method
AMALI improves standard GPU models in three ways, all validated on A100 hardware. First, it measures instruction flow through tensor cores and identifies pipeline stalls caused by insufficient queued work—delays that simpler models overlook—instead of guessing at throughput. Second, it measures instruction and constant cache latencies directly via micro-benchmark kernels rather than assuming constant speeds. Third, it tracks heterogeneous warp execution, accounting for the fact that different thread groups execute different numbers of instructions, affecting occupancy and scheduling—details existing models simplify.

## Key Novelty
Explicit measurement of instruction and constant cache latencies combined with tensor-core stall modeling reduces LLM inference prediction error on A100 GPUs from 127.56% to 23%.

## Contributions
1. Instruction-modifier and throughput-based tensor core model capturing math-pipe throttle stalls
2. Analytical constant-cache and instruction-cache models derived from CUDA micro-benchmarks
3. Multi-warp execution model using warp instruction number distribution for LLM inference characteristics
4. Validation on A100 GPU reducing MAPE from 127.56% to 23%

## Metrics
- **Accuracy:** MAPE reduced from 127.56% to 23% on A100

## Baselines
- Existing GPU analytical models (prior work, MAPE ~127.56%)

## Hardware Target
GPU

## Technique Category
compiler, memory-system

## Workloads
LLM-inference, transformer

## Limitations
Validation is limited to A100 GPU; applicability to other GPU microarchitectures requires additional micro-benchmark re-characterization.

## Tags
analytical-model, LLM-inference, GPU, tensor-core, performance-modeling
