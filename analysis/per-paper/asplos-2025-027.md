# HyperHammer: Breaking Free from KVM-Enforced Isolation

**ID:** asplos-2025-027  
**Venue:** ASPLOS  
**Confidence:** low (abstract-only)  
**Primary Theme:** Rowhammer-based hypervisor escape from KVM guest VMs

## Problem
Existing Rowhammer attacks against virtualized cloud environments can compromise co-resident VMs but have not demonstrated the ability to attack the hypervisor itself, and a key primitive enabling such attacks has been disabled by hypervisor mitigations. This paper develops a new Rowhammer attack capable of breaking KVM-enforced isolation to compromise the hypervisor from a guest VM.

## Motivation
Cloud hypervisors enforce the security boundary between tenants; a successful hypervisor compromise via hardware memory vulnerabilities would undermine the fundamental trust model of multi-tenant cloud infrastructure.

## Method
HyperHammer constructs a new Rowhammer attack path from within a KVM guest VM that circumvents existing KVM-level mitigations. The attack likely leverages alternative hammering primitives or physical memory addressing techniques (e.g., virtual-to-physical mapping exploitation, huge-page-based addressing) to induce bit flips in hypervisor memory regions, enabling privilege escalation from guest to hypervisor level.

## Key Novelty
First demonstrated Rowhammer attack that escapes KVM-enforced guest isolation to directly compromise the hypervisor, bypassing the mitigations that rendered prior hypervisor Rowhammer attacks obsolete.

## Contributions
- Novel Rowhammer attack methodology that targets the KVM hypervisor from a guest VM in a contemporary virtualized environment.
- Circumvention of existing KVM mitigations that had previously disabled the key primitive used by earlier attacks.
- Demonstration of cross-privilege-boundary bit-flip exploitation to achieve hypervisor compromise.

## Hardware Target
CPU

## Technique Categories
security, reliability

## Workloads
database

## Metrics
- **Other:** successful hypervisor compromise demonstrated; specific success rates not stated in abstract

## Baselines
- prior Rowhammer attacks against virtualized environments

## Limitations
Not discussed in abstract; scope of affected hardware platforms and DRAM generations is not specified.

## Tags
rowhammer, virtualization, kvm, hypervisor, security, side-channel
