# Vectron: A Dynamic Programming Auto-vectorization Framework

**Venue:** CGO
**Confidence:** low

## Problem
Dynamic programming algorithms (especially sequence alignment) are computation bottlenecks in genomics and other fields; hand-vectorization is tedious and error-prone. Auto-vectorization of array-based DP code is needed.

## Motivation
DP algorithms are widely used but computationally expensive; SIMD and GPU acceleration via auto-vectorization can deliver massive speedups without algorithm changes.

## Method
Vectron targets Python array-based DP implementations and auto-converts to vectorized C++ equivalents exploiting SIMD (CPU) and GPU parallelism. Processes multiple problem instances in parallel using SIMD/GPU primitives without requiring algorithm modifications.

## Key Novelty
Automatic vectorization of array-based DP from Python to SIMD/GPU-accelerated code without algorithm changes.

## Contributions
- Auto-vectorization framework for Python array-based DP algorithms
- SIMD and GPU parallelization without code rewriting
- 10-20x speedups over conventional C++ and manual implementations

## Hardware Targets
GPU, CPU

## Technique Categories
compiler, parallelism

## Workloads
genomics

## Metrics
{'speedup': '10-20x over C++ and manual implementations'}

## Baselines
Conventional C++ DP, Manual state-of-the-art DP implementations

## Limitations
Limited to array-based DP; other algorithmic patterns not discussed.

## Tags
auto-vectorization, simd, genomics

## Primary Theme
auto-vectorization of dynamic programming algorithms
