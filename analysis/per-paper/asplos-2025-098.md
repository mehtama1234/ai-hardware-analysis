# asplos-2025-098: FLEXPROF: Flexible, Side-Channel-Free Memory Access

**Venue:** ASPLOS  
**Confidence:** low (abstract only)

## Problem
Secure memory controllers that prevent microarchitectural side-channels by allocating fixed 'turns' to co-scheduled VMs impose a ~2x performance slowdown because the schedule must conservatively handle worst-case mixed read/write patterns, leaving memory bus utilization inefficient.

## Motivation
Eliminating timing-based memory side-channels in shared cloud systems is essential for VM isolation, but the current overhead of 2x slowdown makes secure scheduling impractical for production workloads.

## Method
FlexProf introduces two types of memory controller turns: read-optimized turns and write-optimized turns, selected via coarse-grain application profiling that observes the read/write ratio without leaking sensitive information. Each turn type is sized and structured for its dominant operation. Flexibility is added so that a read-optimized turn can opportunistically issue writes when read slots are idle (and vice versa), improving utilization while preserving the fixed-turn structure needed to prevent timing side-channels between VMs.

## Key Novelty
Differentiating memory controller scheduling turns into read-optimized and write-optimized types, determined by side-channel-free coarse profiling, to recover throughput lost by worst-case-only secure scheduling.

## Contributions
- Read-optimized and write-optimized turn types matched to application access patterns via coarse profiling.
- Opportunistic cross-turn operation issuance that preserves isolation guarantees while improving utilization.
- Up to 33% performance improvement (geometric mean 8%) over state-of-the-art on mixed workloads.

## Hardware Target
CPU

## Technique Categories
memory-system, security, scheduling

## Workloads
Mixed memory-intensive workloads (not named).

## Metrics
- Performance: up to 33% improvement, geometric mean 8% over state-of-the-art secure memory scheduling
- Over half of memory-intensive programs show >10% gain

## Baselines
State-of-the-art secure memory scheduling methods.

## Limitations
Hardware implementation complexity and the profiling mechanism's ability to handle dynamically varying workloads are not discussed.

## Tags
memory-controller, security, side-channel, scheduling, virtualization, secure-memory

## Primary Theme
Read/write-differentiated turn scheduling for side-channel-free memory controllers
