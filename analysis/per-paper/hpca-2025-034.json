{
  "id": "hpca-2025-034",
  "title": "GSArch: Breaking Memory Barriers in 3D Gaussian Splatting Training via Architectural Support",
  "venue": "HPCA",
  "problem": "3D Gaussian Splatting (3DGS) training is bottlenecked by three memory system inefficiencies: redundant off-chip DRAM loads for large 'hot' Gaussians accessed repeatedly across pixels, expensive atomic write operations during gradient accumulation, and severe bank conflicts during on-chip buffer reads during backward propagation.",
  "motivation": "Online 3DGS training for dynamic environment perception (autonomous driving, embodied AI) requires much higher training throughput than current GPU implementations provide; existing 3DGS accelerators (e.g., GSCore) address only rendering, not the backward pass.",
  "method": "GSArch fixes the three main slowdowns in 3D Gaussian Splatting training with three targeted hardware techniques. First, it identifies which Gaussian data objects are accessed repeatedly (called 'hot' Gaussians) and stores them in fast on-chip memory rather than fetching them from slower main memory each time—this eliminates the dominant source of redundant memory traffic. Second, it recognizes that many gradients (values used to update the model during training) contribute very little to the final result, so it skips the expensive atomic write operations for these low-value gradients. Third, it reorganizes memory read requests to avoid 'bank conflicts'—a problem where multiple reads hitting the same memory location simultaneously force them to queue up—by bundling conflicting requests into non-conflicting groups. These three changes together speed up the backward pass of 3DGS training.",
  "key_novelty": "The core insight is automatically identifying which Gaussians are used repeatedly and caching them in fast on-chip memory instead of constantly fetching them from slower main memory, which eliminates the single biggest bottleneck in 3DGS training.",
  "contributions": [
    "Characterization of 3DGS training inefficiencies: redundant DRAM loads, atomic write bottlenecks, and on-chip bank conflicts.",
    "Hybrid memory management classifying Gaussians into hot/cold categories with on-chip buffering of hot Gaussians.",
    "Gradient informativeness filtering to skip low-contribution atomic gradient writes.",
    "Rearrangement unit eliminating on-chip buffer bank conflicts; up to 6.49x speedup over A100 and 15.42x over Jetson AGX Xavier with negligible quality loss."
  ],
  "hardware_target": [
    "ASIC",
    "GPU"
  ],
  "technique_category": [
    "memory-system",
    "dataflow",
    "near-data-processing"
  ],
  "workloads": [
    "vision"
  ],
  "metrics": {
    "speedup": "Up to 6.49x over A100 GPU; up to 15.42x over Jetson AGX Xavier",
    "energy_or_tops_w": "Substantially lower energy consumption (not quantified in abstract)"
  },
  "baselines": [
    "NVIDIA A100 GPU",
    "NVIDIA Jetson AGX Xavier"
  ],
  "limitations": "The hot/cold Gaussian classification threshold tuning strategy and its sensitivity to scene complexity or Gaussian density are not discussed.",
  "tags": [
    "3d-gaussian-splatting",
    "neural-scene-representation",
    "memory-management",
    "gradient-sparsity",
    "training-accelerator"
  ],
  "primary_theme": "Architectural support for memory-efficient 3D Gaussian Splatting training via hot/cold Gaussian buffering",
  "confidence": "low"
}