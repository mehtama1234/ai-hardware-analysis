# asplos-2025-151: Stramash: A Fused-Kernel Operating System For Cache-Coherent, Heterogeneous-ISA Platforms

**Venue:** ASPLOS  
**Confidence:** low (abstract-only)

## Problem
Heterogeneous-ISA processors require disjoint OS kernels per ISA with heavy coordination overhead; existing multi-kernel designs use message-passing rather than exploiting cache-coherent shared memory, leaving performance on the table.

## Motivation
Cache-coherent shared memory among heterogeneous-ISA CPUs provides an unexploited substrate for a unified OS design that could eliminate multi-kernel coordination overhead.

## Method
Stramash introduces the fused-kernel OS: a single unified kernel image running across heterogeneous-ISA CPUs using cache-coherent shared kernel data structures, with new ISA-transparent synchronization, memory management, and scheduling mechanisms. Prototype is Stramash-Linux on a multi-ISA QEMU simulator (Stramash-QEMU).

## Key Novelty
Fused-kernel OS treating cache-coherent shared memory as the first-class substrate for a single kernel spanning heterogeneous ISA boundaries.

## Contributions
- Fused-kernel OS design exploiting cache-coherent shared memory among heterogeneous-ISA CPUs
- New OS mechanisms for ISA-transparent synchronization, scheduling, and memory management
- Stramash-Linux prototype extending monolithic Linux across ISAs
- Stramash-QEMU simulator with <4% error; up to 2.1x speedup over multi-kernel OS on NPB

## Hardware Target
CPU, RISC-V

## Techniques
coherence, virtualization, scheduling

## Workloads
HPC

## Metrics
- Speedup: up to 2.1x over multi-kernel OS on NPB benchmarks
- Simulator accuracy: <4% average error vs. real hardware

## Baselines
State-of-the-art multi-kernel OS designs (e.g., McKernel, Popcorn Linux)

## Limitations
Evaluation partially simulation-based; only NPB benchmarks reported.

## Tags
heterogeneous-isa, operating-system, cache-coherence, fused-kernel, risc-v
