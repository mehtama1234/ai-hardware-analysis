{
  "id": "dac-2024-044",
  "title": "SecPaging: Secure Enclave Paging with Hardware-Enforced Protection against Controlled-Channel Attacks",
  "venue": "DAC",
  "year": 2024,
  "problem": "Trusted Execution Environments like Intel SGX are vulnerable to controlled-channel attacks where untrusted operating systems manipulate page tables or observe page faults to deduce enclave secrets.",
  "motivation": "TEEs are widely adopted for privacy-preserving computing, but current paging mechanisms lack sufficient hardware-level protection against controlled-channel attacks that can leak sensitive enclave data.",
  "method": "SecPaging protects an enclave's page tables (the OS's record of where code and data are stored in memory) using hardware barriers that block even privileged OS code from tampering. It adds Eager-Allocation to pre-reserve pages and prevent allocation-observation attacks, and Record-Reload to hide page swaps and prevent swap-observation attacks. These mechanisms work by keeping the OS from seeing enclave memory management decisions—the exact information attackers exploit in controlled-channel attacks (where they spy on page table activity to deduce secrets). The design is validated on real Intel SGX hardware.",
  "key_novelty": "SecPaging prevents attacks that spy on page table activity by having hardware hide the enclave's allocation and swapping decisions from the operating system through Eager-Allocation and Record-Reload mechanisms.",
  "contributions": [
    "Hardware isolation mechanism for protecting enclave page table entries against privileged OS tampering",
    "Eager-Allocation technique to prevent allocation-based controlled-channel attacks",
    "Record-Reload mechanism to prevent swap-based controlled-channel attacks",
    "Prototype implementation and evaluation on SGX with minimal performance overhead"
  ],
  "hardware_target": [
    "CPU"
  ],
  "technique_category": [
    "security",
    "memory-system"
  ],
  "workloads": [],
  "metrics": {
    "other": "minimal performance overhead"
  },
  "baselines": [],
  "limitations": "Not discussed in abstract",
  "tags": [
    "enclave",
    "side-channel",
    "paging",
    "tee",
    "security"
  ],
  "primary_theme": "secure enclave paging against controlled-channel attacks",
  "confidence": "medium"
}