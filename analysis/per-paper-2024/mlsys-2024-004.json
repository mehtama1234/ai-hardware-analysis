{
  "id": "mlsys-2024-004",
  "title": "SLoRA: Scalable Serving of Thousands of LoRA Adapters",
  "venue": "MLSys",
  "year": 2024,
  "problem": "Serving thousands of different LoRA adapters concurrently requires prohibitive memory and compute resources, as naive approaches load each adapter independently. Existing systems only scale to dozens of adapters.",
  "motivation": "Multi-tenant LLM inference systems need to serve many customized models via parameter-efficient LoRA adapters for different users and tasks simultaneously.",
  "method": "SLoRA serves thousands of different LoRA adapters (small model customizations) from a single GPU by swapping adapters in and out of memory as requests arrive, rather than keeping them all loaded. The system batches together requests for different adapters so the GPU processes them in parallel, uses shared memory pools instead of giving each adapter its own space, and reuses computation across requests (prefix caching). These techniques let thousands of specialized models run on a single commodity GPU without exhausting memory or compute resources.",
  "key_novelty": "SLoRA is the first system to serve thousands of LoRA adapters on a single commodity GPU by scheduling which adapters to load when and dynamically sharing GPU memory between them.",
  "contributions": [
    "Scheduling algorithm for efficient adapter batching and memory placement",
    "Memory virtualization layer for managing thousands of adapters",
    "Demonstration of 1000+ concurrent adapters on single GPU",
    "10-20x throughput improvement over naive per-adapter serving"
  ],
  "hardware_target": [
    "GPU"
  ],
  "technique_category": [
    "scheduling",
    "virtualization",
    "memory-system"
  ],
  "workloads": [
    "LLM-inference",
    "transformer",
    "attention"
  ],
  "metrics": {
    "speedup": "10-20x throughput over baseline",
    "accuracy": "preserved"
  },
  "baselines": [
    "vLLM",
    "DeepSpeed",
    "per-adapter loading"
  ],
  "limitations": "Specific hardware constraints and workload limitations not discussed in title.",
  "tags": [
    "slora",
    "lora-adapters",
    "inference-serving",
    "scalability",
    "llm",
    "efficient-serving"
  ],
  "primary_theme": "Scalable multi-tenant LLM inference with parameter-efficient adapters",
  "confidence": "low"
}