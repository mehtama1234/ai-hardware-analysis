{
  "id": "hpca-2024-018",
  "title": "PruneGNN: Algorithm-Architecture Pruning Framework for Graph Neural Network Acceleration",
  "venue": "HPCA",
  "year": 2024,
  "problem": "GNNs face tight latency constraints with massive, unstructured, sparse graphs. Irregular pruning techniques fail to exploit parallelism on vectorized hardware due to diminished sparse matrix multiplication performance, while existing structured pruning introduces scalability challenges that prevent exploiting full GPU parallelism potential.",
  "motivation": "Real-world GNN applications require acceleration under tight latency constraints, but existing pruning approaches either sacrifice exploitable parallelism or introduce architectural inefficiencies.",
  "method": "PruneGNN removes entire dimensions from GNN models during training, reducing model size while maintaining sparse structure that GPUs can work with. Custom GPU code then executes these pruned models, specifically designed to parallelize across multiple matrix operations at once (SIMD-aware kernels: doing many calculations simultaneously). By matching the pruning strategy to how GPUs parallelize work, the approach achieves performance gains that wouldn't be possible with irregular or previous structured pruning methods.",
  "key_novelty": "Custom GPU kernels that parallelize at the matrix-operation level allow dimension-pruned GNN models to run efficiently on vectorized GPUs, overcoming the performance loss that pruning normally causes.",
  "contributions": [
    "Quantitative analysis showing irregular sparsity cannot be exploited on vectorized parallel architectures",
    "Dimension-pruning-aware sparse training method achieving high sparsity with maintained accuracy",
    "Novel SIMD-aware kernels exploiting matrix-operator-level parallelism for reduced-dimension models",
    "2x average speedup over prior structured pruning methods on NVIDIA A100"
  ],
  "hardware_target": [
    "GPU"
  ],
  "technique_category": [
    "pruning",
    "sparsity",
    "parallelism",
    "kernel-fusion"
  ],
  "workloads": [
    "GNN"
  ],
  "metrics": {
    "speedup": "2x over prior structured pruning work",
    "accuracy": "maintained"
  },
  "baselines": [
    "prior structured pruning work for GNNs"
  ],
  "limitations": "Evaluation limited to NVIDIA A100 GPU; generalization to other hardware platforms and scalability to larger graphs not discussed.",
  "tags": [
    "pruning",
    "gnn",
    "sparsity",
    "gpu-acceleration",
    "kernel-optimization",
    "structured-sparsity"
  ],
  "primary_theme": "structured pruning and SIMD-aware kernel optimization for GNN acceleration",
  "confidence": "high"
}