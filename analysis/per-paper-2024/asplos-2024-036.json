{
  "id": "asplos-2024-036",
  "title": "MaxEmbed: Maximizing SSD bandwidth utilization for huge embedding models serving",
  "venue": "ASPLOS",
  "year": 2024,
  "problem": "Large embedding tables in deep learning recommendation models create significant memory capacity and cost challenges. SSDs introduce read amplification due to mismatch between embedding sizes and SSD page granularity, and prior hypergraph partitioning approaches create disjoint embedding clusters that limit potential combinations.",
  "motivation": "Cost-effective and scalable storage solutions for massive embedding tables are essential for recommendation systems in production.",
  "method": "MaxEmbed arranges embedding tables more efficiently on SSDs by allowing multiple embeddings to overlap across pages (fixed-size chunks of data that SSDs read together). Traditional approaches partition embeddings into completely separate clusters to avoid conflicts, but this wastes storage space and creates read amplification—inefficient disk reads when you need one embedding but have to load an entire page containing unrelated data. MaxEmbed instead finds flexible combinations of embeddings that fit tightly on each page, reducing those wasted reads and extracting more value from each disk access.",
  "key_novelty": "MaxEmbed allows embeddings to overlap across SSD pages instead of forcing them into completely separate clusters, reducing wasted disk reads.",
  "contributions": [
    "Identifies fundamental limitations of disjoint clustering in prior hypergraph partitioning approaches",
    "Proposes flexible embedding co-location strategy for improved SSD page utilization",
    "Addresses read amplification problem in SSD-based embedding serving"
  ],
  "hardware_target": [
    "CPU"
  ],
  "technique_category": [
    "memory-system",
    "cache",
    "dataflow"
  ],
  "workloads": [
    "DLRM",
    "recommendation"
  ],
  "metrics": {
    "speedup": "not provided in abstract",
    "accuracy": "not applicable"
  },
  "baselines": [
    "SSD embedding systems with hypergraph partitioning"
  ],
  "limitations": "Not discussed in the provided abstract",
  "tags": [
    "embedding",
    "ssd",
    "recommendation",
    "storage",
    "bandwidth",
    "memory"
  ],
  "primary_theme": "SSD bandwidth optimization for embedding serving",
  "confidence": "low"
}