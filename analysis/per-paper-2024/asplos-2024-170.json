{
  "id": "asplos-2024-170",
  "title": "BypassD: Enabling fast userspace access to shared SSDs",
  "venue": "ASPLOS",
  "year": 2024,
  "problem": "Kernel software I/O stack introduces substantial overhead for ultra-low latency storage devices like Optane NVMe SSDs. Userspace approaches avoid this overhead but lack proper mechanisms for coordinating shared storage access without major changes to filesystems, OS, or hardware.",
  "motivation": "Modern SSDs with microsecond latencies and gigabytes-per-second bandwidth require eliminating kernel I/O stack overhead to deliver their theoretical performance.",
  "method": "Normally, programs access storage through the kernel, the core system software that routes all disk requests, which adds latency overhead. BypassD lets programs directly access an SSD, and when multiple programs use the same SSD, they coordinate their accesses through mechanisms in userspace (instead of having the kernel do it), without requiring changes to the kernel, filesystem, or hardware.",
  "key_novelty": "BypassD is the first system that lets multiple programs directly access the same SSD from userspace (the programs themselves rather than through the kernel), without requiring changes to the kernel, filesystem, or hardware.",
  "contributions": [
    "BypassD userspace SSD access framework",
    "Shared storage coordination mechanism without OS/filesystem changes",
    "Kernel bypass I/O optimization reducing microsecond-scale latencies",
    "Practical userspace storage access for concurrent applications"
  ],
  "hardware_target": [
    "CPU"
  ],
  "technique_category": [
    "virtualization",
    "memory-system"
  ],
  "workloads": [
    "database"
  ],
  "metrics": {
    "speedup": "latency reduction through kernel I/O stack bypass",
    "other": "bandwidth utilization improvement"
  },
  "baselines": [
    "kernel I/O stack",
    "traditional userspace storage approaches"
  ],
  "limitations": "Limitations not discussed in the abstract.",
  "tags": [
    "storage",
    "userspace",
    "i/o-optimization",
    "latency",
    "ssd",
    "kernel-bypass"
  ],
  "primary_theme": "userspace SSD access with shared storage",
  "confidence": "medium"
}