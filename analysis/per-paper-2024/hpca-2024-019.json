{
  "id": "hpca-2024-019",
  "title": "Bandwidth-Effective DRAM Cache for GPU s with Storage-Class Memory",
  "venue": "HPCA",
  "year": 2024,
  "problem": "GPUs have limited HBM capacity for memory-oversubscribed workloads. Storage-Class Memory offers higher capacity but higher latency and lower bandwidth than DRAM, requiring an effective DRAM cache design to bridge the performance gap.",
  "motivation": "Extending GPU memory capacity with SCM enables larger datasets and improves performance for memory-intensive applications.",
  "method": "The system uses four coordinated techniques to make DRAM work as an effective cache layer between the GPU's fast local memory (HBM) and slower Storage-Class Memory. A bypass policy skips caching data the GPU rarely uses; a Configurable Tag Cache (CTC) reuses the GPU's L2 cache to quickly find what data is cached; and Aggregated Metadata-In-Last-column (AMIL) groups all cache tags into a single column—these three reduce memory traffic while keeping error-checking (ECC) intact. Storage-Class Memory throttling and dynamic mode switching then optimize bandwidth based on access patterns.",
  "key_novelty": "Aggregated Metadata-In-Last-column (AMIL) groups all cache tags into a single DRAM column to minimize memory probes when finding cached data, while maintaining full error-checking protection.",
  "contributions": [
    "SCM-aware DRAM cache bypass policy for GPU memory access characteristics",
    "Configurable Tag Cache (CTC) design to reduce DRAM probe traffic with adjustable L2 capacity",
    "Aggregated Metadata-In-Last-column (AMIL) organization with full ECC retention versus TAD",
    "Heterogeneous Memory Stack (HMS) architecture combining stacked DRAM and SCM"
  ],
  "hardware_target": [
    "GPU"
  ],
  "technique_category": [
    "memory-system",
    "cache",
    "power"
  ],
  "workloads": [
    "HPC"
  ],
  "metrics": {
    "speedup": "12.5× vs HBM (2.9× overall)",
    "energy_or_tops_w": "89.3% reduction vs HBM (48.1% overall)"
  },
  "baselines": [
    "HBM",
    "prior DRAM cache implementations with TAD organization"
  ],
  "limitations": "Not discussed in the abstract",
  "tags": [
    "dram-cache",
    "gpu-memory",
    "storage-class-memory",
    "memory-hierarchy",
    "heterogeneous-memory"
  ],
  "primary_theme": "GPU memory capacity extension via DRAM-SCM hierarchy optimization",
  "confidence": "high"
}