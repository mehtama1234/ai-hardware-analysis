{
  "run_id": "colab-real-model-fused-paged-decode-gpt2",
  "mode": "real-model-fused-paged-decode",
  "model": "openai-community/gpt2",
  "device": "cuda",
  "purpose": "Run full GPT-2 greedy decode with the CUDA paged-attention kernel in every one-token decode attention layer on a Colab T4.",
  "required_output": "batch1-decode-vertical-slice/reports/real-model-fused-paged-decode.json",
  "acceptance": "generated-token parity against native GPT-2 for every prompt; synchronized end-to-end timings reported; no production or silicon claim"
}
