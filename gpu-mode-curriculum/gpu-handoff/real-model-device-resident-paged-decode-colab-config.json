{
  "run_id": "colab-real-model-device-resident-paged-decode-gpt2",
  "mode": "real-model-device-resident-paged-decode",
  "model": "openai-community/gpt2",
  "device": "cuda",
  "purpose": "Run full GPT-2 decode with device-side K/V page packing and CUDA paged attention on every one-token decode layer on a Colab T4.",
  "required_output": "batch1-decode-vertical-slice/reports/real-model-device-resident-paged-decode.json",
  "acceptance": "generated-token parity against native GPT-2 for every prompt; synchronized end-to-end timing and memory reported; no production or silicon claim"
}
