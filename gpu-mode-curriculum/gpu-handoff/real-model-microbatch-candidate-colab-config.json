{
  "run_id": "colab-real-model-microbatch-candidate-gpt2",
  "mode": "real-model-microbatch-candidate",
  "model": "openai-community/gpt2",
  "device": "cuda",
  "purpose": "Measure a real GPT-2 padded microbatch serving candidate against sequential service on Colab T4.",
  "required_output": "batch1-decode-vertical-slice/reports/real-model-microbatch-candidate.json",
  "acceptance": "exact per-request generated-token parity is mandatory"
}
