{
  "run_id": "colab-real-model-paged-attention-kernel-gpt2",
  "mode": "real-model-paged-attention-kernel",
  "model": "openai-community/gpt2",
  "device": "cuda",
  "purpose": "Measure CUDA paged-attention parity and launch timing using real GPT-2 layer-0 K/V tensors on a Colab T4.",
  "required_output": "batch1-decode-vertical-slice/reports/real-model-paged-attention-kernel.json",
  "acceptance": "CUDA output max error below 2e-5 for all tested contexts; timing is reported, with no full-model or silicon claim"
}
