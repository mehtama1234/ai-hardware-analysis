# Local profile-to-workload decision

Runtime decision: **digital reference and deterministic fallback only**.

Analog-advantage decision: **unresolved**.

4/9 declared gates pass. The model replay and fallback plumbing are useful local evidence, but the converter profile, timing, matched cost, and analog authorization gates remain open. `analog_authorized` remains `false`.
