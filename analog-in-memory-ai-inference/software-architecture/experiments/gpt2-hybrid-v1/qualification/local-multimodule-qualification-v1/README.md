# Multi-module local qualification decision

The joint ADC12 replay covers 3 GPT-2 modules and fails the provisional task-quality screen at 0.9365 teacher-forced argmax agreement, while preserving 4/4 exact generations.

This is a bounded negative local result. Per-module calibration, governed runtime tracing, matched cost, and physical authorization remain open.
